"""Fork-owned web layer.

Moved out of the former ``channel/web/web_channel.py`` monolith so the fork no
longer edits upstream core files. See the change
``openspec/changes/adopt-upstream-web-split`` (design D2).
"""
