# database-memory-console Specification

## Purpose
恢复数据库模式中的记忆列表与正文浏览，并将既有记忆页面连接到统一的个人记忆管理能力。个人、私有智能体及公共记忆必须按真实归属区分，确保界面入口、文件访问和检索得到一致的隐私与授权结果。
## Requirements
### Requirement: 共享记忆服务在实际文件使用时验证真实归属

个人记忆 SHALL 由已验证 tenant/user 解析可信根，并在列表枚举、元数据及正文读取、保存、删除和清空的实际文件使用阶段验证真实路径归属。相对条目 ID 合法 MUST NOT 替代根目录、中间目录和文件的真实归属检查。系统 SHALL 使用锚定可信根的受约束访问或同等防替换机制，MUST NOT 检查后再次沿可替换路径无约束打开；跨根软链接、路径替换或不可验证的归属 SHALL 在读取元数据或正文前被拒绝。

该边界 SHALL 实现在原共享记忆服务中，同时覆盖既有 `/api/memory/personal*`、新兼容入口及其替代文件访问路径，不得只保护新恢复接口或另建 CRUD。拒绝及列表统计 MUST NOT 泄露另一用户的文件、路径或内容。已交付的功能记录不能替代该安全前置的实际证据。

#### Scenario: 合法条目标识对应跨用户软链接
- **WHEN** 本人根内合法名称的记忆条目或中间目录实际链接到其他用户或租户的文件
- **THEN** 列表、正文和写入均不能沿链接访问目标，不读取其元数据后再过滤，也不影响目标文件

#### Scenario: 路径检查后发生替换
- **WHEN** 记忆路径在校验与实际读取、写入或清空之间被替换
- **THEN** 操作只能作用于仍受原可信根约束的对象或被拒绝，不能访问替换后的未授权对象

#### Scenario: 新旧个人记忆入口共用边界
- **WHEN** 同一越界目标分别经已开放个人记忆接口和恢复后的兼容接口访问
- **THEN** 两者使用同一共享服务拒绝，不能以新入口已修复掩盖旧入口仍可访问

### Requirement: 记忆内容与索引发布共同遵守清空版本

正文提交、索引发布、删除/清空及后台固化 SHALL 在同一 tenant/user 的操作版本与清空 generation 协议下协调。每个发布者 SHALL 在最终提交时重新验证作用域、条目/操作版本和 generation，且校验与发布 MUST 不可分离。过期保存、索引同步或固化 MUST NOT 在清空成功后恢复旧正文或索引，过期删除/清理也 MUST NOT 删除较新版本。

文件及多个索引不能同事务提交时，系统 SHALL 在产生副作用前建立持久化恢复和屏蔽状态，未完成时返回 pending/incomplete 并阻止旧内容被检索；重启重放 SHALL 重验版本，不能把无法读取安全状态当成已完成。清空成功 SHALL 表示所有可发布旧内容的路径均已被版本协议约束。仅支持进程内协调的部署 SHALL 拒绝多个写入进程，不得宣称跨进程原子性。

修复 SHALL 复用原个人记忆服务和现有检索/固化接缝，不创建第二套 CRUD 或检索真值。验收 SHALL 包含受控并发交错，不能仅依赖顺序调用或兄弟 change 的完成标记。

#### Scenario: 保存正文后清空先于索引发布
- **WHEN** 保存已经写入正文但尚未发布索引，此时清空完成，随后原保存尝试发布索引
- **THEN** 原保存的过期发布被拒绝或安全丢弃，原条目与检索结果均不复活

#### Scenario: 索引同步或后台固化跨越清空
- **WHEN** 同步或固化基于清空前的内容取得快照，并在清空后尝试提交
- **THEN** 最终发布按同一版本边界拒绝旧内容，不因先前校验通过而重新写回

#### Scenario: 索引失败与重启恢复
- **WHEN** 记忆变更尚有索引未完成时服务中断并重启
- **THEN** 未完成状态可恢复且旧内容保持不可检索，重放只提交仍有效版本，不把失败当成功或恢复过期内容

#### Scenario: 较新版本不被旧清理覆盖
- **WHEN** 删除或清空后的合法新版本已发布，旧清理操作随后恢复执行
- **THEN** 旧操作不能删除新版本的正文或索引，其他用户及共享记忆不受影响

### Requirement: 记忆浏览兼容入口明确个人和智能体作用域

系统 SHALL 以现有记忆管理页面及正式记忆接口提供同一列表、分页、类别、内容和管理动作，普通用户与管理员共用字段、流程和反馈。目标集合 SHALL 在服务端过滤：普通用户仅有当前租户本人用户记忆及本人私有智能体记忆；管理员额外具有获准共享记忆。相同目标类别和状态的读写规则 SHALL 一致，系统只读类别不因角色变化转为可写。读载荷 SHALL 就每个目标报告可用动作，页面不得广告写入会被拒绝的动作。

列表和内容响应 SHALL 保留兼容字段并明确实际目标；缺少、歧义或错误目标不得回落共享根、另一用户或默认智能体。旧个人接口仅委托同一服务，不保留独立个人功能。

#### Scenario: 两种角色操作本人记忆
- **WHEN** 普通用户或管理员在正式记忆管理选择本人合法目标
- **THEN** 使用相同列表、内容、编辑、删除和清空流程，作用域按实际归属显示

#### Scenario: 普通用户请求共享目标
- **WHEN** 普通用户通过参数指定共享记忆，即使拥有该共享智能体聊天使用权限
- **THEN** 管理访问被拒绝，不返回共享条目、统计或正文

#### Scenario: 成员请求写入与共享范围共根的智能体记忆
- **WHEN** 成员对本人私有智能体（其记忆根为租户共享根）提交编辑、删除或清空
- **THEN** 请求被拒绝且共享根字节不变；该目标仍可读，但读载荷标记写入动作不可用

#### Scenario: 旧客户端目标明确
- **WHEN** 兼容客户端指定其仍有权管理的原目标
- **THEN** 统一服务返回兼容响应，目标不被静默替换

#### Scenario: 本人浏览个人记忆
- **WHEN** 有效成员进入现有记忆管理并选择本人用户记忆
- **THEN** 返回本人条目与正文，使用与管理员相同页面及响应

#### Scenario: 旧客户端指定智能体
- **WHEN** 旧客户端通过已支持的参数指定一个本人有权读取的智能体记忆目标
- **THEN** 响应保留兼容字段且正确标明作用域，不静默替换为默认智能体或共享根

### Requirement: 记忆页面与替代访问入口遵守相同所有权边界

记忆列表、正文、搜索、下载、预览、工作区别名和检索 SHALL 对实际资源执行同一 owner 与租户判定。非 owner 管理员 MUST NOT 通过平台文件根或共享目录别名访问个人正文。拒绝响应和列表统计 MUST NOT 泄露他人条目的存在、路径或摘要。

#### Scenario: 平台管理员通过文件别名访问
- **WHEN** 非 owner 平台管理员经文件预览或工作区路径寻址另一成员记忆
- **THEN** 不返回正文或可探测目录，拒绝结果与个人记忆入口一致

#### Scenario: 同账号切换租户
- **WHEN** 同一账号切换到另一租户后再次访问旧条目标识
- **THEN** 请求被拒绝或目标不可见，不跨租户恢复原个人内容

### Requirement: 记忆浏览与个人写入使用同一内容版本

正式记忆管理、兼容入口、检索和后台固化 SHALL 使用同一真实归属、内容版本和恢复协议，不得复制独立 CRUD 或检索真值。编辑、删除和清空 SHALL 在目标范围内重验权限、版本和 generation，并在目标记忆根目录同时被其他范围读取时要求对该根目录有资格的每个范围都成立（当前存储形态下即租户管理资格）；沿用实际文件访问防逃逸、索引屏蔽及失败重试机制；只能清理所选合法目标，不能清空其他用户或共享根。能力尚未开放的类别或动作 SHALL 对相同资源条件一致显示，不将只读恢复描述为完整管理完成。

#### Scenario: 统一页面编辑后旧接口读取
- **WHEN** 用户在正式页面成功编辑本人条目，兼容接口随后读取
- **THEN** 返回同一版本，其他获准消费者检索也反映该版本

#### Scenario: 并发清空与索引失败
- **WHEN** 清空跨越旧固化或索引发布，或索引清理失败后重启
- **THEN** 过期内容不复活，未完成状态可恢复且如实呈现，不误清其他目标；遵守既有路径与版本协议

#### Scenario: 个人页面编辑后旧入口读取
- **WHEN** 用户在正式记忆管理编辑本人条目后经兼容入口读取
- **THEN** 返回同一版本，兼容入口只委托统一服务，无独立个人实现

#### Scenario: 清空后原入口和旧索引访问
- **WHEN** 本人清空完成后，原浏览入口、搜索或清空前排队的固化任务访问旧内容
- **THEN** 不返回或恢复已删除正文；失败恢复遵循统一记忆事务，不影响共享记忆

