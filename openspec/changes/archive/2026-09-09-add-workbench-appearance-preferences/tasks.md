## 1. PRD-02 增量阶段 A：接入核对与依赖确认

- [x] 1.1 核对最新 Web 主题入口、账号菜单/界面偏好、品牌设置的实际实现，记录本期接入点与经典配色基准；保留工作区其他在途修改。
- [x] 1.2 对齐 `extend-sidebar-account-actions` 的浏览器偏好和单面板约定，明确顶部可独立交付、账号入口就绪则复用；不依赖资料/密码/租户功能，也不复制其语言改造。
- [x] 1.3 记录本期无新接口、数据库迁移或 feature flag 的边界；确认原认证与运行门禁保持既有职责。PRD v1.2 原文恢复时复核归属，缺失时记录限制，不宣称原文验收完成。

## 2. PRD-02 增量阶段 B：外观状态与背景配色

- [x] 2.1 检查阶段 A 接入证据后，实现三套预设与两套明暗颜色值，限定公共框架及首页消费；记录经典配色与现状的对应关系。
- [x] 2.2 统一首屏与运行时偏好解析，按 design 的启动表处理旧 light/dark、system、无偏好、非法值；安全读取 `cow_theme` 与 `cow_web_palette`，保留 `.dark` 契约。为语言、任务通知、各 Agent 选择等会阻断外观入口初始化的现有启动读取补充最小回退，不重构全部业务存储。
- [x] 2.3 实现外观统一应用入口，区分偏好与解析结果；跟随系统时监听变化、显式明暗不跟随，系统检测缺失时回退浅色，并同步代码高亮。
- [x] 2.4 接入侧栏、品牌文案、分组、选中/悬停/焦点、账号卡片和菜单的颜色变量，消除指定区域的固定黑底白字冲突。（已补选中竖线、字重与 aria-current。）
- [x] 2.5 接入顶栏、首页背景和现有卡片、输入框及焦点/拖入/chip/发送/禁用状态；遵循追加授权的单顶栏、三条任务建议与空态输入布局，保留既有菜单功能。
- [x] 2.6 对齐外观面板与启动遮罩的基本前景/背景；保持品牌图源、品牌局部预览及其他业务页原有明暗逻辑，避免全局覆盖工具类。

## 3. PRD-02 增量阶段 C：用户选择与本地恢复

- [x] 3.1 检查阶段 B 首屏、系统模式与高亮同步的浏览器证据后，接入顶部外观面板，提供三套带名称/选中状态的预览、三种明暗模式及恢复默认。
- [x] 3.2 实现即时应用和浏览器保存；关闭保留选择、恢复默认仅重置外观。用户首次操作保存当前完整选择，避免旧用户仅改明暗后丢失经典配色。
- [x] 3.3 增加读取失败、全部/部分写入失败的本页降级及准确提示；明确当前浏览器及账号共享范围，不写 `/config`、身份或品牌接口。
- [x] 3.4 接入同源多标签 storage 同步与清除回退，读取最终有效值、不反写循环；处理同时修改与存储不可用边界。
- [x] 3.5 账号界面偏好已实现时接入同一外观面板与状态；否则保持顶部独立入口并记录后续复用点，不增加资料/改密等实施任务。（已统一为同一 dialog 和 CowAppearance 控制器，语言仍本地保存。）
- [x] 3.6 完成简体/繁体/英文文案、键盘单选、可见焦点、Esc 关闭与焦点归还、窄屏及短视口布局。

## 4. PRD-02 增量阶段 D：行为与视觉验收

- [x] 4.1 增加有行为断言的前端测试，覆盖启动规则、旧偏好首次操作、系统变化、重置、非法值、存储拒绝/部分失败及跨标签最终值收敛；让真实页面的 localStorage 读取统一抛错，验证启动继续执行且外观入口可达，不只测独立解析器或使用已 stub 掉主题初始化的账号测试。
- [x] 4.2 在真实浏览器检查三套预设的浅/深六种外观；系统模式通过状态切换验证。在 1440×900、1280×720 和 375px 宽视口检查侧栏/账号菜单、首页、输入框及选择面板，记录正文对比度、选中/焦点、窄屏和短屏结果。
- [x] 4.3 验证配色切换不刷新页面、不清草稿或附件、不改变 Agent/业务会话、不打断流式展示；回归代码高亮、品牌默认/自定义与局部预览、历史和其他主要内页可读性。（历史页浅色辅助文字的既有对比度不足单独记录于 verification.md，未宣称全部内页达到 4.5:1。）
- [x] 4.4 验证已允许进入控制台的 database 普通成员、legacy、免登录模式均可本地选择，账号/租户切换保留设备外观；检查没有新增配置、品牌或身份写请求。（已在同一浏览器走通退出、登录另一账号及切租户的生产前端流程，后端为隔离 fixture；不作为后端授权验收。）
- [x] 4.5 运行受影响的现有账号/品牌/首页前端回归；三语用代表性布局覆盖，确认 Desktop 主题协议及 `cow_theme_id` 未被修改，不把运行验收标记为文档校验通过。（最新相关前端回归 117/117，双入口及首次保存已完成浏览器验证。）

## 5. PRD-02 增量交付：迁移、回退与说明

- [x] 5.1 检查阶段 D 的真实证据后记录启用结论：本期无新增 feature flag，外观入口可用性不等待无关账号任务；既有认证与企业运行门禁无变化。
- [x] 5.2 演练旧 light/dark 的经典配色恢复、新浏览器默认和回滚前 system 到实际 light/dark 的转换；确认新配色键及其他偏好、品牌与业务数据保留。
- [x] 5.3 补充用户说明：选择入口、三套风格、明暗模式、即时保存、当前浏览器共享范围、恢复默认和存储失败提示。
- [x] 5.4 核对最终改动覆盖外观选择和用户追加授权的首页布局；协调相关账号 change 的重叠偏好条款，保留业务菜单及品牌归属，重新运行 OpenSpec 校验。

## 6. 用户追加授权：实现参考图中的首页风格与布局

- [x] 6.1 更新 proposal/design/spec 的实施范围，明确用户已授权此前建议项；无新增后端接口、数据库或开关。
- [x] 6.2 合并页面/Agent 为单顶栏，支持桌面侧栏折叠、窄屏抽屉、当前项非颜色标记与键盘导航。
- [x] 6.3 增加侧栏新建对话及资源管理收纳，保留全部路由与未保存编辑保护，进入页面时展开相关分组。
- [x] 6.4 实现左对齐欢迎、介绍下方真实输入框、三条仅填草稿的任务建议；新建/发送/恢复消息时切换布局，复用唯一composer。
- [x] 6.5 实现三语、新旧品牌、三配色明暗、窄屏/短屏及折叠布局适配，保留附件、模型、Agent和权限工具。
- [x] 6.6 完成统一账号/顶部入口、新建/消息/附件/流式连续、真实用户与租户切换的隔离浏览器验收，更新不再适用的双顶栏/六卡断言。
- [x] 6.7 人工查看参考图对应浅色桌面效果与深色/手机截图，记录用户说明、实际测试证据及剩余限制。

## 7. 用户反馈回归：恢复输入区多智能体选择

- [x] 7.1 兼容租户可见智能体 projection 的 `can_chat` / `is_default` 和旧版 `enabled` / `default_agent_id`，恢复输入区头像入口及可聊天智能体列表；保留已有群聊成员操作与租户响应时效检查。
- [x] 7.2 使用真实租户接口字段在隔离浏览器中验证当前会话连续添加、移除智能体、草稿与会话保持，以及窄屏菜单可用性；记录相关回归与修复证据。（122/122 前端回归、有界浏览器场景 1/1；桌面及 375px 首页/消息态菜单、实际邀请和移除通过。）
