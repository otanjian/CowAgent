# 实施与验证记录

## 回归修复：输入区多智能体入口（2026-09-08）

用户反馈模型选择右侧的智能体入口消失。排查确认租户模式 `/api/agents` 已返回经过启用与租户可见性筛选的安全 projection，使用 `can_chat` / `is_default`，不再包含管理快照的 `enabled` / 顶层 `default_agent_id`。旧前端按 `enabled` 筛选，误判可选智能体为零并隐藏入口；桌面 CSS 没有裁剪头像。

- 在前端目录接入处兼容两种字段契约，使用租户 `is_default`，保留绑定会话的 owner 和已有的身份/租户时效检查；不回退到全局管理快照，也不修改服务端可见性与授权。
- `availableChatAgents()` 单独筛选可聊天的切换/邀请候选，管理用途的 `enabledAgents()` 保留配置启用语义；`can_chat=false` 不作为聊天候选。已有群聊成员的入口和移除操作不因可用目录缩减而消失。
- 手机宽度下，菜单改为相对整个输入卡片定位并限制宽度，避免 300px 菜单以工具条中间的 22px 头像为右锚点而超出左边界；桌面保持原头像位置。
- 新增 5 项生产函数回归，覆盖租户 projection、旧格式、不可聊天/异常能力值、已有成员保留及旧租户迟到响应；与外观、账号、品牌、历史、智能体工作台一起运行，**122/122** 通过。日志：`/tmp/cow-composer-agents-tests.log`。
- 浏览器 fixture 已改用真实租户字段，有界场景 **1/1** 通过：桌面当前会话连续添加两位、移除一位，菜单持续打开，owner、草稿和会话保持；375px 首页操作菜单上部选项、滚动至底部邀请并移除，消息态再次实际邀请均通过。共 6 次隔离会话设置写入，0 个页面脚本错误和未配置路由；两种手机状态菜单左边界分别为 48px、50px，宽度均为 300px，无水平越界。
- 人工查看桌面消息态及两张手机截图，确认原工具条头像入口、人数标记、成员与候选列表正常显示。报告及截图：`/tmp/cow-appearance.zxUNym/browser-multi-agent/results.json` 及同目录 PNG。仅使用隔离后端样例，没有修改真实会话或调用外部模型；本次未重复 18 组合视觉矩阵。
- 本地运行服务的 JavaScript 与 CSS 均返回 200 且已包含本次修复，刷新即可加载。JavaScript 语法、差异格式和 OpenSpec 严格校验通过。任务 7.1/7.2 完成，本 change 当前 **33/33** 项完成。

之前的浏览器数据样例含有 `enabled`，没有覆盖此次接口兼容问题，下方布局完成记录不能代替本次回归证据。

## 最终实施：用户追加参考图布局（2026-09-08）

用户已明确要求实现此前列为建议的首页布局，本 change 范围已同步修订，31/31 项任务完成。当前实现包括：

- 单顶栏整合页面与 Agent；桌面 240px 侧栏可收为 72px 图标列，窄屏保留抽屉；侧栏选中项有竖线、字重与 aria-current，资源管理收纳低频入口，全部原路由保留。
- 左对齐欢迎语、介绍下方输入框、三条纵向任务建议。唯一 composer 保持原 DOM，CSS 仅调整空态视觉排序；消息态回到底部输入，新建使用同一欢迎模板按当前品牌/语言重绘。任务建议只填草稿。
- 新建对话入口先执行工作空间和已有页面未保存保护；取消不换页面、不创建会话、不抢输入焦点。草稿、附件和其他会话后台流保持既有行为。
- 账号和顶部复用同一外观 dialog 及 CowAppearance，三配色/三明暗/恢复默认完整可用；三语选择进入同一面板，语言和外观各自报告本地存储失败。旧账号主题直接写键路径已移除。
- 长品牌名称在侧栏省略并提供完整名称提示，首页可换行；Logo 继续来自已发布品牌。创建智能体图标的交互色遗漏已补齐。桌面显示完整语言名，窄屏使用简短标签。

### 本轮验证

- **117/117** 项相关前端回归通过，包括新增账号统一外观测试及当前工作区账号回归；JavaScript 语法、git diff --check、OpenSpec 严格校验通过。页面 MIME 单测 **1/1** 通过。
- **12/12** 个隔离 Chrome 浏览器场景、三配色 × 明暗 × 三视口共 **18** 组合通过。指定正常文字 **384** 项对比度均不低于 4.5:1，最低 **4.615:1**；0 个页面脚本错误、未配置请求、几何溢出或对比度失败，输出 **53** 张 PNG。
- 新增真实生产前端流程：账号/顶部共用面板，键盘配色与模式、X/Esc/背景关闭及焦点归还；同一浏览器退出、登录另一账号、选择另一租户后偏好保留。原生 dialog 的 Tab 循环经过浏览器边界时 activeElement 可为 BODY，继续 Tab 回到 dialog，未进入后台业务控件。
- 真实上传至隔离 fixture 后发送，校验发送载荷包含附件；新建会话保留同一输入框节点、新草稿和附件，旧流继续在后台处理且不串入新会话。已验证工作空间未保存编辑取消、窄屏 slash/工作空间菜单与折叠状态账号菜单可用。
- 从受控 /api/branding/public 读取已发布的长名称与不同 Logo，随后新建会话及 375px 视口均保留正确品牌，无横向溢出；品牌局部预览与个人外观继续独立。
- 人工查看商务浅色桌面、深蓝深色桌面、手机首页、手机外观面板和长品牌手机截图，效果符合参考图的本期适配范围；1280×720/375px 首页通过整体滚动保持底部建议可达。
- 运行中的本地服务 /chat 返回 200，已包含新建按钮、单顶栏、新欢迎区、三条任务建议，并已移除旧账号偏好面板。

最终浏览器报告：/tmp/cow-appearance.zxUNym/browser-redesign/results.json；截图同目录。前端测试日志：/tmp/cow-home-final-tests.log。临时产物用于本次验收，后续可用仓库测试重新生成。

### 仍需准确理解的边界

浏览器执行完整生产 HTML/JS/CSS，但后端身份、消息、上传与品牌响应为隔离 fixture，不涉及真实用户资料或外部模型执行，也不作为后端鉴权/租户隔离验收。对比度为本期指定区域的文字抽样；历史页已有浅色辅助文字不足继续保留为内页后续优化记录。未新增后端 API、数据库、功能开关或 Desktop 主题协议，未归档其他在途 change。

## 前次复查记录（追加截图实施之前）

前次复查发现不足，曾将任务由 24/24 更正为 20/24。以下内容保留为当时发现与证据，不代表最终状态；这些接入与验收缺口已在上方最终实施记录中逐项补齐。

- **账号入口未复用统一外观面板。** 当前 `openAccountPrefs()` 打开 `account-prefs-modal`，其中只有浅/深两项主题选择；顶部仍打开包含三配色、系统模式与恢复默认的 `appearance-dialog`。两个独立面板不满足已接入账号入口共用同一外观状态和面板的要求。
- **账号主题操作未更新控制器。** 当前 `setAccountTheme()` 直接赋值 `currentTheme` 并只写 `cow_theme`，随后 `applyTheme()` 调用 `CowAppearance.apply()` 重绘旧内存状态。使用生产控制器和实际函数复现：新浏览器在系统浅色时点账号“深色”，当前仍为商务青蓝浅色；存储只新增 `cow_theme=dark`，刷新后被旧偏好迁移规则恢复为经典深色。应统一经 `CowAppearance.setMode()` 保存完整选择，并复用相同失败提示。
- **侧栏选中提示。** `appearance.css` 的 `.sidebar-item.active` 及原有样式仅改变前景/背景颜色，没有额外形状、标记或字重区别。虽然沿用了旧行为，但未完全满足本规范“颜色不作为唯一选中提示”的要求，任务 2.4 重新打开。外观面板自身有勾选和原生单选，不存在这一问题。
- **测试结果与覆盖边界。** 当前 98 项前端回归中 97 项通过，`tests/test_sidebar_account_frontend.cjs` 的账号启动检查失败后重试用例在第 216 行期望初始化 1 次、实际 0 次。已定位为新增租户准备 Promise 链的等待时序：`refreshAccountIdentity()` 完成不代表 `initApp()` 已调用；在临时测试副本中使用已有 `settle()` 等待后该项通过，生产和仓库测试未修改，不能把此断言失败说成真实初始化故障。现有 9 个浏览器场景与 18 组视觉组合全部通过，但这些场景只通过顶部选择外观，未覆盖新增账号偏好入口，因此无法发现上述接入缺陷。复查报告：`/tmp/cow-appearance-reaudit/results.json`。
- **身份切换验收缺口。** 三种身份使用独立浏览器上下文和已允许进入的固定数据样例，未在同一浏览器实际执行退出、换账号、切租户。代码核对未发现这些路径删除外观键，但不能代替流程验收，任务 4.4 重新打开。
- **其余抽样边界。** 自定义品牌验证覆盖草稿与局部预览独立性，未覆盖发布后自定义 Logo/长名称的全部组合；真实浏览器多标签测试为顺序切换，近同时写入为控制器单测；键盘主要覆盖顶部 Esc 和配色选择；384 项文字对比度为指定选择器样本，不是所有交互状态。应按这些实际范围解读早先验证记录。
- **低优先级样式建议。** 输入区 Agent 菜单“创建智能体”的图标未消费外观变量，hover 仍继承 `console.css` 中固定绿色；深色配色图标与背景对比偏低，旁边文字正常。可在补齐交互状态时一并调整，不将带文字说明的装饰图标直接判为正文对比度失败。
- OpenSpec 严格校验、差异格式检查、页面 MIME 回归通过；规范文件合法不等于功能已完成。后续完成账号入口统一后，须补充双入口、系统模式、首次保存及存储失败回归，再恢复任务完成状态。

## 接入范围

- 新增 `channel/web/static/js/appearance.js`：首屏与运行时共用的本地外观控制器；模式/配色独立存储，系统模式、多标签同步、迁移及读写降级。
- 新增 `channel/web/static/css/appearance.css`：三套配色及六种明暗组合，限定公共框架、首页、输入框、账号菜单和外观面板；保留现有页面结构。
- `chat.html` 加载上述资源、复用顶部主题位置打开原生 dialog，提供可键盘操作的选择和默认恢复；新资源纳入 `ChatHandler` 既有缓存更新列表。
- `console.js` 保留 `applyTheme` / `toggleTheme` 的调用兼容，统一读控制器状态并同步代码高亮；新增三语文案及必要启动读取容错。读取受限时仍可显示偏好面板，未改服务器认证、业务存储或语言写入协议。
- 实施核对时账号“界面偏好”扩展尚未接入：工作区已有在途 HTML 占位，但当前没有对应 `openAccountPrefs` 等运行时函数。顶部入口独立使用，后续账号入口应调用同一 `openAppearancePreferences`，不建设第二套外观状态或面板。本轮未增加其他账号动作。
- 当前浏览器共享偏好已在面板和 Web README 告知；不新增接口、数据库字段或 feature flag，不产生品牌/实例设置写入。PRD v1.2 原文仍未提供，未声称逐项核对原文。

## 阶段性检查

- 新控制器行为测试：29 项通过，包括仅禁止读取和静默丢弃写入时的准确失败提示。
- 账号、品牌、历史、智能体与外观的前端回归：合计 98 项通过。
- `node --check` 校验 `appearance.js`、`console.js`；Python AST 校验 `web_channel.py`；`tests.test_web_chat_content_type` 1 项通过。
- CSS 在 Chrome 中解析：493 条声明合法，变量引用有定义；六组合的 192 组语义前景/背景对比全部不低于 4.5:1。实际页面状态由浏览器集成检查进一步覆盖。
- 运行中的本地服务 `127.0.0.1:9899`：`/chat`、新增 JavaScript、新增 CSS 返回 200，MIME 类型正确，HTML 已含外观面板。此检查仅验证页面资源交付，未读取或修改用户业务数据。

## 既有测试修正

账号回归中的 `_ensureTenantSelected` 测试替身原来返回 undefined，但当前生产函数的契约已是 Promise。用本轮修改前的 `console.js` 快照复现相同 4 项失败，确认与外观改动无关；将该测试替身改为 `Promise.resolve()`，恢复其与生产接口一致，未修改账号实现以迁就测试。

最后一次 Python 检查发现并行工作区修改中的 `_require_private_owner` 字符串类型注解缺少正确的闭合引号位置；只修正该语法字符，使模块可导入，未改变其授权逻辑。修正后页面响应类型回归通过。

## 浏览器验收结果

- Chrome 使用完整生产页面与静态资源，8 个场景通过：新用户/旧偏好恢复、即时选择、键盘/Esc与焦点、系统与高亮、跨标签及清除、读写降级、三种身份状态下的本地行为、登录过期收起面板及视觉矩阵。
- 使用 1440×900、1280×720、375×812 三种视口，覆盖三配色 × 浅/深模式，共 18 组；另覆盖简体、繁体、英文窄屏和六组账号菜单，共输出 47 张截图。
- 实际渲染的 384 项文字/合成背景对比度全部达到 4.5:1，最低 4.615:1；0 个脚本错误、未配置请求、几何溢出或对比度失败。侧栏品牌 AI 使用独立蓝色，不被经典配色的绿色交互色覆盖。
- 首屏观察确认解析期外观与运行时一致；六张卡片与两层头部保留。人工查看商务浅色首页、深蓝侧栏面板、经典配色账号菜单、深色短屏及中英文手机面板，布局与可读性符合本期范围。
- 使用真实业务请求的受控 401 响应验证：外观面板自动收起，登录输入框可用且焦点不会回到隐藏的工作台。键盘打开外观面板时已有附件菜单收起。
- 回退演练：先将 system 转为实际 light/dark，并选择经典配色；分别运行本轮修改前的真实主题代码，两种模式均正确恢复，语言、会话标识、Desktop 主题标识和新配色键保留。
- 补充场景使用生产 `startSSE` 与浏览器 EventSource 接收隔离服务的分段消息：打开面板并切换配色/明暗后，连接与消息 DOM 节点保持，第二段追加到原消息；附件节点、附件列表、输入草稿、Agent 与会话标识保持。
- 同一补充场景运行真实品牌局部预览事件：自定义品牌草稿和当前品牌状态保持，局部预览切成浅色时页面仍保持个人选择的深色。配置内页浅/深各抽样标题、说明、卡片标题，6 项实际文字对比度通过，最低 4.548:1。
- 历史页通过真实导航加载受控空态，浅/深各抽样标题、说明与空态共 6 项，并与禁用新增样式时的同模式基准比较。深色三项最低 6.477:1；浅色标题 12.364:1，辅助文字的既有不足及背景微小变化见下方验收边界。补充场景通过，无页面错误、未配置请求或服务端写入。

本次完整浏览器报告保存在测试输出目录的 `results.json`；默认输出为独立临时目录，可通过 `COW_APPEARANCE_BROWSER_OUTPUT` 指定位置。复验会重新生成截图与报告。

本轮主报告：`/tmp/cow-appearance.zxUNym/browser/results.json`；补充报告单独保存在 `/tmp/cow-appearance.zxUNym/browser-stream/results.json`，未覆盖主报告。截图与机器报告为临时验收产物，核心结论记录于本文。

## 验收边界

浏览器测试加载完整生产 HTML、脚本和 CSS，以隔离服务提供后端数据样例，避免操作本地用户数据。身份样例用于验证普通成员/legacy/免登录状态下的前端入口和本地行为，不作为后端身份授权、租户隔离或企业运行门禁的验收证据；本 change 不新增这些服务器消费者。

历史页浅色辅助文字存在原有对比度不足：说明文字约 3.51:1、空态约 3.67:1，未达到普通文字 4.5:1。通过同页关闭新增样式、保持相同明暗的方式复核，文字色仍为原有 `--history-muted: #7b8792`；本期公共背景微调使说明从 3.511:1 变为 3.507:1，空态比例不变。该问题记录为内页后续可读性优化建议，不宣称历史页全部符合 4.5:1，也不将全站样式重构纳入本次范围。

顶栏合并、导航重组、新建会话入口、首页输入框移位、卡片精简均保持为 design 第 6 节的后续建议。本轮不归档或覆盖其他在途 change。

归档协调：本 change 的配色、三种明暗模式、浏览器共享范围和两项存储键作为外观能力的唯一规范；后续 `extend-sidebar-account-actions` 归档时应将其界面偏好中的明暗要求引用至本能力，保留其语言职责，避免另存主题或重复绑定处理器。当前不提前修改该 change。

## 复验命令

```sh
node --test tests/test_composer_agents_frontend.cjs tests/test_appearance_frontend.cjs tests/test_sidebar_account_frontend.cjs tests/branding_frontend.test.cjs tests/test_session_history_frontend.cjs tests/test_agent_workbench_frontend.cjs
node --test tests/test_appearance_browser.cjs
python -m unittest tests.test_web_chat_content_type
openspec validate add-workbench-appearance-preferences --strict
```

浏览器集成测试需要开发环境提供 Playwright 与 Chrome，不是新增应用运行时依赖。

可设置 `COW_APPEARANCE_BROWSER_FILTER='stream, attachment'` 重跑流式补充场景，或设为 `database safe agent projection` 重跑多智能体入口场景。当前不设置过滤器会执行全部 13 个场景；各次执行范围以上方相应记录为准。
