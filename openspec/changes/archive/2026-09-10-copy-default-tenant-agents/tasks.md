## 1. 数据模型与身份域（先写失败测试）

- [x] 1.1 新增 `auth/store.py` 迁移 `_migration_8`：`agent_bindings` 加列 `cloned_from_agent_id TEXT`，并建部分唯一索引 `(tenant_id, cloned_from_agent_id)`（仅非 NULL 生效）；追加到 `_migrations` 列表。
- [x] 1.2 先写迁移测试：旧库（含既有绑定）升级后新列存在、既有记录该列为 NULL、索引生效；重复打开库迁移幂等。
- [x] 1.3 `IdentityService.bind_agent()` 增加可选 `cloned_from_agent_id`（默认 None）与可选 actor 参数，保持既有调用与返回兼容；映射写入 + 不变式（同租户同来源唯一）失败时返回 409 语义。
- [x] 1.4 新增按来源查绑定的方法（如 `clone_of(tenant_id, source_agent_id)`），供幂等判定与列表投影使用。
- [x] 1.5 新增"把某克隆设为目标租户默认智能体"的服务方法（仅当目标无既有默认时由编排层调用），含审计。
- [x] 1.6 先写测试：`bind_agent` 记录来源映射；重复同源绑定不产生重复；跨租户改绑仍被拒（回归既有 `tenant-resource-isolation` 需求）。

## 2. 克隆原语抽取与复用（agent/admin.py）

- [x] 2.1 先写测试：抽取公共克隆入口后，`create_agent(clone_from=...)` 的既有行为（人设文件集合、名称种子、USER.md 补齐、失败清理）保持不变。
- [x] 2.2 抽取/暴露可复用原语（`_bootstrap_workspace`、`_clone_persona`、`_seed_name`、知识模式复刻），供编排层调用；重构以行为不变为准，不改外部契约。
- [x] 2.3 新增"按目标工作区构造配置属性完整的 AgentProfile"的构造路径（描述、头像、model/bot_type、skills/knowledge 选择、数字员工字段、工具白/黑名单 1:1）。
- [x] 2.4 先写测试：克隆工作区只含 `CLONED_FILES`，不含记忆文件、会话库、凭据、自有技能/知识实体目录；知识模式按来源形态复刻。

## 3. 编排模块 `agent/tenant_provisioning.py`

- [x] 3.1 先写测试：来源解析锚定全局默认智能体的绑定；锚点缺失回退到绑定数最多的租户；都无法确定时报可操作错误且不写任何数据。
- [x] 3.2 实现来源解析与"可复制候选"枚举（含停用智能体；来源无绑定返回空候选与说明），并按 `(target_tenant, source_id)` 反查标注 `already_copied` 与 `clone_agent_id`。
- [x] 3.3 先写测试：选中集合校验——空集合被拒且不写入；含不属于候选的标识时**整体**拒绝且不产生任何部分写入；未选中的候选不被复制。
- [x] 3.4 实现"先整体校验选中集合、再开始逐项写入"的顺序（不得逐项校验后跳过无效项）。
- [x] 3.5 先写测试：新 id 生成规则（清洗、截断 ≤64、冲突追加序号）与工作区落在 `<target.shared_root>/agents/<id>`。
- [x] 3.6 实现 id/工作区生成，复用第 2 组的克隆原语与 `_reject_overlapping_workspace` 的既有守卫。
- [x] 3.7 先写测试：按来源映射幂等跳过（含混选已复制与未复制）；跳过不计为失败；已改名/改 id 的克隆仍被识别。
- [x] 3.8 实现逐智能体处理与响应汇总 `{selected, copied, skipped, failed:[{source_agent_id, error}], default_agent_id}`。
- [x] 3.9 先写测试：单个智能体失败时补偿（清理新建工作区、回滚 roster 条目）、其它已成功项保留、整体不误报成功；重跑可续跑。
- [x] 3.10 实现补偿与失败明细；落实"在 roster 但未绑定"的孤儿收养/清理恢复策略并加测试。
- [x] 3.11 先写测试：目标复制前无智能体时——来源默认智能体的克隆在选中集合内则被设为默认；不在选中集合内则取本次第一个成功新增的克隆；全部跳过时不改默认；目标非空时默认不被改动。
- [x] 3.12 实现目标默认承接（调用 1.5）并在响应回带 `default_agent_id`。
- [x] 3.13 先写测试：拒绝 `target == source`；目标租户不存在时拒绝。
- [x] 3.14 实现来源=目标与不存在租户的守卫。

## 4. HTTP 边界与运行时

- [x] 4.1 `auth/http_policy.py` 登记 `/api/platform/tenants/([^/]+)/agents` 的 `GET`/`POST` 为 `platform`；补 `tests/test_http_policy.py` 的路由完整性断言。
- [x] 4.2 `channel/web/admin_handlers.py` 新增 handler：`GET` 返回 `{agents, copy_source}`（目标租户绑定智能体 + 来源候选与已复制标记；来源不可解析时 `copy_source` 为 null 并附原因；不含 workspace 与宿主路径）；`POST` 接受 `{action, source_agent_ids, recent_password}`，校验顺序为平台管理员 → 近期密码 → 来源可解析 → 集合非空 → 标识均在候选内。
- [x] 4.3 `channel/web/web_channel.py` 注册路由，并在成功后调用 `_reload_agent_runtime`，使新智能体无需重启可用。
- [x] 4.4 先写测试：非平台管理员 POST 返回 403 且无写入；缺有效近期密码被拒且无克隆/绑定/成功审计；空集合与越界标识返回 400 且无任何写入；GET 不泄露 workspace/宿主路径。
- [x] 4.5 先写测试（审计）：成功复制写一条脱敏汇总审计（来源/目标/选中/新增/跳过/失败计数与涉及 id），不含密码、哈希、令牌或凭据明文；拒绝请求按既有口径写拒绝审计。
- [x] 4.6 legacy 身份模式下端点不启用（走 `_guard_database`），且不改写既有智能体。

## 5. 前端：租户编辑器第五个标签

- [x] 5.1 `identity-admin.js` 编辑器标签集合与面板扩为 `basic/model/tool/agent/admin`（「智能体」插在「工具授权」与「租户管理」之间，既有四标签相对顺序不变），新增标签与面板骨架。
- [x] 5.2 实现目标租户已绑定智能体的只读列表：名称、id、启用状态、默认标记；空列表与读取失败区分渲染；MUST NOT 渲染宿主路径。
- [x] 5.3 实现默认租户候选的勾选列表：复选框 + 已复制标记（已同步的候选不可重复勾选），并展示当前选中数量。
- [x] 5.4 未勾选即提交时前端不发请求并给出可操作提示；勾选后作为编辑器批量提交的一个新步骤（放在 grants 之后、basics 之前；不参与租户 version 串接），复用统一密码弹窗。
- [x] 5.5 复制结果回显新增/跳过/失败明细与承接的默认智能体；失败时不显示整体成功并保留可重试错误。
- [x] 5.6 新建租户时「智能体」标签呈说明态且不发出写入；`console.css` 样式与三语 i18n 对齐既有标签；补上缺失的 `admin_back_to_list` 三语条目（当前渲染裸 key）。
- [x] 5.7 先写/更新 `tests/test_tenant_tabbed_editor_frontend.cjs`：标签集合含 `agent` 且顺序为 `basic/model/tool/agent/admin`、目标列表渲染、候选勾选与已同步标记、未勾选不发请求、复制调用与结果、失败不误报、未创建禁用、批量顺序与草稿保留。

## 6. 端到端与隔离验收

- [x] 6.1 端到端测试：平台管理员勾选若干来源智能体复制后，目标租户获准成员可列出并使用克隆智能体；源租户可见集合与状态不变。
- [x] 6.2 隔离验收：克隆工作区落在目标租户共享根内，不含来源租户的记忆/会话/凭据；读取不到其它租户智能体；越权与跨租户访问被拒。
- [x] 6.3 验收"两侧独立演化"：复制后分别修改两侧的人设/启用状态，互不影响。
- [x] 6.4 验收"未选中的不复制"：只勾选部分候选时，其余来源智能体在目标租户不存在。
- [x] 6.5 迁移与升级验收：对既有 `identity.db` 副本跑升级 + 复制 + 重跑，数据不丢、幂等、无孤儿绑定。

## 7. 文档与阶段门槛

- [x] 7.1 记录 `design.md` 的两项 Open Questions 的落地取值（租户根 `agents/` 预创建判据、克隆展示名称），更新对应实现与测试。
- [x] 7.2 产出 `evidence.md`：逐条对应 `specs/**/spec.md` 的场景，标注测试名与实测输出；未实测的能力 MUST NOT 标记为已实现。
- [x] 7.3 PRD 原文恢复后复核两处口径（复制是否为快照、目标默认唯一性）。本次复核结论：**口径与本 change 实现一致，无需另开 change**——复制是快照（只带人设与配置，之后两侧独立演化，见 `IndependentEvolutionTests`）；目标默认唯一性由 `tenants.default_agent_id` 单列结构与仅"目标原本无智能体时才写入"的守卫共同保证（见 `DefaultAgentTakeoverTests`）。PRD 原文在本仓库不可得，如后续恢复且口径有出入，另开 change 处理。
- [x] 7.4 确认发布门槛：仅在 database 身份模式启用；不引入运行开关；legacy 行为与既有智能体配置不受影响。
