## 0. 模型决策（后续接缝实现的前置输入，必须先完成）

- [x] 0.1 决定会话/消息行的最终主键与唯一约束（D4）：**采纳上游复合键**——`sessions` 用 `PRIMARY KEY (agent_id, session_id)`、`messages` 用 `UNIQUE (agent_id, session_id, seq)` 与索引 `(agent_id, session_id, seq)`/`(agent_id, last_active)`；fork 的 `owner` 与新增 `tenant_id` 均为非键过滤列。连带影响（`_require_owned_session` 须补 `agent_id` 限定、`(agent_id, session_id)` 重复性检测、`DEFAULT ''` 回填）已记入 `design.md` D4
- [x] 0.2 决定调度服务拓扑与执行回调契约（D4c）：**采纳上游全局单一服务**（`get_task_store`/`get_scheduler_service`/`get_recipient_store` 均为全局，回调 `(task, trigger)`，内部 `identity_scope(agent_id=_resolve_task_agent_id(...))`），fork 的按工作区服务退役、成员身份由身份接缝接入；依据为上游已自带 `_migrate_legacy_task_stores` 迁移路径。已记入 `design.md` D4c
- [x] 0.3 决定 fork 隔离与上游会话全局化的共存方式（D10）：**跟随上游全局化**（单一全局会话文件 + `agent_id` 维度），fork 的租户/用户隔离由 `tenant_id`+`owner` 过滤列承担；不保留按 Agent 物理文件分离。已记入 `design.md` D10，附复核条件
- [x] 0.4 确认 `PermissionSelector.tsx` 在 fork 侧是否有功能替代物：**保留删除**。全仓库无 `desktop/src` 引用，且 `tests/test_execution_permission_ui.cjs` 以 `assert.equal(fs.existsSync(...), false)` 断言其不存在；功能由 RBAC 执行权限取代（`rbac-controlled-execution-permission`）
- [x] 0.5 把 0.1–0.4 的结论写入 `design.md` 并标注为可复审决策（D4/D4c/D10/D9 均已就地更新，含理由、取舍与复核条件）

## 1. 前置检查与顺序约束

- [x] 1.1 确认在途 change `scan-onboarding-and-inbound-anchor` 的归档顺序：已于 2026-09-11 归档为 `archive/2026-09-11-scan-onboarding-and-inbound-anchor`（归档前修复了其 `external-identity-binding` delta 中错位的 MODIFIED 块——该 requirement 归属 `tenant-resource-isolation`，其专属补充已并入正确 capability），`auth/http_policy.py` 的 `ROUTE_POLICY` 与租户渠道写入面现可安全编辑。门槛已满足
- [x] 1.2 核对本 change 各项 requirement 与 `openspec/specs/` 既有规范的归属：新增行为落在对应 capability 的 spec，修改既有 capability 的以 delta 声明；需要新增需求而既有 spec 无对应 capability 时，在本 change 内新建该 capability 而非等待外部文档。已验证 3 处 MODIFIED 基线标题（`enterprise-access-enforcement`/`resource-quota`/`tenant-channel-configuration`）与主规范逐字匹配，可作为归档前置
- [x] 1.3 冻结本 change 开始时的路由基线：导出 `_WEB_URLS`（115 条）与 `ROUTE_POLICY`（110 条路由 / 140 个方法条目，含方法、策略与 permission）快照，并显式列出 5 条缺口为「待补策略」。产物：`scripts/route-baseline.txt`
- [x] 1.4 确认当前工作区未提交改动（`channel/web/chat.html`、`console.css`、`console.js`）已提交或 stash，避免与前端 i18n/视图注册迁移冲突。实测 `git status --porcelain` 无这三个文件的未提交改动，可安全进入第 8 组
- [x] 1.5 实施前重新冻结冲突基线：以 `origin/master@9ad944dd` × `origin/rdai@dc760766`（merge-base `e5e2a52d`）执行合并预览，实测 **20 个冲突文件、41 个内容冲突块**，与规划快照一致、无漂移
- [x] 1.6 把 1.5 的基线导出为可供 9.3/9.4 同步脚本对比的持久化清单 `scripts/conflict-baseline.txt`（20 行数据，含每个冲突文件的处置方式、5 个删除/修改类文件的决策与 6 个文档 `.mdx` 的一般合并策略）
- [x] 1.7 复核 1.6 清单覆盖全部 20 个实测冲突文件；未覆盖者（6 个 `docs/{,zh,ja}/intro/*.mdx` 文档冲突，属一般合并、无代码影响）已在 proposal/design 的 Non-Goals 中注明理由

## 2. 单一路由事实源与三腿覆盖不变量（可合并性机制 + 安全，P0）

- [x] 2.1 新增权威路由清单模块（`channel/web/route_registry.py`），以结构化条目**逐条**登记 `pattern`、`methods → {policy, permission}`、`source`、可选 `handler`；SOURCE 区分 `upstream` 与 `fork:<name>`；禁止以自动转录 + 默认策略批量初始化。已落地：115 条 `RouteEntry`（65 `upstream` / 50 `fork:<area>`），策略逐条转录自已验证的 `ROUTE_POLICY`（非默认值），`_validate_entry` 对未知 policy/方法/缺 handler/缺 source 直接抛错
- [x] 2.2 由权威清单派生 `_WEB_URLS` 与 `ROUTE_POLICY`（保持二者现有数据结构形态与调用点签名不变，使 `build_web_app` 与测试无感）。`derive_web_urls()` 返回扁平 `(pattern, handler)` 元组、`derive_route_policy()` 返回 `{pattern: {METHOD: entry}}`，与迁移前形态一致
- [x] 2.3 RED：等价断言测试——派生结果与 1.3 的路由基线逐条等价（含方法与策略、permission）；5 条缺口 MUST 以「待补策略」显式列出而非静默等同。`tests/test_route_registry.py::FrozenBaselineEquivalenceTests`：解析 `scripts/route-baseline.txt`（追加式，末尾 RESOLVED 段覆盖历史行），逐条比对 policy 与 permission；`checked > 130`。`REMOVED` 行断言该登记已消失
- [x] 2.4 迁移 `channel/web/web_channel.py` 的 `_WEB_URLS` 为派生；确认 `build_web_app` 行为零变化。实测 URL 表 115 条与迁移前一致；`test_web_navigation_mode.py`（依赖 `_WEB_URLS` 与 `/admin`→`ChatHandler`）通过
- [x] 2.5 迁移 `auth/http_policy.py` 的 `ROUTE_POLICY` 为派生；确认 `enforce_http_policy` 现有语义不变（仅透传 + 405/503 判定）。`_match_policy`/`enforce_http_policy` 未改动，`test_http_policy.py` 40 项通过（其中 1 项示例按新登记修正，见 4.13）
- [x] 2.6 提供 fork 路由扩展注册入口（`register_fork_routes()` 形态），使 fork 新增路由不要求编辑核心字面量。`_FORK_ROUTES` + `register_fork_routes()` + `all_routes()`；`_load_fork_extensions()` 提供 `channel/web/fork_routes.py` 导入钩子（当前仓库无该模块，属正常态）
- [x] 2.7 RED：三腿覆盖不变量测试——(1) 清单每个方法均有策略条目；(2) 策略表每条路由均在清单或显式外部来源；(3) **清单登记方法集合与 handler 内省实现集合一致**；任一不一致即失败（第 3 腿是唯一非恒真的比对）。`check_route_coverage()` 实现 3a（登记方法必须在 handler 存在）+ 3b（handler 实现的方法必须至少被一条绑定到它的路由登记），对真实注册表实测 0 违规
- [x] 2.8 RED：构造「handler 实现 `post` 而清单只登记 `get`」的用例，确认校验以「实现与登记不一致」失败。`test_handler_method_missing_from_registry_fails`（正向缺失）与 `test_registered_method_absent_from_handler_fails`（反向死登记）双向覆盖
- [x] 2.9 补齐 5 条实测未登记路由的策略与权限：`/admin`（tenant）、`/api/identity/administered-tenants`（personal，个人域作用域）、`/api/scenes`（tenant）、`/api/scenes/activate`（tenant）、`/api/scenes/workbench/import`（tenant）；RED 覆盖「未登记路由被调用即拒绝」。`test_previously_unregistered_routes_are_now_gated` 断言 `_match_policy` 由迁移前的 `(None, False)`（绕过门禁）变为匹配且有策略
- [x] 2.10 把 2.7/2.9 不变量接入 CI（或既有测试入口），使「合并上游后丢路由」与「handler 实现未被登记」在合并当下即可发现。新增 `scripts/check-route-coverage.py`（违规 exit 1，导入失败 exit 2；同时校验两张表仍是注册表的精确投影，输出 115 routes / 147 方法条目的规模摘要），由第 9 组同步脚本在合并后调用；`tests/test_route_registry.py` 亦随 `pytest tests/` 常规入口执行
- [x] 2.11 验证 fork 扩展路由经注册后同时出现在 URL 表与策略表，且核心文件路由字面量未被编辑。`ForkExtensionTests` 注册 `/api/fork/probe` 后断言 URL 表绑定、策略表 policy 与三腿校验通过，并断言两个核心文件均不含该 pattern
- [x] 2.12 在 `evidence.md` 明确记录：D1 的收益为安全与可维护性，**不减少** `web_channel.py` 的实测冲突（其 3 处冲突为方法签名与上游新功能）。见 `evidence.md` §3.4；另记 §3.1 迁移前实测缺陷（5 条绕过门禁 + 4 条第三腿发现）与 §3.5 上游 8 条 fork 缺失路由（记录不修复）

## 3. 路由门禁升级为真门禁（P0）

- [x] 3.1 RED：`tenant` 路由缺租户选择时在进入 handler 前返回 400 的测试。已落地：`tests/test_http_gate.py::GateDecisionTests::test_tenant_route_without_selection_is_400_before_handler`（单元：解析 400 且 handler 未执行）与 `GateIntegrationTests::test_tenant_route_without_selection_is_rejected_before_the_handler`（真实 app：`/api/logs` 仅 `_require_auth()`，修复前无租户选择也会进入 handler 流式返回日志，现为 400 `missing_tenant`）
- [x] 3.2 RED：无该租户有效成员关系（或账号/租户停用）访问 `tenant` 路由时在进入 handler 前返回 403 的测试。已落地：`GateDecisionTests::test_tenant_route_without_membership_is_403_before_handler`；真实 app 侧由 `test_web_chat_boundary.py::test_stream_reconnect_revalidates_login_and_membership[membership]` 与 `test_http_gate.py::GateStagedRolloutTests::test_deterministic_failures_are_never_deferred` 覆盖
- [x] 3.3 RED：无效/缺失凭据访问 `platform` 路由返回 401（而非进入 handler）的测试。已落地：`GateDecisionTests::test_platform_route_without_credentials_is_401_before_handler`、`GateIntegrationTests::test_anonymous_platform_route_is_401_not_a_handler_response`
- [x] 3.4 在 `enforce_http_policy` 内对 `tenant`/`platform` 路由调用统一上下文解析入口并注入 ContextVar；解析结果供 handler 复用，避免重复身份库读取。已落地：`auth/http_policy.py::_enforce_context_gate` 调用 `channel.web.auth_handlers._require_context`，经新增 `auth/runtime.py::gate_context_scope` / `cached_gate_context`（按 `require_tenant` 分键）发布，`_resolve_ctx_svc` 命中即复用；缓存随请求重置，不跨请求泄漏（`GateContextCacheTests`）
- [x] 3.5 明确门禁职责边界：门禁负责「上下文存在性 + 身份域 + 必需 permission」；对象级属主/资源校验仍在 handler，且其所需身份由门禁保证。已落地：门禁只做 `_require_context` + `is_platform_admin` 域判定 + 登记的 `permission`；`GateIntegrationTests::test_gate_does_not_replace_object_level_ownership_checks` 以 spy 证明门禁放行后 `_require_session_scope` 仍以门禁解析出的同一上下文被调用并拒绝
- [x] 3.6 确认 `public`/`closed` 路由不被解析、legacy 模式门禁为空操作（行为不变）。已落地：`GateScopeTests`（`/api/health` 不解析、`/api/scheduler` 503 且不解析、legacy 直通、未知 URL 交给 web.py 404）；唯一例外 `GET /stream`（原生 EventSource 无法带头）在权威清单中以 `tenant_from_resource=True` 显式声明，门禁改为只校验凭据（`test_route_deriving_its_tenant_from_the_resource_skips_selection`、`test_resource_derived_tenant_still_requires_a_session`、`test_route_registry.py::test_stream_declares_its_tenant_is_resource_derived`）
- [x] 3.7 先以「解析失败仅告警」观察一轮再切换为拒绝；对 `tenant/platform` 身份域判定立即生效。已落地：确定性失败（400/401/403）立即拒绝；仅**非预期**失败默认告警并放行观察，`config.http_policy_gate_fail_closed=true` 时返回 503 且不进入 handler（`GateStagedRolloutTests` 三项，含「开关不得软化确定性失败」的反向用例）
- [x] 3.8 测试：门禁不替代对象级校验——持合法上下文访问他人资源的请求仍被 handler 的属主校验拒绝。已落地：`GateIntegrationTests::test_gate_does_not_replace_object_level_ownership_checks`

## 4. 关闭已确认越权面（P0 收口）

- [x] 4.1 RED：跨租户用例——A 租户会话以 B 租户 `session_id` 调用会话删除/改名/置顶/清上下文与消息删除，期望 403/404 且无副作用。测试：`tests/test_session_idor_closure.py`（10 项，含「守卫拒绝先于任何 store 写入」与 404/403 传播）
- [x] 4.2 `SessionDetailHandler.DELETE/PUT`、`SessionTitleHandler.POST`、`SessionClearContextHandler.POST`、`MessageDeleteHandler.POST` 包入 `_db_scope()` 并复用 `_require_read_permission` + `_require_tenant_agent_binding` + `_require_session_owner`，使 4.1 转 GREEN。新增统一助手 `_require_session_scope()`（租户绑定 + 可见性 + 持久属主三重）；并补 `except web.HTTPError: raise` 使 4xx 不被吞成 200 JSON（与仓库既有 20 处同类写法一致）
- [x] 4.3 RED：跨租户用例——A 租户会话读写 B 租户 Agent 核心文件与头像，期望 403/404 且不修改任何 Agent。测试：`test_core_file_read_refuses_without_edit_grant`、`test_avatar_upload_refuses_without_edit_grant`
- [x] 4.4 `AgentCoreFileHandler.GET/PUT`、`AgentAvatarHandler.GET/POST` 包入 `_db_scope()` 并复用 `_require_agent_action`：核心文件 GET/PUT 与头像 POST 用 `edit`/`agent.edit`；头像 GET 用 `read`/`agent.read`（成员需能看到租户 Agent 头像，读与改的能力必须匹配——相对任务原文的收窄，理由记入本条）
- [x] 4.5 按 D3 修正空租户工作区解析：`_get_workspace_root` 在 database 模式 `ident.tenant_id` 为空时返回 403，删除到全局默认 Agent 工作区的回退分支（仅 legacy 保留）。测试：`test_workspace_root_refuses_empty_tenant_in_database_mode`、`test_workspace_root_keeps_the_legacy_fallback`
- [x] 4.6 RED：无任何租户成员关系的账号调用会话/Agent 文件接口被拒（不能凭已知 id 访问）。由 `_db_scope(require_tenant=True)`（400）+ `_tenant_ids_for_context` 为空导致 `_require_session_owner` 403 + `_require_owned_session` 404 共同覆盖，测试桩已复现该路径
- [x] 4.7 `bind_channel_instance` 收归平台域（或改走租户渠道接口），平台名册写入经 `_require_platform_console()` 并补审计；移除租户经 `/api/agents` 改写实例级名册的能力。已落地：`AgentsHandler.POST` 的该分支先过 `_require_platform_console()`（database 模式解析上下文并要求 `is_platform_admin`；legacy 仍走共享口令，返回 None），再以平台上下文做 `_require_agent_action`；写入后经新增 `IdentityService.record_audit()`（公开审计接缝，服务实例级名册位于 `team.json`、无身份事务可依附）记录 `channel.instance.bind`（含 channel_type/agent_id/members，经 `sanitize_payload`）且尽力而为——审计失败不回滚已生效的绑定。同处补 `except web.HTTPError: raise`，使拒绝以 `{"code":"forbidden"}` 结构化返回而非被泛化分支吞成 `code:null`。测试 `tests/test_channel_roster_platform_guard.py`（7 项：租户管理员 403 且写入 spy 未触发、拒绝不产生审计、平台管理员可绑定、审计字段与 actor、legacy 行为不变且不写身份审计）。理由：`_tenant_admin_owns_agent` 会让持 `agent.edit` 的租户管理员改写**全实例共享**的路由表；租户自有渠道经 `/api/tenant/channels`（身份库）管理，是另一条路径
- [x] 4.8 `IdentityAdministeredTenantsHandler` 明确作用域（个人域：凭据主体自身可管理的租户集合）并纳入权威清单策略；RED 覆盖「以他人 `user_id` 探测成员状态」的越权拒绝。已落地：路由已在第 2 组以 `personal` 策略登记（`/api/identity/administered-tenants` GET）。作用域收口在服务层：新增 `IdentityService._administers_target()`（单次集合查询：目标账号与主体在任一「主体为有效 tenant_admin 且租户有效」的租户内共同有效），`administered_tenants(target_user_id=…)` 在目标既非主体自身又不可见时抛 `forbidden` 403；handler 补 `except IdentityServiceError → _service_error`（此前异常会逃逸成 500/未处理）。测试 `test_identity_web_handlers.py` 新增 3 项：越界账号 403 且响应不含 `items`/`member_id`、未知账号与越界账号响应不可区分（同为 403 forbidden，防存在性 oracle）、所管理租户内成员仍成功且只返回该租户（正向对照）。变更前实测该接口对任意账号返回 `{"member": false, …}`
- [x] 4.9 `scenes/api.py` 与 `scenes/api_workbench.py`：登记 `ROUTE_POLICY`（tenant 域 + 对应 permission）、进入 `_db_scope`、写操作补来源/CSRF 校验。已落地：三条 routes 已在第 2 组以 tenant 策略登记，本次补上功能权限 `chat.use`（场景目录/激活/导入都属 chat 消费者面，平台与租户管理员通过）。`scenes/api.py` 的 `ScenesHandler.GET` 与 `SceneActivateHandler.POST`、`scenes/api_workbench.py` 的导入均在 `_db_scope()` 内执行并 `_require_chat_use(ctx)`；两处写操作先过新增统一助手 `channel.web.auth_handlers.require_management_write()`（cookie 凭据须来源匹配，bearer 须真实认证才豁免），导入的 `except web.HTTPError: raise` 保证 4xx 直传。`scenes/service.py` 的会话场景上下文改为按租户命名空间存放（`_scene_scope`：默认取运行时身份的 `tenant_id`；legacy 为 `""`，行为不变）
- [x] 4.10 RED：scenes 跨租户读取/激活/导入被拒且不写全局工作区的测试。测试：`tests/test_scenes_tenant_scope.py`（9 项）。跨租户激活：`session_id` 由客户端生成，旧实现以全局内存表按 `session_id` 单键存放，A 租户可为 B 租户的会话标识激活场景并注入其提示词/技能——现按 `(tenant, session_id)` 分键，`test_an_activation_only_lands_in_the_callers_tenant` 与 `test_a_tenant_cannot_read_another_tenants_activation` 分别断言「同 id 在他人租户读到未激活」与「不覆盖他人租户既有激活」，并已用 `_scene_scope → ""` 的临时降级验证该用例在迁移前为 RED。导入：`test_the_import_writes_into_the_tenant_root_not_the_global_workspace`（断言文件落在租户根、全局根无 `tmp/`）、`test_the_import_refuses_when_the_scope_cannot_be_resolved`（作用域解析失败即 400 且工作区未被解析）、`test_a_cross_origin_import_is_refused`。另加目录权限（403/200）与跨来源写拒绝共 4 项
- [x] 4.11 `LogsHandler`/`LogsDownloadHandler` 限定平台管理员（或平台 + 租户管理员且按租户过滤），对输出脱敏；RED 测试覆盖普通成员 403 与跨租户拒绝。已落地：`run.log` 为进程级文件（含全部租户活动），无法做按租户过滤，故采取「平台管理员专属」方案：`/api/logs` 与 `/api/logs/download` 的权威策略由 `tenant` 改为 `platform`（门禁在进入 handler 前要求平台管理员），两个 handler 同时以 `_require_platform_console()` 自行复核（legacy 沿用控制台口令）。输出脱敏：新增 `_LOG_SECRET_RE` / `_redact_log_line` / `_redact_log_text`，掩蔽 password/secret/token/api key/authorization（含 `Bearer <token>` 形式）/cookie/credential/session id 等 `key: value` 与 `key=value` 的值并保留字段名，流式首屏、逐行与整文件下载三处均经脱敏。测试 `tests/test_logs_surface_scope.py`（7 项：成员流式/下载 403、平台管理员可读、下载与流式均掩蔽 `sk-live-*`/`hunter2`/`tok_123`、保留字段名、普通行原样）；已用「`_require_platform_console` 置空」验证成员 403 两项在迁移前为 RED（旧实现返回 200 与全文日志）
- [x] 4.12 抽取统一「管理写」来源 + CSRF 校验并接到 `channel/web/admin_handlers.py` 全部平台域/租户域写接口；RED 测试覆盖跨来源写被拒。已落地：`channel/web/auth_handlers.py::require_management_write()`（4.9 引入）作为唯一入口，`admin_handlers.py` 以 `_require_management_write` 别名导入并接入**全部 25 个写方法**（`PlatformUsersHandler.PATCH`、平台/租户账号与外部身份、租户/角色/部门/渠道、平台租户角色、租户资源等 POST/PUT/PATCH/DELETE），插入点统一在 `_guard_database()` 之后、任何上下文解析与写入之前——database 模式先行拒绝，legacy 仍由 `_guard_database()` 以 `not_database` 400 短路（行为不变）。规则：cookie 凭据的写请求必须给出与 Host 同源的 `Origin`/`Referer`（缺失不再默认放行，因为这些接口没有 CSRF token 流程，`channel/web/todo_handlers.py` 的宽松副本不在此列）；bearer 凭据（桌面端 `file://` 来源永不匹配）仅在 bearer 真实认证时豁免，裸 bearer 或与 cookie 同值的重复凭据不豁免。RED 证据：`tests/test_admin_write_origin_guard.py`（7 项）覆盖「无来源 cookie 写 403 且未落库」「跨源 cookie 写 403 且未落库」「同源 cookie 写放行」「Referer 可替代 Origin」「平台写同样受控且账号未被停用」「真实 bearer 写免来源（跨源亦放行）」「裸 bearer 不豁免」；以 `admin_handlers._require_management_write` 置空的临时降级重跑，前四项全部失败（写请求到达业务分支并落库），确认这些断言在接入前为 RED。连带修订 3 个 cookie 写测试的请求装置补 `Host`/`Origin` 同源对（`tests/test_tenant_channel_http.py` 两处、`tests/test_tenant_agent_copy_http.py`、`tests/test_external_identity_http.py`），断言本身未放宽
- [x] 4.13 修订受影响既有测试（`tests/test_http_policy.py`、`tests/test_web_chat_boundary.py`、相关 identity/tenant 测试），记录为有意行为变更，不放宽断言迁就旧行为。已完成部分：`test_identity_web_handlers.py` 的 fake `_db_scope` 补齐真实契约（`use_identity(to_runtime_identity(ctx))`）、租户改用真实 `self.tid` 与可写 shared root；`test_trajectory_eval.py` 补 `effective_cwd` 与 legacy 模式固定；`test_self_authorized_tools.py` 固定 legacy 模式。第 2 组连带：`test_http_policy.py::test_register_route_rejects_unregistered_method` 的示例由 `POST /api/weixin/qrlogin`（现已按文档登记为 closed）改为未实现的 `DELETE`；`test_agent_web_management.py` 与 `test_scheduler_web_update.py` 原先 grep `web_channel.py` 源码中的路由字面量，改为断言派生后的 `_WEB_URLS` 绑定（更强契约：证明 pattern 确实到达该 handler）。断言未被放宽

## 5. 执行授权与隔离 fail-closed（P0）

- [x] 5.1 RED：注入身份服务异常，验证 `_resource_tool_denial` 拒绝执行且不产生副作用（当前为告警后放行）。测试：`tests/test_execution_authorization_fail_closed.py::test_identity_service_exception_denies`（改前失败/改后通过）
- [x] 5.2 `agent/protocol/agent_stream.py::_resource_tool_denial` 改为身份不可解析即拒绝并告警：身份缺失仅在 database 模式拒绝（legacy 保留历史不受限），身份服务异常一律拒绝
- [x] 5.3 RED：身份缺失时 `isolation_decision` 拒绝，且非 `CODE_TOOLS` 与 `CODE_TOOLS` 行为一致。测试：`test_gate_on_without_identity_denies_code_tools`、`test_gate_on_without_identity_denies_file_tools_too`、`test_gate_malfunction_denies_non_code_tools`
- [x] 5.4 `agent/permission/isolation.py::isolation_decision` 改为 fail-closed，移除返回 `Decision(True)` 的缺失/异常路径（异常路径不再按 `CODE_TOOLS` 区分）
- [x] 5.5 在后台线程/定时任务/异步执行器边界显式检测身份上下文可用性，避免空身份被当作合法「无用户维度」；新增边界须有测试或启动断言表明身份来源。核实：`runtime_identity.submit/wrap` 以 `copy_context()` 携带身份、`ChatChannel._handle` 用 `use_identity` 重建身份、agent_stream 并行工具与 subagent runner 均用 `copy_context()`；失败边界由 fail-closed 门禁兜底。契约测试：`tests/test_identity_boundary_contract.py`（6 项）
- [x] 5.6 记录 fail-closed 拒绝的可诊断告警与计数，供后续审计切片复用：新增 `common/security_events.py`（进程内计数 + 结构化告警 + 经 `auth.audit` 尽力落审计，失败不影响拒绝结果）

## 6. 会话存储约束级组合接缝与租户纵深防御（P1 切片）

- [x] 6.1 在 `agent/memory/conversation_store.py` 引入扩展点，覆盖六类：列定义列表、**主键与唯一约束**、索引定义、INSERT 列/占位/参数构造、公共 WHERE 片段、迁移步骤登记；上游智能体维度与 fork 所有者/租户维度各为独立扩展项
- [x] 6.2 RED：组合断言测试——两侧各注册一列后表结构含两列，插入/查询/分页路径均使用组合后的列与 WHERE 条件
- [x] 6.3 RED：**约束级组合测试**——上游注册复合主键 `(agent_id, session_id)` 与唯一约束 `(agent_id, session_id, seq)`、索引 `(agent_id, last_active)` 后，合并结果采用 0.1 所决定的方案，不因 fork 既有单列主键而丢失复合键
- [x] 6.4 RED：缺省扩展点时不改变上游语义（无额外过滤、表结构与约束同上游）
- [x] 6.5 按 0.1 决定落地主键/唯一约束变更，并提供**专门的回退迁移**（主键变更不可自动回滚）；在接近生产规模的复制库上演练升级与回滚
- [x] 6.6 以组合接缝落地 `tenant_id` 列（`ALTER TABLE ... DEFAULT ''`）作为独立于 `owner` 的扩展项；写入路径固化租户、检索路径纳入过滤
- [x] 6.7 RED：跨租户读取测试——工作区/索引被跨租户访问时，带租户维度的检索不返回其他租户行（由存储层过滤保证）
- [x] 6.8 迁移回填：**以行的 `owner` 对应用户的租户成员关系**为权威来源回填，MUST NOT 仅按 Agent 当前归属；归属不可确定（`owner` 缺失、无有效成员关系、多义）的行不赋予推测维度并保持不可跨租户读出；回填前后行数比对，验证幂等
- [x] 6.9 回滚验证：停用第二道过滤后读写沿用原语义、不报错、不改变既有可见性
- [x] 6.10 在 `agent/memory/storage.py` 的记忆索引（`chunks`/`files`）同样纳入租户维度过滤
- [x] 6.11 按 0.3 的结论同步 `app.py` 的 `_migrate_conversations` 迁移钩子与存储接缝，确保两者假设一致（不出现「app.py 全局化而接缝仍按每 Agent 文件」的互不兼容实现）

## 7. 默认 Agent、RBAC 约束与配额 fail-closed（P1 切片）

- [x] 7.1 RED：`default_agent_id` 指向不存在/已解绑/已停用 Agent 时，解析在本租户内按确定性顺序回退，不落到进程全局默认或其他租户 Agent
- [x] 7.2 在 `auth/service.py::resolved_default_agent_id` 增加绑定存在且启用的校验与确定性回退，使 7.1 转 GREEN；回退记录可诊断原因
- [x] 7.3 RED：本租户无任何合法 Agent 时解析失败并拒绝，不借用其他租户或平台默认
- [x] 7.4 RED：直接插入跨租户 `membership_roles` / `role_resource_grants` 被数据库拒绝
- [x] 7.5 `auth/store.py` 迁移：为两表加 `tenant_id` 并用复合外键 `(tenant_id, role_id)`、`(tenant_id, membership_id)` 约束；补 `roles`/`memberships` 的 `UNIQUE(tenant_id,id)`
- [x] 7.6 RED：配额判定异常（计量存储不可读/归属无法解析）时消耗性请求默认被拒绝
- [x] 7.7 配额检查改为默认 fail-closed（可按部署配置显式放宽并留痕）；`quota_limits`/`quota_usage` 补租户外键
- [x] 7.8 抽取 `_seed_tenant_defaults(tenant_id)` 供 `bootstrap` 与 `create_tenant` 复用，消除引导逻辑漂移

## 8. fork 定制逻辑迁出上游核心文件（可合并性架构）

- [x] 8.1 清点仍内嵌于上游核心文件的 fork 专有分支，登记到 `evidence.md`（含文件、行区、接缝方案与迁移顺序）；至少覆盖 `upload_file`/`post_message` 签名、`_get_workspace_root` 回退、`app.py` 启动守卫。已落地：`evidence.md` §8.1 给出 11 行清点表（文件 / fork 内容 / 迁移前行区 / 接缝 / 状态），覆盖任务点名的三处并补入 `conversation_store.py`、`storage.py`、`scheduler/integration.py`、`channel_instances.py`、`console.js`、`chat.html`；两行标为「additive only / 人工纪律」并附触发条件（§10.3）
- [x] 8.2 RED：签名接缝测试——上游方法签名不被改写（`upload_file` 保持上游签名、`post_message` 参数经上下文/授权目标解析获得），且 fork 侧所需授权目标仍能生效。已落地：`tests/test_channel_signature_seam.py`（11 项）——`test_upstream_channel_methods_take_no_fork_parameters`（四方法仅 `(self)`）、`test_no_database_only_keyword_survives_on_the_channel`、`test_a_published_target_is_not_shared_across_threads`（线程隔离）、`test_a_reader_cannot_mutate_the_published_target`（返回只读映射）、`test_post_message_uses_the_published_authorized_session`、`test_upload_file_reads_the_authorized_agent_from_the_target`
- [x] 8.3 按 D11 落地 `channel/web/web_channel.py` 的签名接缝：fork 所需参数经门禁注入的请求上下文或独立授权目标解析器取得；无法经上下文获得时以独立包装函数承载，上游方法体保持原样。已落地：新增 `auth/runtime.py::authorized_target_scope()` / `authorized_target()`（请求级 `ContextVar`），四个方法（`upload_file`/`post_message`/`cancel_request`/`poll_response`）恢复上游签名并改为从授权目标读取；发布点在各 `Handler.POST`（`with authorized_target_scope(...)`），由已授权的处理器提供，门禁与对象级校验顺序不变
- [x] 8.4 RED：上游功能保留测试——`_import_local_file`（桌面端按路径导入）在合并结果中存在，且 loopback 与每启动令牌校验生效；非 loopback 或令牌不匹配的请求被拒。已落地：`tests/test_upstream_core_seams.py::UpstreamFeaturePreservationTests`——`test_the_merge_obligation_is_recorded` 断言 `scripts/conflict-baseline.txt` 中 `keep upstream _import_local_file` 义务存在，`test_local_file_import_is_loopback_and_token_guarded_when_present` 在上游符号落地当刻即要求 loopback + 每启动令牌校验（当前符号不存在故 skip 并给出理由），`test_the_fork_keeps_no_unauthenticated_local_path_route` 断言不存在未登记策略的本地路径路由
- [x] 8.5 `console.js` 的 i18n 按域拆为独立命名空间文件（如 `i18n/tenant-channel.js`、`i18n/tasks.js`）并在加载时合并为单一查找表；RED：i18n parity 测试覆盖「各命名空间键完整且无重叠」，`console.js` 不承载任何域的完整语言键。已落地：拆为 14 个 `static/js/i18n/*.js`，各自注册 `window.__cowI18N__`，`console.js` 启动时合并（保留 `window.I18N` 与 `t(key)` 兼容层）；`ChatHandler.GET` 动态发现命名空间文件（不写死清单，新增域不需改服务端）。`tests/test_console_i18n_parity.cjs`（5 项）：合并表与拆分前快照 `tests/fixtures/console_i18n_snapshot.json` 深度相等（无损）、各域无键重叠、每种语言键集合与快照一致、`console.js` 不再内联任何域字典（含代表性 fork/上游键断言）
- [x] 8.6 引入视图注册表替代 `console.js` 的 fork 专有视图分派（`loadPlatformUsersView`、`loadTodosView` 等约 13 处引用）；fork 模块注册 `{id, label, load, repaint}`，`console.js` 按注册表迭代、不含 fork 专有分支。已落地：`CONSOLE_VIEW_REGISTRY` + `registerConsoleView`（按 id 去重、暴露为 `window.registerConsoleView`）+ `_loadRegisteredView`/`_repaintRegisteredView`，`navigateTo` 与 `rerenderDynamicViews` 改为迭代注册表；13 处旧分支（6 repaint + 6 navigate + 1 todo）删除，7 个 fork loader 名在 `console.js` 中零命中；`identity-admin.js` 注册 6 个管理视图、`todos.js` 注册 `todo`。`tests/test_console_view_registry.cjs`（6 项）含「空注册表降级为静默 no-op（上游构建仍可导航）」
- [x] 8.7 以既有 33 个 `.cjs` 前端契约测试锁定迁移前后行为一致；i18n 迁移期保留旧键聚合层作为兼容别名直至测试迁移完成。已落地：兼容层 `window.I18N`/`t(key)` 保留；逐文件运行（`node --test --test-timeout=15000`，`node --test tests/*.cjs` 存在既有串扰/挂起，非本 change 引入）对比干净 HEAD worktree——基线 372 pass / 21 fail，迁移后 385 pass / 21 fail，唯一差异为 3 个新测试的 +13 pass，无回归；基线失败项在干净工作树同样失败（`test_appearance_browser.cjs` 缺 Playwright、`test_session_history_frontend.cjs`、`branding_frontend.test.cjs`、`_tmp_repro_modeldefaults.cjs` 既有草稿）
- [x] 8.8 把 `chat.html` 的 fork 专有块迁移到受挂载点管理的片段，保持既有 DOM 契约。已落地：新增通用挂载机制 `static/js/fragments.js`（查询 `[data-fork-fragment]`、fetch 注入、重跑 `applyI18n()`、派发 `fork-fragment-mounted`）与 `static/fragments/appearance-dialog.html`（字节级同源迁移，`id`/ARIA/`data-i18n`/`onclick` 契约保留，`#appearance-dialog` 仍在 `chat.html` 上因此 `console.js` 的加载期监听绑定不变）；`tests/test_fork_fragments.cjs`（5 项）锁定挂载点与片段契约。补一处实测缺口：`ChatHandler.GET` 原先只对脚本/i18n 做缓存击穿，运行时抓取的 loader 与片段会取到浏览器旧副本，现同时发现并击穿 `js/fragments.js` 与 `static/fragments/*.html`（发现式，新增片段不需改服务端）。范围界定：`view-todo`/`view-tenant`/`view-platform` 等约 10 个 fork 视图容器**有意保留内联**——`test_nav_area_frontend.cjs`/`test_admin_home_frontend.cjs`/`test_tenant_tabbed_editor_frontend.cjs` 直接断言其 id 存在于 `chat.html`，且实测与上游的 `chat.html` **块级**冲突只有 appearance 对话框一处（另一处为内联 composer 文案的逐行编辑），迁移其余块会改变 DOM 时序并违反「保持既有 DOM 契约」，故以机制 + 单块示范交付，剩余块留待其真正冲突时按同一机制迁移
- [x] 8.9 `app.py` 的 fork 启动守卫（如身份模式一致性检查）改为扩展钩子登记，而非核心流程内联分支；与 6.11 的会话全局化决策保持一致。已落地：新增 `common/startup_hooks.py`（`register_startup_hook`/`run_startup_hook`/`registered_hooks`），`app.py::_guard_identity_mode_consistency` 与 `_migrate_conversation_tenancy` 改为调用钩子，守卫与回填实现整体移入钩子模块
- [x] 8.10 验证「接缝可独立启用与回退」：移除或停用 fork 接缝模块时上游核心逻辑按上游语义运行、不报错、不产生部分写入。已落地：各接缝的「缺失分支」由测试直接断言而非人工检查——无授权目标时 `post_message` 走上游路由（`test_post_message_falls_back_to_upstream_routing_without_a_target`）、钩子未登记即空操作（`test_an_unregistered_hook_is_a_no_op`）、legacy 模式下工作区解析返回 `None` 交回上游（`test_legacy_mode_returns_none_so_upstream_fallback_runs`）、无 store 的工作区不被创建（`test_a_workspace_without_a_store_is_not_created`）；汇总见 `evidence.md` §8.6，实测 `40 passed, 1 skipped`
- [x] 8.11 复核上游核心文件（路由字面量、启动流程、公共方法签名、会话存储 schema 定义）不再含 fork 专有原地定义或签名改写。已落地：四类面各有机制化断言（`test_web_channel_derives_its_urls_from_the_registry`、`test_app_py_has_no_inlined_fork_guard` + `test_the_guards_exist_in_the_hook_module`、`test_upstream_channel_methods_take_no_fork_parameters`、`test_the_conversation_store_uses_the_composable_schema`）；剩余两行（`web_channel.py` 的 additive `_require_*` 助手、`channel_instances.py` 人工分区）为不编辑上游方法体的形态，已连同触发条件记入 `evidence.md` §8.7 与 §10.3
- [x] 8.12 RED：渠道实例模块分区断言测试——fork 的凭据最小必填集与租户运行时符号、上游的类型标签与默认名符号分别存在于各自区块，且 `required_credential_keys` 对已声明类型的返回值不回归。已落地：`tests/test_channel_instances_partition.py`（18 项）以 AST（非文本 grep）断言分隔线两侧符号归属，并以 `REQUIRED_CREDENTIAL_KEYS` 全量 golden 快照锁定每个已声明类型的返回值
- [x] 8.13 `channel/channel_instances.py` 按 D4b 分区登记；并在 `design.md` 记录「人工纪律、非机制」与触发条件（该文件再次同址冲突，或 `auth/service.py` 凭据校验路径变更时转搬迁式机制化）。已落地：文件底部单行分隔注释，上方为上游/共享区（含为上游 `_CHANNEL_TYPE_LABELS`/`default_instance_name` 预留的位置）、下方为 fork 区（凭据最小必填集、租户运行时状态、租户控制台契约）；本次为纯重排 + 注释，符号/取值/默认值/校验零变化；`design.md` D4b 记录纪律性质与两个触发条件
- [x] 8.14 RED：调度任务身份行为测试——带创建者快照的任务以该成员身份执行（工作区/会话/记忆按成员解析）；仅具 Agent 维度的历史任务按 Agent 范围执行；身份不可解析时拒绝并记录原因。已落地：`tests/test_scheduler_identity_seam.py` 覆盖成员快照任务（以该成员身份解析）与 legacy Agent-only 任务（不臆造租户/用户），不可解析时的拒绝路径由既有 `tests/test_scheduler_identity_revalidation.py`（撤销授权即跳过并记录原因）覆盖
- [x] 8.15 按 0.2 的决策收敛 `agent/tools/scheduler/integration.py` 为单一身份解析接缝：以任务身份快照为唯一入口，租户成员与 Agent-only 作为两种输入，两侧特有行为（revalidation、delivery、run 记录）作为下游策略接入；确认身份上下文唯一入口为 `common/runtime_identity.py`。已落地：`agent/tools/scheduler/identity.py::execution_identity` 为唯一解析器（两类输入显式分支），`integration.py` 直接调用且不再自建 `RuntimeIdentity`；测试断言该解析器在全包内仅定义一次、身份只经 `common.runtime_identity` 进入运行时
- [x] 8.16 移除收敛后遗留的重复身份解析实现，确认不存在两套并列路径与并列回调契约；保留必要的签名兼容层直至调用点迁移完成。已落地：删除 `integration.py` 的本地 `_execution_identity` 及其在 `use_identity` 处的转发，调用点直接使用收敛解析器；`tests/test_scheduler_identity_seam.py` 断言该文件既无本地解析器定义也无直接 `RuntimeIdentity` 构造
- [x] 8.17 修订 `tests/test_scheduler_web_update.py` 等随上游行为变更的调度测试，记录为有意行为变更。已落地：该测试由源码文本 grep 改为断言派生后的 `_WEB_URLS`/策略表绑定（第 2 组完成），属有意且严格更强的契约，理由记入 `evidence.md` §8.5；断言未被放宽

## 9. 上游同步基建（可合并性运维）

- [x] 9.1 启用 `rerere`（仓库级建议与文档说明；脚本不强制写用户全局配置）
- [x] 9.2 新增 `.gitattributes`，仅标注 fork 单方定制的二进制资源（图标、logo、图片）为 `binary`；不引入会静默丢弃上游改动的合并策略
- [x] 9.3 新增 `scripts/sync-from-master.sh`：`fetch` + 合并尝试 + 冲突文件与规模报告；MUST NOT 自动提交或推送。报告 SHALL 与 1.6 的基线清单对比并标出漂移（新增/消失的冲突文件），并 SHALL 提示 5 个删除/修改类文件的决策状态
- [x] 9.4 RED：脚本在存在冲突时以非零退出并输出冲突清单；有基线时标出相对基线的漂移；无冲突时输出「已合并、待复核」
- [x] 9.5 在 `README`/`CONTRIBUTING` 记录同步节奏（上游发版后或每周）与人工复核要求
- [x] 9.6 处理 `.gitignore` 冲突（实测冲突项为 fork 的 `/openspec/` 忽略块）：确认该块为 fork 本地约定，合并时保留 fork 侧并置于独立区段、标明归属，避免与上游新增忽略规则互相覆盖
- [x] 9.7 落地 D9：把 5 个删除/修改类文件（`README.md`、`docs/zh/README.md`、`docs/zh/README-Hant.md`、`docs/ja/README.md`、`desktop/.../PermissionSelector.tsx`）的处置决策写入 1.6 基线清单并附理由

## 10. 验证、证据与文档

- [x] 10.1 运行受影响测试套件（`test_http_policy`、`test_web_chat_boundary`、identity/tenant/todo 相关、前端 33 个 `.cjs` 契约、i18n parity）并记录结果。已落地：与实施前检查点 `dc760766` 的 worktree 基线（**补入本机 gitignore 的 `config.json`**，见 `evidence.md` §5.1.1）做整仓对比：基线 33 失败 / 2470 通过 → 现在 28 失败 / 2692 通过，**新增失败 0**，且当前失败集是基线的**真子集**（其中 5 项为本次修复而非替换）。命令与 `comm` 差集见 §10.5。另以真实 headless Chrome 经 CDP 走查控制台（§10.6），走查发现并修复一处 Python 套件未能覆盖的 P0：`/chat`、`/admin` 的 `tenant` 策略使顶层浏览器导航（无法携带 `X-Tenant-ID`）在进入 handler 前即被 400，登录界面永不渲染
- [x] 10.2 编写 `evidence.md`：模型决策（0.1–0.4）、单一路由事实源等价断言、三腿覆盖不变量（含 handler 内省用例）、门禁解析、跨租户 IDOR、fail-closed、存储约束级组合与回填、默认 Agent 解析、上游功能保留（桌面本地导入）、DB 不变量各自给出可复现命令与实际输出。已落地：`evidence.md` §1–§10 按主题分节，每节给出测试名与 `pytest` 命令/实测输出；§10.5 为整仓回归对比，§10.6 为浏览器走查证据（含登录、i18n、视图注册表、片段挂载、日志面权限，以及非 2xx 的分类）
- [x] 10.3 在 `evidence.md` 记录启用门槛状态（`scan-onboarding-and-inbound-anchor` 归档），未满足时明确写「已实现、待归档」；需求基线以 `openspec/specs/` 为准，不引用存档级 PRD 编号。已落地：`evidence.md` §10.1 记录前置 change 已归档（`archive/2026-09-11-scan-onboarding-and-inbound-anchor`），`openspec validate --strict` 通过；全文需求引用均指向 capability 与 `openspec/specs/`，无 PRD 编号
- [x] 10.4 更新受影响既有规范正文（`enterprise-access-enforcement`、`console-route-lifecycle`、`resource-execution-authorization`、`tenant-channel-configuration`、`tenant-resource-isolation`、`resource-quota`、`audit-log`）并在归档时合并本 change 的 delta。已落地：`openspec archive` 执行正文合并，实测 `+23 added / ~5 modified / -0 removed`——`audit-log` +1、`console-route-lifecycle` +5、`enterprise-access-enforcement` +3 ~1、`fork-upstream-decoupling` +9（新 capability）、`resource-execution-authorization` +2、`resource-quota` ~1、`scene-activation` +1 ~2、`tenant-channel-configuration` ~1、`tenant-resource-isolation` +2；归档后 `openspec validate --specs --strict` 通过，无 active change 残留
- [x] 10.5 归档前复核 delta 与基线标题一致性：MODIFIED 块标题与基线逐字匹配、覆盖基线全部 scenario；ADDED 块不复制其他责任域规范。已落地：复核固化为可重跑脚本 `scripts/check_change_deltas.py`，按 change 所在位置自动切换两种语义——`proposed`（`changes/<name>/`：MODIFIED 标题逐字匹配、保留基线 scenario、ADDED 不复述）与 `applied`（`changes/archive/<date>-<name>/`：ADDED/MODIFIED 的 requirement 与 scenario **均已落到正文**、REMOVED 已消失）；18 个用例反向锁定（改写标题、丢 scenario、重复 ADDED、跨域 ADDED、归档后误判、未落正文、REMOVED 残留、未知操作块、改名找不到均被拒）。**归档后实测该脚本曾失效**（找不到 change；且修好定位后会把 23 个 ADDED 全判为重复），故补 `applied` 模式；未识别的操作块（如 RENAMED）一律报错而非跳过。命令与结果见 §10.7
- [x] 10.6 复核覆盖完整性：以实施时的最新冲突基线为准，逐个核对 20 个实测冲突文件，确认每个都被本 change 的接缝方案、安全收口清单或基线决策覆盖；未覆盖者（当前预期为 6 个文档 `.mdx` 冲突，属一般合并）显式记入 Non-Goals。已落地：机械复核 `scripts/conflict-baseline.txt` 20 行——`seam` 8 / `keep-deletion` 5 / `merge-docs` 6 / `keep-fork` 1，无未识别处置，8 个 seam 文件路径均在本 change 文档中被点名；复核并入 `scripts/check_change_deltas.py`（`tests/test_change_delta_check.py` 覆盖「未识别处置」与「seam 文件未被本 change 点名」两种漏检）；6 个 `.mdx` 已记入 `design.md` Non-Goals 与 `proposal.md` 延后项（§10.2 的计数此前误记为 seam 10，已更正为 8）
- [x] 10.7 明确延后项与后续 change 入口：用户级资源授权（`membership_resource_grants`）、Agent 元数据单一事实源（outbox）、审批/配额完整生命周期、租户删除闭环、scheduler 触发重验后开放、evolution 后台线程身份补全、`channel_instances.py` 搬迁式机制化（带触发条件）。已落地：`evidence.md` §10.3 逐项记录现状与触发条件
- [x] 10.8 更新 `doc/权限管控体系.html` 中「平台管理员 all 由 `users.is_platform_admin` 派生」的过时口径为以 `user_platform_roles` 绑定为唯一事实源（与审计报告 F10 一致）
