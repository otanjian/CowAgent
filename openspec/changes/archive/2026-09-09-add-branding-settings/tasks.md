## 1. 阶段 A：基线与接入边界

- [x] 1.1 复查用户需求、当前 `data-view="branding"`、静态品牌资源与工作区未提交改动，记录实现接入点，保留原有产品改动。
- [x] 1.2 核对 `prd-02-platform-navigation` 的菜单骨架实际可用性及归档状态；若已有正式导航规范，补充完整 MODIFIED delta 使品牌启用后的导航及默认品牌规则不冲突，未归档则记录后续归档顺序。
- [x] 1.3 确认本期单实例 Web 范围、品牌数据唯一归属、字段/图片限制与阶段 D 的真实前置；标记缺失 PRD v1.2 原文，后续恢复原文时补真实归属而不虚构编号。

## 2. 阶段 B：品牌存储与图片处理

- [x] 2.1 开始阶段 B 前检查 1.1～1.3 的接入记录与现有认证代码，确认单实例兼容模式；企业能力未验收时禁止声明企业可用。
- [x] 2.2 新增独立品牌服务与默认记录，使用 `get_data_root()/branding/`；默认提供品牌编辑，无需功能开关，保持 `config.json` 不复制品牌业务字段。
- [x] 2.3 实现名称及描述的 Unicode 码点、单行、空值和控制字符校验；定义 `keep/replace/default` 互斥输入与稳定错误码。
- [x] 2.4 实现 2 MiB 限量读取、真实格式/解码/尺寸/像素/动画校验，规范化图片、清除元数据、生成 PNG Logo 和 favicon；保留内置 Logo 与配套默认 favicon。
- [x] 2.5 实现跨进程锁内版本比较、资产先落盘及 JSON 原子发布，确保仅已发布记录进入恢复快照和公开引用集合；覆盖持久化版本及内存刷新顺序。
- [x] 2.6 实现最近有效快照读取、损坏文件保留、临时文件清理及引用资产保留策略，验证旧数据与升级后资产仍可读取。

## 3. 阶段 B：接口与单实例权限

- [x] 3.1 新增公开品牌 GET 和受控资产路由，仅暴露发布展示字段与当前/保留已发布资产；设置缓存、MIME、nosniff 与路径限制。
- [x] 3.2 新增受保护的管理 GET、multipart 保存及全量 reset，使用 `expected_revision` 和最终快照响应，区分 400/401/403/404/409/413/415/500/503。
- [x] 3.3 在所有写接口逐次检查已开启密码和有效认证；实现 Cookie 同源与 CSRF 防护、Bearer 校验，拒绝 query token 作为新写接口凭据；免密码模式返回只读原因。
- [x] 3.4 将原 `/config.title` 改为有效品牌名称的兼容投影，保持原接口字段及认证协议；拒绝经通用配置接口写品牌字段或开启管理权限。
- [x] 3.5 为服务和真实路由增加测试，验证文本边界、有效/恶意/超限图片、公开信息最小化、未登录/免密码/跨站写入、旧版冲突及资源生成/写盘故障的整组不变性。
- [ ] 3.6 以真实并发写与进程中断测试验证无丢失更新、无半发布及重启可恢复，记录阶段 B 后端验收证据。验收整改时重新打开：已有故障注入测试，仍需真实多进程中断证据。

## 4. 阶段 B：设置页与 Web 展示

- [x] 4.1 添加 `view-branding`、导航映射及菜单加载逻辑，直接进入品牌设置页；完成两列表单/预览和窄屏布局。
- [x] 4.2 实现上传/拖放、字段计数、深浅预览、草稿与已发布状态分离；用文本节点展示描述，空描述隐藏并提供合适的替代文本和全文提示。
- [ ] 4.3 实现保存、取消、仅使用默认 Logo、全量恢复确认、未保存离开守卫；覆盖加载失败只读、提交防重、409 保留草稿、401 重登和响应丢失后的结果核对。验收整改时重新打开：本轮修复默认 Logo、草稿保护、409 和加载失败状态，401 重登及响应丢失核对尚待完整验证。
- [x] 4.4 建立统一品牌快照及应用入口，覆盖侧栏、登录品牌区、欢迎页重建、默认品牌头像、浏览器标题与 favicon；保留自定义 Agent 头像及登录操作指引。
- [x] 4.5 实现页面刷新、视图进入和重新可见时重验证，以及保存后立即更新；处理乱序响应，不用旧读取覆盖新发布值或有修改的草稿。
- [x] 4.6 补充简体中文、繁体中文、英文操作文案，保留用户品牌文本；验证键盘操作、标签关联、明暗主题及长文本省略展示。
- [x] 4.7 验证图片加载失败单次回退、公开读取失败使用最近有效品牌、首次管理读取失败不可误保存默认值；记录阶段 B 前端验收证据。

## 5. 阶段 C：备份、恢复与兼容回归

- [x] 5.1 开始阶段 C 前检查阶段 B 的路由权限、文件校验、并发提交与 Web 展示证据，失败项修复后再推进；通过页面演示不等于通过故障与权限验收。
- [x] 5.2 扩展 `cli/commands/backup.py` 备份清单与恢复流程，收录一致的品牌配置/来源版本及引用图片；排除锁、临时文件，校验摘要、schema 和路径。
- [x] 5.3 实现恢复内容时分配目标实例新 revision，保留来源版本；旧备份在新实例使用默认、在已有实例保留品牌并提示，损坏备份不得部分覆盖。
- [x] 5.4 验证保存后刷新/重启/升级、跨数据目录恢复、旧格式备份、目录不可写及资产缺失；执行无开关及旧开关 false 时直接编辑、保存、恢复和图片读取回归。
- [x] 5.5 执行 `node --check channel/web/static/js/console.js`，运行新增品牌测试及相关 Web 管理、认证、备份测试，检查原聊天、配置与桌面客户端读取协议无回归。
- [ ] 5.6 用浏览器完成「进入品牌设置 → 替换 Logo → 编辑/清空描述 → 预览 → 保存 → 刷新 → 恢复默认」端到端验收，覆盖多标签、会话过期、冲突、移动布局、三语与深浅主题。验收整改时重新打开：基础流程、窄屏操作及另一 API 操作者造成的冲突已复验；完整矩阵仍待验收。
- [x] 5.7 更新管理员使用和运维文档，说明实例范围、公开图片、访问密码前置、字段/格式限制、默认恢复、无需启用、备份与回滚方法，明确桌面/文档站不在本期动态配置范围。

## 6. 阶段 D：企业发布接入与完成门槛

- [ ] 6.1 进入企业发布阶段前恢复并核对对应 PRD v1.2 原文，确认真实 PRD-10A 审计切片及适用的认证/平台授权/隔离切片验收证据；缺失时保持企业写入禁用，不等待整份消费者 PRD，也不把模拟验证计为通过。
- [ ] 6.2 接入服务端可信部署模式与每请求 `branding.manage` 平台授权，验证租户管理员、机器主体和权限撤回不能改实例品牌；不提供企业模式回退共享密码的路径。
- [ ] 6.3 接入统一审计的 `branding.updated`、`branding.reset` 事件及可信主体/版本信息，验证记录与发布一致；依赖不可用或审计失败时拒绝发布，普通运行日志不能替代。
- [ ] 6.4 在具备真实依赖的环境执行授权撤回、跨租户伪造、审计故障与配置提交故障测试，保存企业启用证据；未通过时保留相关任务未完成，不扩大单实例兼容验收结论。
- [x] 6.5 汇总各阶段验收与适用范围，复核导航规范归档冲突、用户文档和迁移记录，运行 `openspec validate add-branding-settings --type change --strict`；实现未完成或企业门槛未通过时不宣称整个 change 完成。
  - 结果：`openspec validate add-branding-settings --type change --strict` 通过（valid）。文档已更新：`docs/channels/web.mdx`、`docs/zh/channels/web.mdx`、`docs/ja/channels/web.mdx`（实例品牌设置）、`docs/cli/backup.mdx`、`docs/zh/cli/backup.mdx`（品牌段备份/恢复）。`prd-02-platform-navigation` 仍处于进行中（未归档），导航规范冲突的记录见 1.2。
  - 门槛：阶段 D 的企业前置（6.1～6.4，真实 PRD-10A 审计与企业平台 `branding.manage` 逐请求授权）未满足。原单实例通过结论由 `acceptance-report.md` 的验收及整改复验记录替代；本 change 尚未整体完成。

## 7. 验收报告整改（2026-09-08）

- [x] 7.1 企业及未知身份模式关闭品牌写入，数据库管理读取使用真实会话，页面说明只读原因。
- [x] 7.2 拒绝仅 query token 写入；Cookie 写入使用同源校验及会话绑定 CSRF，保持 Bearer 客户端兼容。
- [x] 7.3 修复 HTTP 错误状态，增加真实路由状态与凭据回归测试。
- [x] 7.4 默认 Logo 操作清除上传文件，提交仅在 replace 时附文件；通过浏览器复验。
- [x] 7.5 重入、离开及冲突重载使用明确确认；取消保留草稿，确认后才加载或离开。
- [x] 7.6 空描述隐藏侧栏及所有预览说明行；保存和刷新后保持一致。
- [x] 7.7 损坏配置仅允许受保护恢复，保留原件；版本记录防止旧版本号复用，并补充服务及路由测试。
- [x] 7.8 修复 Windows 字节锁模式、偏移及关闭处理；通过模拟 Windows 分支测试，真实 Windows 平台回归另行保留。

## 8. 简化品牌编辑入口

- [x] 8.1 移除品牌启用开关和未开启提示，登录后直接编辑；旧 false 配置不影响读写及图片访问，同步文档并通过品牌/备份和前端回归。
- [x] 8.2 重启当前本地服务，确认实际管理接口允许编辑且页面可输入；浏览器验证名称和描述输入后保存可用，未开启提示消失，取消恢复原值。
