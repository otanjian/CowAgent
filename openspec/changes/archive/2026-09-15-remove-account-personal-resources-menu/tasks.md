## 1. 基线与相邻 change 协调

- [ ] 1.1 记录最新工作区改动和账号菜单实现，核对个人页、账号操作、移动面板及通用导航的实际引用，保留进行中的渠道工作台实现。
- [ ] 1.2 按 design D5 核对 `move-personal-menu-to-account` 与 `upgrade-personal-channel-workbench` 的入口条款和主规范同步状态，落实具体协调结果；主规范有变化时重定基本 change 增量，防止最终同时要求显示和移除同一资源分组。
- [ ] 1.3 核对既有身份恢复、离页保护、个人页授权和响应式面板的可复跑验证，建立菜单移除前的账号操作与深链接基线；相关依赖明确后进入实现阶段。

## 2. 菜单结构与关联呈现清理

- [ ] 2.1 删除共享 HTML 的“我的资源”标题、五个个人链接、分组专用检查/重试及个人区域状态节点，整理分隔线和间距，保留身份、账号设置、帮助、退出和版本。
- [ ] 2.2 移除资源分组专属渲染、点击、检查重试及状态同步调用；按引用保留通用身份/租户授权刷新、个人 view 识别、页面注册和 `navigateTo` 的未保存保护，不整段删除个人导航标记区。
- [ ] 2.3 清理无其他引用的资源项/个人区域样式与翻译键，将用途提示更新为三语账号设置文案；保留页标题翻译、个人偏好子菜单、移动面板和账号触摸尺寸。
- [ ] 2.4 适配无菜单个人页的定位：标题和面包屑正确、其他主导航无错误当前项、账号按钮无个人区域标记；返回普通页面后恢复唯一当前项，直接地址和页面焦点保持兼容。

## 3. 行为与浏览器回归

- [ ] 3.1 更新现有账号菜单、个人页和 i18n 快照测试，将五项菜单存在性断言改为移除后的呈现约束；保留独立的个人业务、授权、深链接和通用导航验证。
- [ ] 3.2 验证语言/品牌/身份重绘、权限恢复、能力开关切换及迟到租户结果均不恢复分组；打开菜单不新增个人列表、资源检查专用请求或个人业务轮询，原认证及主导航上下文仍正常。
- [ ] 3.3 使用真实浏览器覆盖 `/chat` 与 `/admin`、普通成员及管理员、classic/split、展开/图标侧栏、移动/低高度窗口、三语与主题；确认菜单缺失且账号资料、安全、偏好、帮助、退出恢复和版本均可操作，保存截图和结果。
- [ ] 3.4 验证键盘/移动焦点、Escape/关闭、顶部菜单互斥、关闭侧栏无残留；覆盖五个个人页面有效深链接、无权拒绝和未保存取消，不将菜单省略当作权限变化。

## 4. 文档与交付

- [ ] 4.1 更新账号菜单使用说明及相关个人页面入口描述，明确只移除菜单、保留页面与数据、不新增替代入口，并记录相邻 change 的协调结果。
- [ ] 4.2 核对无数据库/API/grant/运行开关变更，验证匹配版本的 HTML/JS/CSS/i18n 发布及仅回退本菜单差异的恢复方式；不回退并行渠道工作台成果。
- [ ] 4.3 全部实施和回归通过后运行 OpenSpec 严格校验及 `scripts/check_change_deltas.py` 的 `check_deltas`，同步主规范并归档后使用 `check_applied` 复核。脚本 CLI 默认的历史上游冲突清单不属于本菜单 change 的覆盖范围，未实施任务不能因文档生成而勾选。
