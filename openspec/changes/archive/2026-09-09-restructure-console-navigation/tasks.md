## 1. PRD-02 后续阶段 0：基线与依赖

- [x] 1.1 核对当前源码与评审报告中的 22 项菜单、17 个页面及 5 个占位入口，记录实施版本、内部跳转和相邻变更差异，形成脱敏的 `evidence/navigation-baseline.md`。
- [x] 1.2 建立菜单→页面→读取/写入接口→权限/作用域→运行模式→开放状态矩阵，分别记录 legacy 与 database；核对 `/auth/me`、`/auth/context` 和真实消费者关闭状态。
- [x] 1.3 建立配置字段来源清单，覆盖模型、全局默认、单体覆盖、执行策略、共享访问密码、界面/运行语言、通知、品牌与偏好，确认现有保存及版本合同。
- [x] 1.4 核对 design.md 的跨 change 衔接，记录 PRD v1.2 原文缺失或恢复后的条款映射；整理身份摘要、已开放读取、账号、外观、历史和待办的依赖证据，不变更其他 change 任务勾选。
- [x] 1.5 按 compatibility.md 逐 requirement 确认保留/替代范围，补充已交付全局账号/身份审计的宿主与页签映射；盘点全部“跳转后动作”、业务启动入口、草稿适配器及账号/成员/租户/初始化写入口，记录新租户约束与现有零租户平台分支的差异。

## 2. PRD-02 阶段 A：消除无反馈入口与可访问性问题

- [x] 2.1 移除五个无效占位实现，保留基线已交付身份审计等业务入口，建立已知未开放目标的说明与返回处理；在系统设置真实页面接通前保留其未开放状态。
- [x] 2.2 统一菜单和页标题的新名称及三语文案，保留旧内部 ID 兼容；区分返回对话、新建对话、智能体使用/管理、待办/定时任务。
- [x] 2.3 修复关闭分组仍可 Tab 聚焦的问题，统一箭头、缩进、展开状态与焦点恢复；保留桌面图标模式和移动抽屉。
- [x] 2.4 调整首页 AI 建议文案与填入草稿行为的一致性，明确指令帮助不会自动发送；先不补建独立工作文件页。
- [ ] 2.5 验证 A 阶段的无反馈入口、隐藏焦点、当前项、旧会话行为与三语标签，记录可独立交付结果，作为 B 阶段前置。

## 3. PRD-02 阶段 B1：统一登记与权限可用性

- [ ] 3.1 在开始 B1 前确认 A 阶段结果，以及身份摘要、受限改密和所用消费者的真实依赖证据；未就绪时继续独立布局工作但不开放对应 database 能力。
- [x] 3.2 在接入登记前实现 `web_navigation_mode=classic|split` 的合法值、默认 classic 及页面响应安全注入；定义 classic 单侧栏与 split 区域切换共用同一页面和配置主位置，不恢复旧综合 config 编辑器。
- [ ] 3.3 建立单一导航登记，覆盖区域、分组、名称、地址、合法页签、view 适配、逐页签范围/能力、旧别名及页面生命周期；校对 22 项及已交付补充入口，无重复主入口。
- [x] 3.4 根据身份服务及消费者状态补齐 `console_pages` 有限页面/页签和动作投影，保持旧字段兼容；测试智能体列表/全局默认、租户成员/平台账号、read/create/update/execute 分离、未知动作拒绝、默认获准子页和禁止预加载，不新增权限 ID 或持久授权。
- [ ] 3.5 实现 database/legacy 可用性适配，统一侧栏、直接路由、首页、页内管理入口和初始化请求的判断；无有效租户不调用 `/auth/context`，保证未知、无权限、未开放及读取失败状态可区分。
- [ ] 3.6 接入能力刷新与强制失效处理，覆盖页面重新激活、403/401/503、强制改密、无租户受限恢复、一/多租户和 A→B→A；撤权不能被离页取消阻止，停用受影响连接/轮询，不把草稿展示到新身份。
- [x] 3.7 在既有身份服务的成员/账号创建、绑定、初始化与恢复流程落实显式租户及 User/Membership/角色/审计原子提交；无归属启用账号必须被拒绝，保留原管理资格、绑定已有凭据保护和多租户语义。
- [x] 3.8 为成员停用/解绑、租户停用、账号恢复及其他受影响写入口统一增加事务内有效租户连续性校验，覆盖全部受影响账号、最后管理员保护、并发及错误脱敏；表单说明先合法补分配或按平台权限停用账号，不自动全局停用/跨租户绑定。
- [x] 3.9 对正常控制台及受保护管理/业务 API 增加无有效归属的服务端限制，保留最小本人/改密/退出/恢复访问；替换旧无租户平台直达分支并更新对应测试，不改变平台接口的授权资格或消费者关闭状态。
- [ ] 3.10 通过既有维护工具提供只读归属预检、显式指定租户的补分配与复查，包含全部管理员无租户时的受审计修复；禁止默认分配、批量提权及业务 owner 迁移，记录对应版本和权限前提。

## 4. PRD-02 阶段 B2：路由与页面生命周期

- [ ] 4.1 在开始 B2 前确认导航登记和可用性合同可用；实现 hash 解析、页面/页签白名单、真实 href、无 hash 首页与非法目标恢复，不在地址保存凭据、草稿或绝对路径。
- [ ] 4.2 收口原 `navigateTo` 及二次覆盖，返回可等待的五种导航结果；逐项改写创建表单、新对话、智能体发起、历史恢复及文件等依赖调用，业务指针在提交前暂存，成功且上下文有效后动作仅执行一次。
- [ ] 4.3 统一品牌、身份管理、智能体、配置、待办编辑、知识新增/导入、文档和工作文件的未保存保护，包含同页页签/对象切换，确认后才清理；普通取消保留原状态，强制身份失效不被取消。
- [ ] 4.4 实现浏览器前后退、手动 hash、首次加载和重复点击去重；取消后退时恢复原历史位置并防止 pop/hash 循环，保留适用 beforeunload 提示。
- [ ] 4.5 对页面加载绑定导航序号与身份/租户上下文，处理连续点击、迟到请求、部分加载失败和重试；保证目标加载不被取消导航触发。
- [ ] 4.6 回归旧智能体创建、新会话发起、历史搜索/恢复、文件预览和后台回复；确认返回对话不创建会话，进入管理区不取消独立业务运行。
- [ ] 4.7 将 initApp 拆为壳和页面/消费者初始化，聊天会话、工作区、配置、轮询及知识请求按目标能力延迟启动；验证首次管理深链接、无租户和强制改密均不触发聊天/知识请求，保留已合法启动的后台回复。

## 5. PRD-02 阶段 B3：双区域侧栏与响应式布局

- [ ] 5.1 在开始 B3 前确认 B1/B2 合同和行为验证；由登记生成工作台六个候选页面与管理四分组，按权限隐藏空分组/无可用管理区，给无可用页面用户提供中性恢复状态。
- [ ] 5.2 实现区域切换、当前组展开与选中项滚动可见，统一当前项非颜色标记和可访问名称；保留底部账号区与独立滚动。
- [ ] 5.3 调整顶栏为区域/页面上下文、适用智能体与文件动作、租户选择，并标注平台操作范围；保留原主题、字号、配色与三语偏好。
- [ ] 5.4 验证桌面完整/图标模式、低高度及移动抽屉，确保长名称、大字号和最后一个菜单可达；不通过缩小字体掩盖布局问题。

## 6. PRD-02 阶段 B4：配置职责拆分

- [ ] 6.1 在开始 B4 前复核字段归属清单与当前模式开放状态；将现有模型配置接入模型服务，设置厂商/能力页签及深链接，移除重复主编辑位置。
- [ ] 6.2 将全局智能体默认参数与默认执行策略迁入智能体管理的默认设置，保留单体详情及既有覆盖规则，显示有效值、来源和影响范围。
- [ ] 6.3 将适用部署字段接入真实系统设置的一般/安全页面；legacy 共享访问密码与 database 本人改密分开，未适配 database 配置保持关闭；通过后才登记可用系统设置入口。
- [ ] 6.4 将界面语言、通知、提示音统一到个人偏好组件，保留现有浏览器本地键与同步规则并说明同浏览器账号共用，不新增用户/租户前缀；保留业务指针原隔离，将运行语言留在部署配置位置。
- [ ] 6.5 接通旧 config 默认及已知子操作到新职责位置的兼容导航，保持每个字段唯一主要编辑位置；保留保存错误、冲突版本和未保存处理。
- [ ] 6.6 在隔离配置中比对重组前后字段、默认/覆盖、密钥掩码、个人偏好读取与写入目标，确认未创建配置副本、未改数据归属或新增覆盖机制。

## 7. PRD-02 阶段 B5：账号与局部入口整合

- [ ] 7.1 合并账号入口为个人资料、账号安全、个人偏好、帮助与关于及适用退出，移除新布局顶部重复入口；保留退出未确认/重试、认证变化及强制改密行为。
- [ ] 7.2 让租户切换调用共用名称选择器，一租户显示名称、多租户选择、零租户进入待分配恢复；复用主动切换未保存保护、一次性 switch_tenant、资格校验与整页刷新，补分配后从本人接口重验，移除数字输入框分支。
- [ ] 7.3 实现当前品牌产品信息与明确的更新日志链接，保留真实版本及上游来源；接通首页功能跳转与 AI 草稿两类动作，并按可用性处理知识和当前对话文件入口。
- [ ] 7.4 将我的待办范围与互斥状态筛选分开，使用既有服务筛选语义及分页，保持本人未完成徽标；更新智能体设置/能力/核心文件、记忆进化记录和知识文档/图谱的名称与范围说明。

## 8. PRD-02 阶段 C1：集成与验收

- [ ] 8.1 在开始 C1 前检查 B1～B5 及依赖切片证据，建立六份规范 requirement→行为用例→代码版本→结果的验收表，区分模拟界面测试、真实接口验证和未开放消费者。
- [ ] 8.2 自动验证全部菜单迁移与页面/页签地址，覆盖复制/刷新、旧别名、前后退取消、非法目标、无权限、未开放、加载失败及重试，无空白或无反馈导航。
- [ ] 8.3 使用隔离身份库验证普通成员、只读名录成员、零权限成员、租户管理员、平台管理员与无目标 Membership 的入口和按钮矩阵，确认实际 API 独立授权，未开放消费者保持关闭。
- [ ] 8.4 验证品牌、成员、智能体、配置、待办、知识和文件的取消/确认、同页对象切换、冲突、快速导航及 A→B→A；取消/拒绝/被替代不创建会话或打开表单，当前401/撤权强制恢复且草稿不跨身份，旧401不使新账号退出。
- [ ] 8.5 浏览器验证 1440×900、1280×720、390×844 及 320px 宽度，覆盖三语、深浅主题、大字号、图标模式、Tab/Escape、账号弹层与当前项可见性。
- [ ] 8.6 运行适用的现有历史、智能体、外观、账号、品牌、待办、身份与工作文件回归，菜单断言更新保留原业务保护断言；按当前实际开放能力完成五个找入口任务，记录路径与结果。
- [ ] 8.7 使用隔离身份库验证缺租户创建回滚、最后有效归属拒绝、两租户并发移除、租户停用影响普通成员、离职停用/恢复、全部平台管理员缺归属修复、运行期失效及直接 API 绕过被拒绝；复查存量补分配，保留原多租户隔离和管理员连续性证据。
- [ ] 8.8 记录首次管理深链接的网络调用断言、混合范围页签的拒绝/不预加载、同浏览器切换账号后外观保留及业务指针隔离；验证两种布局均只有一个配置主编辑位置。

## 9. PRD-02 阶段 C2：开关、迁移与文档

- [ ] 9.1 复验 B1 已实现开关的合法值、默认值及安全注入，两种布局共享能力过滤、占位修复、路由、键盘和成员租户约束；确认开关不改变认证或消费者状态。
- [ ] 9.2 在通过真实依赖验收的模式中演练 classic→split→classic→split，验证已有业务数据、配置、偏好和新地址恢复；记录兼容构建、缓存更新、发布门槛与回退步骤，不切换身份模式或清空数据。
- [ ] 9.3 更新 Web 使用说明、三语相关入口及必要的迁移说明，明确新旧名称、作用范围和延期能力；提交最终验收记录，生产启用仅依据对应部署证据。
- [ ] 9.4 按阶段0已确认的 compatibility.md 再核对归档时正式规范和相邻变更，更新具体冲突条款而非整份覆盖；保留原安全/业务保护并纳入新增租户连续性，保持其他 change 任务状态独立。
