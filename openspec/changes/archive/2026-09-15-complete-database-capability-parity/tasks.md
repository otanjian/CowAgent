> **归档说明（2026-09-15，部分归档）**
>
> 本 change 按“已验收切片”部分归档：已取得实现、迁移与真实授权/拒绝证据的 7 个 capability 增量合并主规范。以下 8 项任务因缺少外部条件（真实微信提供方、真实打包 Desktop 客户端、远程 HTTPS 部署形态）**保持未勾选**，并已整体移交 `complete-desktop-and-scan-real-acceptance`：
>
> | 本 change 任务 | 移交目标 | 移交后的运行期表现 |
> | --- | --- | --- |
> | 7.8、7.10 | 2.1、2.2 | 微信个人执行保持关闭并报告“已保存未连接” |
> | 8.7、8.8、R2 | 3.1、3.2、3.3 | `desktop_tenant_context` 切片保持 `accepted=false`、`awaiting_acceptance` |
> | 11.3 | 4.1 | 未验收类型/版本/scope 仍关闭 |
>
> 同时从本 change 增量中移出（因此不会合并进主规范）：`specs/desktop-tenant-context/**` 与 `specs/channel-scan-onboarding/spec.md` 的 ADDED requirement「微信个人执行按实际类型与作用域独立验收」。移交只改变承接方，未放宽任何判据；详见 `evidence/12-validation-and-archive.md` 与移交后 change 的 `evidence/1-transfer-and-residual.md`。


> 同时从本 change 增量中移出（因此不会合并进主规范）：`specs/desktop-tenant-context/**` 与 `specs/channel-scan-onboarding/spec.md` 的 ADDED requirement「微信个人执行按实际类型与作用域独立验收」。移交只改变承接方，未放宽任何判据；详见 `evidence/12-validation-and-archive.md` 与移交后 change 的 `evidence/1-transfer-and-residual.md`。

## 1. 基线与依赖切片

- [x] 1.1 记录实施时真实代码版本、schema、10 个关闭路由及 Desktop 传输基线，并建立设计 D1 的产品规划 3.1 对照验收表；既有 17 项审计测试仅记为历史证据。
- [x] 1.2 按 2026-09-15 基线登记 `enable-member-personal-console` 已交付的目录、私有 Agent、个人记忆 CRUD、渠道配置及个人工具配置，引用 9.1/9.2/9.6 证据并固定实现版本、接口、共享文件和归档顺序；53/53 不作为个人执行或记忆安全前置通过证明。将记忆补强、承诺微信范围的个人执行及适用审批消费方明确登记为本 change 责任，不复制 schema、服务或已完成开发任务。
- [x] 1.3 建立本人、同租户他人、租户管理员、平台管理员、跨租户、失效成员及无租户的访问矩阵，区分私有内容、公共管理和治理元数据。
- [x] 1.4 记录 owner、凭据、审计、硬配额、执行隔离和适用审批的真实前置证据；明确记忆路径及发布并发待 5.6-5.8 修复，个人执行及适用审批待 7.8-7.10 完成。缺少证据的具体动作或执行切片保持关闭，已交付配置及其他独立切片不退回未实现。

## 2. 接缝与唯一能力登记

- [x] 2.1 在 fork 自有 handler 模块和 `channel/web/route_registry.py` 接入既有路由适配，必要时增加有限替换接缝；拒绝重复 URL 遮蔽，检查原 handler 来源与方法集合，不复制第二份路由表。
- [x] 2.2 建立独立前端功能模块及稳定挂载点，明确 `channel/web/static/js/console.js`、`channel/web/chat.html` 的最小改动；保留上游功能和 `data-fork-fragment` 契约。
- [x] 2.3 将必要迁移和启动检查挂入 `common/startup_hooks.py`；`app.py` 保持转调，database 强制接缝缺失时关闭对应消费者，不运行旧无身份路径。
- [x] 2.4 复用或建立唯一切片开放登记，供路由、消费者投影和页面动作共同使用，分开 read/config/execute、部署启用和连接状态；加入缺失接缝拒绝测试。

## 3. 本人定时任务管理

- [x] 3.1 前置：1.3、1.4 的任务授权切片通过；实现 HTTP、SchedulerTool 与后台共用的真实任务 tenant/owner/Agent 授权服务，个人任务不因使用公共 Agent 改变归属，公共任务单独判定；`self_authorized=True` 不得豁免工具内部授权。
- [x] 3.2 实现先授权后聚合、分页及计数的任务列表，接入运行、启停、更新和删除对象校验；SchedulerTool 的 create/list/get/delete/enable/disable 全部走同一服务，创建时可信身份与 owner 解析失败必须拒绝，不能留下无 owner 任务；拒绝伪造 owner、tenant、任务路径及通知目标。
- [x] 3.3 在既有 TaskStore 接入原子 revision 判断、动作白名单、隐藏/未知字段保留和脱敏审计恢复；HTTP、工具、后台和迁移共用写入协调，工具创建与启用不得绕过配额；不建第二套任务存储，不支持跨进程协调时拒绝多写入者部署。
- [x] 3.4 实现手动运行幂等入队，复用 `agent/tools/scheduler/identity.py` 与 `agent/tools/scheduler/integration.py` 的唯一执行身份和触发重验；覆盖授权上限、停用、配额及每次资源调用的拒绝。
- [x] 3.5 保留 `tests/test_scheduler_web_update.py` 的上游任务编辑、隐藏通知元数据和手动运行行为断言，使用合法身份 fixture 补入真实路由授权，不通过删测试放行。
- [x] 3.6 通过真实临时身份库与 TaskStore 验证双租户多用户、管理员非 owner 拒绝、公共范围、并发编辑、幂等运行、审计失败、运行中删除和执行撤权；通过实际 Agent 工具分发入口覆盖共享公共 Agent 的不同成员、工具所有动作、身份解析异常、跨入口并发和配额拒绝，不能只单测授权辅助函数。
- [x] 3.7 前置：3.6、4.3 及 R1 通过；HTTP、工具、后台和历史任务隔离同时满足授权门槛后开放 5 个定时管理方法，更新 Web 定时页面和读取/配置/运行状态；本人因执行撤权仍可暂停或删除，未完成真实入口不得勾选。

## 4. 历史任务与数据迁移

- [x] 4.1 盘点历史任务及真实身份/会话证据，仅为可证明归属的任务幂等补齐元数据，保留任务 ID、schedule/action 与上游字段。
- [x] 4.2 对无 owner、归属冲突或执行主体未适配的任务隔离停用，提供脱敏待处理信息和明确恢复步骤；移除 database 下缺省 owner 直接执行的路径。
- [x] 4.3 演练迁移中断、重跑、版本恢复和撤权触发，证明不重复任务、不归给当前管理员、不恢复 legacy 执行；保留一致备份证据。

## 5. 记忆浏览与个人记忆衔接

- [x] 5.1 复用已交付的 owner、个人记忆服务及接口；前置：5.6 的原共享服务真实路径修复通过，不以兄弟 change 勾选代替。为旧记忆读接口实现明确 user/private-agent/shared 目标解析和兼容字段，拒绝共享根默认回退。
- [x] 5.2 将列表、正文、搜索、下载、预览、工作区别名与检索接入同一归属判断，覆盖平台文件根及管理员旁路；不复制个人记忆 CRUD。
- [x] 5.3 通过真实身份库和文件/检索链路验证本人、管理员非 owner、跨租户、同账号跨租户、非法 category 及未知条目标识；同时覆盖新兼容接口和已开放 `/api/memory/personal*`，测试条目/中间目录/根目录软链接及检查后替换，不读取他人元数据或正文后再过滤。
- [x] 5.4 前置：5.3、5.6-5.8 通过；开放 2 个记忆 GET 方法和对应浏览入口，保留旧管理深链接作用域；新入口与既有写入接口共同满足路径及索引发布边界，不能只修新接口。
- [x] 5.5 复用已有个人记忆 CRUD，联合验收编辑/删除/清空、索引失败恢复、跨获准 Agent 一致性和旧任务防回写；必须使用 5.6-5.8 修复后的实现版本，不以依赖已结项或顺序用例代替共享服务补强。
- [x] 5.6 在原 `agent/memory/personal.py` 共同文件访问层实现可信 tenant/user 根的真实归属与使用时校验，覆盖枚举、元数据、读取、保存、删除和清空；拒绝跨根链接及路径替换，复用安全访问接缝，不另建 CRUD。
- [x] 5.7 在原共享服务及实际索引/固化发布接缝统一正文、索引、删除/清空的操作版本和 generation 协调；修复 save 释放正文锁后索引可能复活的窗口，最终版本校验与发布不可分离。补齐持久化恢复/屏蔽标记、pending/incomplete 结果和重启过期操作拒绝；仅有进程内协调时明确禁止多写者部署。
- [x] 5.8 用可控并发屏障覆盖保存已落正文但未发布索引时清空、旧索引同步/固化与清空交错、连续编辑及删除后新版本、索引失败和重启恢复；断言清空成功后旧正文/索引均不复活，过期清理也不删新版本，其他用户和共享记忆不受影响。测试通过才解除 5.4、5.5 的阻断。

## 6. 个人项目浏览与受控本机导入

- [x] 6.1 前置：实际 owner/路径边界通过；实现本人项目根内的目录列表、受限父级、相对标识及合法旧绝对路径归一化，移除该已适配路径的整体关闭守卫。
- [x] 6.2 对最终选择重验会话归属、真实路径和当前 tenant/user，覆盖浏览后软链接替换、跨租户旧项目标识及平台根嵌套私有目录。
- [x] 6.3 通过独立接缝实现 Desktop 原生选择后的目录导入，保留 `channel/web/web_channel.py` 中上游 `_import_local_file` 的接口、loopback 和每启动令牌校验，并叠加数据库身份及一次性用途。
- [x] 6.4 加入导入预览、目标冲突、原子配额预占、暂存/发布、取消及失败补偿；排除未授权个人运行数据和跨根软链接，远程后端采用受保护上传而非解释本机路径。
- [x] 6.5 验证合法浏览/选择/导入、路径编码/盘符、网页伪造、单独本机令牌、并发配额、审计失败及中断恢复；确认原目录和已有项目不被静默覆盖。
- [x] 6.6 前置：6.5 通过；开放项目浏览方法和真实 Web/桌面选择器，提供受控导入说明，保留现有个人项目创建、重命名、排序和删除行为。

## 7. 微信扫码实例适配

- [x] 7.1 前置：公共渠道凭据/审计/配额切片通过；建立绑定发起者会话、scope、tenant/owner、provider、目标及用途的 QR 状态机，替换全局 `_qr_state`，定义过期和终态清理。
- [x] 7.2 复用一次性接入授权与实例写入事务，实现微信自动落库、冲突不提前消费及敏感临时结果加密；授权消费、实例/凭据、配额占用、审计、幂等回执原子提交。幂等绑定 actor/AuthSession/tenant/scope/owner/provider/授权句柄/目标/规范化摘要，24 小时内同键同内容只回读已授权的非敏感结果，新键或变更内容不能重放已消费授权。微信兼容字段不回显长期密钥；飞书保留既有发起者一次性领取及表单接入契约。
- [x] 7.3 在 `channel/channel_instances.py` 和微信适配器中按显式实例读取密文、登记能力和连接状态，保留上游类型/默认名称行为，拒绝同应用连接占用和全局配置覆盖。
- [x] 7.4 复用已交付的个人实例、配额、密文 owner、治理停用和绑定挑战服务，将个人 QR 接入同一接口并复验当前契约，禁止复制 schema 或自行绑定任意外部 subject；本任务完成只表示配置/绑定可用。
- [x] 7.5 在所承诺微信适配器中接入已有验证后本人私聊路由，不重建路由模型；验证未绑定、他人、跨租户及群聊均不能进入私有 Agent，成员/凭据/路由撤销和治理停用在后续入站生效，组件通过不代替 7.8 的真实执行验收。
- [x] 7.6 验证多发起者及多租户并发、非 owner 平台管理员查询、失效会话、来源校验、状态过期、配额/审计/连接失败及 worker 状态一致性；覆盖同键同摘要并发只提交一次、响应丢失回读、不同键/参数/会话重放、回读前撤权、授权与回执分别过期、事务失败未消费及无重复连接。通过真实飞书领取到创建入口的回归确认原字段仍可用、不额外披露秘密；R3、R4 未通过不得开放扫码能力。
- [x] 7.7 前置：对应 scope 的 7.6 通过；开放 QR 配置 GET/POST 及 Web/桌面扫码入口，保存与连接状态分开；个人执行及公共实例个人入口须再满足 7.8-7.10，未就绪部分保留关闭原因，不影响已验收公共配置。
- [ ] 7.8 由本 change 对首个承诺支持的微信适配器完成真实扫码、密文落库、连接和收发验收，分别记录提供方/版本/公共或个人 scope/连接形态；个人范围包含本人私聊、他人/群聊拒绝、解绑及治理停用，公共成功不能代替个人验收。缺少外部条件保持未完成，不沿用依赖的“关闭即结项”分支。**部分归档时移交 `complete-desktop-and-scan-real-acceptance` 任务 2.1；本 change 未完成，保持未勾选。**
- [x] 7.9 在承诺的实际 Bridge/工具分发接缝接入既有动作审批服务，覆盖适用动作待批无副作用、批准后同一动作执行、拒绝/撤回/过期/参数改变/重放拒绝及执行前身份资源配额复验；明确非适用动作的服务端策略依据，不以客户端声明或无需审批用例代替真实消费方交付。
- [ ] 7.10 前置：对应类型/scope 的 7.8、适用审批 7.9 及既有隔离/凭据/配额门槛通过；按证据更新 `PERSONAL_RUNTIME_ACCEPTED_TYPES`、`PUBLIC_PERSONAL_INGRESS_TYPES` 及必要的适配范围约束，复用部署总开关与统一运行判定，类型记录不自动打开部署。验证配置已保存但执行关闭、个人实例执行和公共个人入口分别投影，未验收类型/版本/scope 不扩大开放范围。**部分归档时移交 `complete-desktop-and-scan-real-acceptance` 任务 2.2；本 change 未完成，保持未勾选。**

## 8. Desktop 身份与租户上下文

- [x] 8.1 在独立身份状态及请求上下文模块中建立唯一有效 tenant/epoch；`desktop/src/renderer/src/api/client.ts` 通过统一装饰器接入，保留上游业务方法和签名。
- [x] 8.2 按 D8 实现主进程认证/传输 broker、窄化 preload IPC 及系统浏览器授权码加 PKCE S256 协议；新增独立 authorize/token 扩展路由，本地与远程共用协议，修正 `/auth/login` 始终只返回 Cookie 和空兼容 token，Bearer 仅留主进程内存。完成精确后端 origin/loopback 回调约束、用户确认、60 秒单次授权码、原子兑换、在线撤销、通用 httpRelay 隔离和旧客户端拒绝降级；通过 R2 后才能进入 8.7、8.8 的交付验收。
- [x] 8.3 完成强制改密及按目标域区分的租户 gate；租户业务（含个人记忆/渠道）在单/多租户选择就绪前不初始化。完成改密的零租户账号保留 `/auth/me`、账号设置和退出，获权平台管理员保留平台管理且不附加伪造 tenant/Membership；强制改密期间仍仅改密或退出，平台目标不改变业务租户。
- [x] 8.4 为 JSON、FormData、语音、流及重连、预览、下载和本机导入传递有效上下文，不能加头的传输复用受保护资源派生或带头读取，禁止 URL 会话 token。
- [x] 8.5 实现租户切换的请求取消、连接关闭、缓存隔离及 epoch 迟到响应拒绝，旧写操作不换租户自动重发。
- [x] 8.6 复用 Web 权威页面/对象动作及兄弟 change 的个人入口，准确处理401、强制改密、租户失效、403、版本冲突和503，不退回共享密码或假成功。
- [ ] 8.7 使用真实后端及受控 Desktop 客户端验证登录到聊天、附件回读、任务、记忆、项目及个人页完整链路，覆盖两个租户、两个成员、失效身份和重连；增加零租户普通账号、零租户平台管理员及受限改密平台账号矩阵，证明平台入口不被租户 gate 误挡且不能越权进入租户业务。**部分归档时移交 `complete-desktop-and-scan-real-acceptance` 任务 3.2；本 change 未完成，保持未勾选。**
- [ ] 8.8 前置：8.7 通过；由统一开放矩阵更新 Desktop 及兼容 `desktop_enterprise` 投影，清理永久 deferred，不把只完成 LoginGate 计作桌面适配完成。**部分归档时移交 `complete-desktop-and-scan-real-acceptance` 任务 3.3；本 change 未完成，保持未勾选。**

## 9. 路由与界面开放一致性

- [x] 9.1 将定时、记忆、项目和扫码的 Web 页面接入独立模块与权威动作投影；复用个人菜单迁移结果，保持旧公共页作用域及上游交互行为。
- [x] 9.2 逐方法登记 10 个恢复入口的策略、来源、授权与真实 handler，并更新 `scripts/route-baseline.txt` 的可追溯变化，不删除历史项掩盖差异。
- [x] 9.3 用真实 `build_web_app()` 验证 10 个方法的合法正向调用、未登录、非 owner、无资源权及故障路径；直接调用 handler 成功不计作入口验收。
- [x] 9.4 覆盖两种现有导航布局、直接链接、页内入口、无菜单授权、只读动作与真实403/503，确认不预加载拒绝页面或把无权状态当版本未开放。
- [x] 9.5 核对切片启用/关闭、read/config/execute 与实例连接状态，拒绝路由已开但投影永久关闭或投影已开而接口仍503；未通过项保持明确的实施缺口。

## 10. master 更新后合入 rdai 的保全与回归

- [x] 10.1 在隔离干净检出记录源 master、目标 rdai 与 merge-base，识别相对旧基线的路由、方法、签名、任务字段、传输和启动点变化；不把历史冻结哈希当当前版本。
- [x] 10.2 按设计 D10 对共享文件实施逐接缝合并；保全 `agent/memory/conversation_store.py` 的组合 schema、`app.py` 的启动 hook、调度身份、fragment、本地导入及 Desktop 上游功能。新增登记 `agent/memory/personal.py`、`manager.py`、`summarizer.py` 与实际工具发布接缝的路径/版本边界，以及渠道逐类型验收和审批消费接缝，禁止整文件覆盖或改公共签名迁就 fork。
- [x] 10.3 对最终候选运行既有 route coverage、upstream seams、conversation schema、fork fragments、scheduler web update、旧认证不复活及本 change 的真实授权回归；无文本冲突也必须通过。
- [x] 10.4 在隔离检出执行 `scripts/sync-from-master.sh` 取得真实报告，更新 `scripts/conflict-baseline.txt` 的逐文件 `seam:<任务>`/局部 `keep-fork`/`merge-docs` 决策，整文件删除才用 `keep-deletion`；排练中止不能替代最终候选测试。
- [x] 10.5 加入可重复的上游漂移用例：新增 HTTP 方法、新增任务 action 字段、新增 Desktop 传输、记忆索引/固化发布入口及渠道动作分发；证明功能保留且仍经过 rdai 身份/owner、路径、generation 和适用审批边界，未分类变化必须阻止验收。
- [x] 10.6 将上述步骤写入后续每次 master 到 rdai 更新的执行说明，记录最终候选及证据版本；不通过删除基线、放宽断言或复活旧鉴权获得通过。

## 11. 产品规划 3.1 联合验收

- [x] 11.1 依赖兄弟 change 的私有 Agent 生命周期，联测专属助理与成员自建对象的本人配置/启停/删除边界，确认任务不因默认入口复活停用 Agent，也不扩大资源授权。
- [x] 11.2 复用既有个人工具/技能参数及凭据引用，并使用 5.6-5.8 补强后的共享记忆服务，联测 Web/Desktop 本人维护与获权目录，确认公共配置和其他用户内容不变；补强未完成不得以原 CRUD 交付记录代替。
- [ ] 11.3 复用已交付个人渠道、身份挑战与路由服务，使用 7.8-7.10 的实际验收类型联测扫码到个人收发、适用审批、解绑、治理停用及群聊隔离；管理员只获允许的治理元数据，未验收类型仍关闭。**部分归档时移交 `complete-desktop-and-scan-real-acceptance` 任务 4.1；本 change 未完成，保持未勾选。**
- [x] 11.4 对照设计 D1 逐条记录 requirement、实现版本、已交付复用项、本 change 补强项及剩余阻断；依赖 53/53 不替代真实能力证明。记忆补强、承诺的个人执行或适用审批未完成时保留未覆盖项，不宣布全部恢复或归档。
- [x] 11.5 演练分阶段关闭、停止连接/触发、保留 owner 及迁移数据的恢复，证明不会切回 legacy、忽略新归属字段或恢复管理员私有内容旁路。

## 12. 文档与交付

- [x] 12.1 更新实际支持的功能/scope/客户端与微信提供方清单、个人项目导入区别、历史任务隔离处理及部署恢复说明；不把提案状态写成当前版本已具备。
- [x] 12.2 执行 `openspec validate complete-database-capability-parity --strict` 和 `.venv/bin/python scripts/check_change_deltas.py complete-database-capability-parity`，核对增量可应用及当前冲突基线覆盖。
- [x] 12.3（部分归档执行；原任务要求“全部必要依赖和验收完成”，按用户决策改为按已验收切片归档）将已取得验收的切片——`database-scheduler-console`、`database-memory-console`、`scoped-project-browser`、`channel-scan-onboarding` 的配置面与适用审批消费方、`database-runtime-consumers`、`console-navigation-availability`、`fork-upstream-decoupling`——按 requirement 合并主规范并归档本 change，保留兄弟 change 及产品 3.1 边界；未验收的 `desktop-tenant-context` 与「微信个人执行按实际类型与作用域独立验收」移交 `complete-desktop-and-scan-real-acceptance`，由该 change 验收后合并。归档后以 `scripts/check_change_deltas.py complete-database-capability-parity` 的 applied 模式验证主规范与增量一致。
- [x] 12.4（部分交付）交付真实实现、迁移与 master/rdai 合并证据（见 evidence 3/4/5/6/7/8/10/13）；客户端与提供方真实验收（7.8-7.10、8.7-8.8、R2）移交 `complete-desktop-and-scan-real-acceptance` 并保持未勾选，本 change 不宣称该部分已实现，规划产物完成不等于功能实现完成。

## 13. 本轮审查补充验收门槛

以下任务是实现阶段的阻断项，不因规划文档完成而勾选；各切片须同时满足原任务及对应补充门槛。

- [x] R1 定时工具闭环：经真实 Agent 工具分发执行 create/list/get/delete/enable/disable，覆盖同一公共 Agent 下不同用户、跨租户、非 owner 管理员、身份快照失败、配额耗尽、撤权后暂停/删除与跨入口并发；工具、HTTP 和后台共用目标授权、审计及 TaskStore 写入协调，完成后才允许 3.7 开放。
- [ ] R2 Desktop 原生协议闭环：分别通过本地 loopback 与远程 HTTPS 后端完成系统浏览器确认、PKCE 兑换、主进程请求及在线撤销；覆盖强制改密、错误 state/redirect/verifier、60 秒过期、授权码重放、发起会话撤销、兑换响应丢失、旧后端拒绝降级、Web 自报 Desktop 仍只获空 token、非主 frame IPC、任意 URL/认证头覆盖、通用 httpRelay 旁路和携带凭据重定向。令牌不得进入 renderer/浏览器存储/日志/URL，上传、流、预览及下载均验证 broker 路径；未完成不得进入 8.7、8.8 的交付验收。**部分归档时移交 `complete-desktop-and-scan-real-acceptance` 任务 3.1（并因本 change 已改为部分归档，交付验收门槛由该 change 的 3.2/3.3 承接）；本 change 未完成，保持未勾选。**
- [x] R3 飞书兼容边界：确认“不回传长期密钥”仅适用于微信新流程；通过现有飞书发起、领取、表单填充及创建入口验证 app_id/app_secret/scan_ticket 的既有一次性契约、非发起者拒绝和秘密不持久化/不显示。不得因通用规范文字变化要求飞书切换到未实现的服务端托管；该切换另立 change。
- [x] R4 扫码一次性与幂等一致：保留主规范原有全部场景，并分别验证同键同内容结果回读、新键或修改内容重放拒绝、跨用户/会话/作用域/provider 拒绝、回读前撤权、并发仅一次提交、事务失败不消费、24 小时回执及授权分别过期、回执清理不复活授权、回执无秘密和无重复连接。飞书未采用幂等协议时第二次创建仍须拒绝；R3/R4 未通过不得完成 7.7。
- [x] R5 接缝回退与合并契约一致：将主规范“定制逻辑位于稳定接缝而非上游核心文件内”以完整 MODIFIED requirement 保留原场景并明确回退边界；在合并验收中覆盖独立上游、完整 rdai、rdai 强制接缝缺失、仅可选 UI 接缝缺失四种形态。必需接缝由独立启动装配检查，不能以插件开关降级鉴权；将 scheduler tool/store、Desktop main/preload/broker 及认证响应纳入 master 到 rdai 的语义漂移和双侧回归清单。
