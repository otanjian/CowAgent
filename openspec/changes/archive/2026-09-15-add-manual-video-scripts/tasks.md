# 任务

## 1. 脚本

- [x] 1.1 新增 `manual-video-scripts.md`（与手册同目录）：文件头说明用法与维护约定（脚本随 `content.php` 增删视频同步）。
- [x] 1.2 按分组补 37 份脚本，标题格式 `### \`<主题>.<视频>\` · <标题> · <时长>`。
- [x] 1.3 每份脚本含「开拍前准备 / 操作序列 / 旁白 / 录制注意」四段；操作序列取自上一版手册已审计的界面步骤，合并且不再单独成片的主题按 id 映射并入。
- [x] 1.4 旁白口径与手册、能力文档保持一致；不引入未经核对的新说法。

## 2. 校验

- [x] 2.1 `tools/check-manual.php`：解析脚本文件的 `主题.视频` 标识。
- [x] 2.2 断言 `content.php` 的每个视频都有脚本小节，且无多余小节。
- [x] 2.3 断言脚本小节的标题与时长与结构文件 / 语言包一致。
- [x] 2.4 缺项、多项或漂移时以非零状态退出并列出问题项。

## 3. 文档与验收

- [x] 3.1 `webhelp/README.md`：说明脚本文件的位置、用法与校验命令。
- [x] 3.2 验收：`php tools/check-manual.php` 通过；故意删掉一份脚本、改错一个时长、加一个多余小节时，校验分别失败并指名问题项。
- [x] 3.3 验收：站点页面渲染结果不因本 change 变化（手册页仍 200、无脚本内容泄漏）。
- [x] 3.4 `openspec validate add-manual-video-scripts --strict` 通过后归档。
