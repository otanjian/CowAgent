## 1. P0 — 确认接入点

- [x] 1.1 核对六项能力、现有账号/身份测试和在途改动，确认本人读取、改密、顶部语言、租户选择及现有离开提示的接入点；仅记录本次依赖缺口。

## 2. P1 — 最小后端支持

- [x] 2.1 新增单一 GET /auth/me，白名单返回本人全局信息和有效租户中的本人成员摘要，复用登录的有效租户筛选；普通成员可读、无租户可读、受限状态不返回组织详情。
- [x] 2.2 登录/check 返回强制改密状态，共享认证守卫在服务端阻止受限账号访问业务与管理接口，保留必要身份读取、改密和退出。
- [x] 2.3 在现有服务中补齐本人改密原密码验证、事务内密码/审计/会话撤销及 Cookie 来源/CSRF 检查；成功清 Cookie 并要求重新登录，不扩展临时密码生命周期。
- [x] 2.4 修正本次复用的身份响应 HTTP 状态，区分原密码错误、会话失效、受限改密和服务失败，不整改无关业务路由。
- [x] 2.5 用隔离数据库和真实路由验证本人白名单、有效成员、受限访问，以及改密成功/错误原密码/事务失败/并发旧凭据/两会话失效；通过后接通对应前端入口。

## 3. P2 — 六项完整功能与简化切换

前置：2.5 的真实接口验证通过；复用现有组件和账号状态，不新增通用框架。

- [x] 3.1 扩展六项菜单，保留个人资料侧边抽屉、改密弹窗和偏好设置，复用现有样式/关闭/焦点处理，覆盖三语、主题、键盘与短视口。
- [x] 3.2 接通只读资料的全部字段及全局/成员区分、空态和重试，复用认证代次及一个资料请求序号，防止退出/换账号后旧资料恢复。
- [x] 3.3 接通普通/强制改密共用表单、单次提交、错误分流和成功重新登录；登录及刷新先处理强制改密，密码输入不持久化。
- [x] 3.4 接通本地主题/语言偏好并与顶部同步，分离实例语言保存；检查 storage 失败提示及个人切换不写实例配置。
- [x] 3.5 关于行展示品牌/版本并打开原更新记录；退出保留当前会话语义、既有离开检查和失败重试，补本次密码表单离开检查；按 legacy 模式显示适用菜单。
- [x] 3.6 两个租户入口共用本人列表，通过一次性目标参数导航刷新；新页面验证后才提交选择，取消保留旧上下文，覆盖零/单/多租户和失效目标。
- [x] 3.7 仅为现有 Agent/会话选择标识加 user/tenant storage 分区，身份与有效租户确认后才恢复并启动相关请求；旧未分区值不带入 database，legacy 保持，不清理其他标签存储。

## 4. P3 — 针对性验证与交付

前置：P1/P2 功能完成；测试按实际风险合并，不重复验收整个身份系统。

- [x] 4.1 执行相关账号/身份/品牌/历史回归，补菜单字段、改密错误/网络未知、语言无全局写入、旧身份响应及刷新上下文的针对性测试。
- [x] 4.2 在真实浏览器验证六项功能、普通/强制改密、零/单/多租户、切换取消/连续切换/另一标签、三语主题及移动端；检查全部资料字段、版本链接和退出重试实际可用。
- [x] 4.3 验证 database 与两种 legacy 模式、旧选择键兼容、无数据库迁移和无新增开关，保持原运行功能门禁及可用改密回退路径。
- [x] 4.4 更新使用说明和精简验收记录，说明当前浏览器偏好、切租户刷新、改密使其他旧会话失效及归档衔接；运行 OpenSpec 严格校验，仅勾选实际完成的任务。
