# 任务：手册重建为截图与步骤

## 1. 截图采集

- [x] 1.1 以租户 `test15` 的成员账号 `RC001` 登录控制台，确认菜单与页面范围符合 `member` 角色。
- [x] 1.2 采集「发起对话」截图：登录页、对话页、智能体挑选菜单、模型选择菜单、一次真实问答。
- [x] 1.3 采集「多智能体协同对话」截图：新对话菜单（含「多智能体对话」入口）、参与智能体挑选、协同作答结果。
- [x] 1.4 采集「设置本人的知识库」截图：知识库页、新建分类、新建文档、文档入库后的索引与正文。
- [x] 1.5 采集「创建与配置本人的智能体」截图：智能体配置页与详情、创建表单、核心文件。
- [x] 1.6 采集「接入消息渠道」截图：本人渠道页、接入通道表单（含渠道类型与绑定 Agent）。
- [x] 1.7 截图压缩到最大宽 1600px 并归档到 `assets/img/manual/`；渐变背景的登录页转 JPEG。
- [x] 1.8 截图文件名统一为 `<主题>-<序号>-<语义>`（前缀即主题 id，序号即步骤顺序），共 17 张。

## 2. 结构与文案

- [x] 2.1 `content.php`：`manual_parts` 改为「对话与协作」「我的资源」两组。
- [x] 2.2 `content.php`：`manual_topics` 收敛为 5 个主题，`videos` 改为 `steps`（id + shot）。
- [x] 2.3 `lang/zh.php` 与 `lang/en.php`：写 5 个主题的定位与全部步骤的目标、操作要点。
- [x] 2.4 `lang/*.php`：删除 11 个已移除主题与全部视频条目文案。
- [x] 2.5 步骤文案以中文界面为基准，英文侧给出对应的英文表述；不在步骤里写像素位置或「右上角第几个」这类改版即失效的描述。
- [x] 2.6 步骤里的界面路径一律用界面上真实存在的菜单名与按钮名（「对话」「新建对话」「多智能体对话」「智能体」「创建智能体」「核心文件」「概况」「知识库」「新建 / 新建分类 / 新建文档 / 导入文档」「消息渠道」「接入通道」「会话历史」），逐条对照 `channel/web/static/js/i18n/*.js` 与 RC001 可见菜单核对；分组名只作手册自身的信息架构，不当作界面路径。

## 3. 渲染

- [x] 3.1 `bootstrap.php`：`manual_video()` 换成 `manual_step()`（编号 + 目标 + 操作要点 + 截图，`loading="lazy"` 与 `alt`）。
- [x] 3.2 `bootstrap.php`：移除 `manual_faq()`；`manual_nav()` 改为按新分组渲染。
- [x] 3.3 `manual.php`：按步骤结构渲染主题，移除命令速查与「深入阅读」分支；页头补一行「点击截图可查看原图」提示。
- [x] 3.4 `style.css`：新增截图卡片与编号步骤样式，移除视频占位样式；窄屏下截图与步骤纵向排列。

## 4. 校验与文档

- [x] 4.1 `tools/check-manual.php`：改为校验「主题 → 步骤 → 双语键」对齐，每个步骤有目标与操作要点。
- [x] 4.2 `tools/check-manual.php`：断言每个步骤的 `shot` 在 `assets/` 下真实存在且非空，缺失时报出主题与步骤。
- [x] 4.3 `tools/check-manual.php`：移除录屏脚本相关校验（视频清单与脚本的对应关系）。
- [x] 4.4 删除 `webhelp/manual-video-scripts.md`。
- [x] 4.5 `README.md`：同步手册的形态、位置与校验命令；删除录屏脚本一节。
- [x] 4.6 `tools/check-manual.php` 追加断言：同一张截图被两个步骤引用、截图文件名前缀与主题不符、截图目录存在未被引用的孤儿文件、两张截图内容完全相同（多半是抓错屏）。

## 5. 验收

- [x] 5.1 验收：`php tools/check-manual.php` 通过；删掉一个步骤的文案、删掉一张截图、把一张截图的路径改错时，校验分别失败并指名问题项。
- [x] 5.2 验收：中文与英文手册页均返回 200，5 个主题、17 个步骤与 17 张截图渲染完整，无键名、无空白、无破图。
- [x] 5.3 验收：手册页不含 `http://` / `https://` / `主机:端口`，也不含指向站外资源的 `src`。
- [x] 5.4 验收：窄屏（390px）下无横向溢出，编号与截图线性排列、截图等比缩放。
- [x] 5.5 验收：以成员账号打开手册，每个主题的操作路径都能在该账号可见的界面上走通；消息渠道主题如实说明部分渠道支持扫码接入，其余需手工填写凭据。
- [x] 5.6 `openspec validate rebuild-manual-as-screenshot-guide --strict` 通过后归档。
