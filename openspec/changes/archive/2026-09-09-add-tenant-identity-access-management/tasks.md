## 1. P0 — 确定首期边界与复用点

- [ ] 1.1 按本轮明确的精简范围登记 PRD v1.2 原文缺口和当前需求依据，核对导航/品牌 change 的接口归属；恢复原文后补 PRD→change 映射，不虚构编号或等待未明确的完整业务改造。<!-- 外部依赖：待 PRD v1.2 原文恢复后核对映射 -->
- [x] 1.2 建立具体路由/方法的 public、identity、platform、tenant-read、disabled 分类清单，明确安全读取响应字段、全部拒绝入口及实际后台启动点。<!-- design.md §6 首期允许入口与延期边界 -->
- [x] 1.3 盘点 Registry/state_dir、shared/base 回退、每 Agent 数据库和个人 owner 字段，确定默认租户/初始管理员及独立身份库和新租户根，输出只读迁移预检。<!-- design.md §Context 代码证据 + §Migration Plan -->
- [x] 1.4 确认真实 PRD-10A 身份审计最小切片范围，以及 08S/09G/10B/执行隔离在延期消费者开放时的门槛；后续运行能力不加入本次实现任务。<!-- design.md §7 最小真实审计与并发 -->

## 2. P1 — 身份存储、认证与最小审计

- [x] 2.1 检查 P0 路由及归属清单，新增少量 auth 模块和 identity.db，建立设计中的身份/组织/会话/Agent 绑定/审计表、外键、唯一约束、对象 version 和数据库迁移版本。
- [x] 2.2 实现维护窗口引导命令，创建默认租户、初始平台管理员及 tenant_admin/member，拒绝通用默认密码；目录/归属不合法时不提交可用配置。
- [x] 2.3 实现随机盐 PBKDF2、临时密码期限、强制改密与平台重置；测量并固化密码工作因子、限流/会话期限，验证临时登录仅可改密退出且改密后撤销旧会话。
- [x] 2.4 实现数据库 AuthSession 和 `/auth/login/check/logout/password`，保持 legacy 字段/接口；Web Cookie、CSRF、来源校验、统一错误和限流，拒绝 database 旧 token、URL 长效凭据与故障回退。
- [x] 2.5 实现固定 X-Tenant-ID 请求上下文和每请求用户/成员/租户/角色校验，个人/平台接口独立授权；复用 RuntimeIdentity，缺失/冲突/越权参数不能选择默认租户。
- [x] 2.6 实现身份变更与 append-only audit_events 同事务提交、脱敏拒绝事件及 `/api/identity/audit` 受限查询；真实验证审计失败回滚、跨租户拒绝及备份恢复，不使用 outbox 或 mock 代替验收。
- [x] 2.7 实现固定权限目录、内置管理员资格、自定义业务权限并集；所有身份写入仅 tenant_admin，事务内统一校验 expected_version 和平台/active 租户最后管理员条件。

## 3. P2 — 四个管理模块与现有存储隔离

- [x] 3.1 检查 P1 认证、审计和管理员保护的真实验证结果，通过后实现平台租户查询/创建/编辑/active 及管理员配置接口；目录先就绪、同库身份事务提交，不增加 provisioning 或邀请状态。
- [x] 3.2 实现平台账号状态/管理员标识管理及近期密码验证；停用租户可直接配置管理员，恢复启用独立校验；验证全局停用影响多个租户的最后管理员约束。
- [x] 3.3 实现当前租户成员查询、create-new/bind-existing、名片/active/角色/单部门/岗位整体更新，禁止改已有全局密码/其他租户资料及重复绑定复活停用成员。
- [x] 3.4 实现自定义角色增改复制删除及固定目录展示，保留内置角色与在用引用保护；验证未知权限、平台字段、跨租户关联和自定义角色提权均被拒绝。
- [x] 3.5 实现部门树增改移动/排序/停用/删除，单部门 nullable 和岗位文本；同事务校验无环、同租户及引用，在用部门先通过普通编辑解除引用，组织变化不产生业务授权。
- [x] 3.6 接通四个 Web view、VIEW_META、面包屑、列表/树和表单，新增一个 identity-admin.js 及公共请求封装，复用三语、主题和移动布局，处理空态/错误/未保存/409 重载。
- [x] 3.7 实现每标签租户选择、sessionStorage、generation 晚响应检查及 tenant/user 缓存键；切换清理页面/请求/订阅，覆盖 A→B→A、标签互不干扰和已经提交的旧租户写入。
- [x] 3.8 为 Registry 增加 identity.db 租户绑定投影及当前租户默认 Agent 过滤，保留全局 ID/配置真值；未绑定、损坏映射、无 Agent 不回全局默认，运行时普通接口不能重绑租户。
- [x] 3.9 扩展 state_dir 的可信租户 shared/base 和私有来源校验，阻止跨租户根互含、路径/软链逃逸及 home/配置兜底；保留同租户现有目录，不进行全量复制或重建索引。
- [x] 3.10 对指定七个业务 GET 接口逐项接入租户、私有 Agent、会话 owner/个人记忆及功能权限校验，先过滤再分页，裁剪目录/配置/预览字段；历史读取不启动运行时，文本查询不调用模型。
- [x] 3.11 在路由分类及 Bridge/scheduler/evolution/ChannelManager 实际 init/start/restart 处关闭未适配消费者、文件/运行控制/品牌写等入口；database Web 使用可用管理/读取页并提示关闭项，legacy 原接口保持。

## 4. P3 — 验收、原地登记与发布

- [x] 4.1 检查 P2 所有允许入口已有对应测试、未分类默认拒绝及后台关闭证据；实施默认租户原地登记、Agent 私有归属、会话 owner 和必要 run.user_id 补齐迁移，版本化重复执行不重复写入。
- [x] 4.2 验证两租户、同账号多成员、同租户两用户、相同 session 不同全局 Agent 的列表/详情/总数隔离，覆盖共享 Agent 内的私有会话、共享资产实际来源和非法目录回退。
- [x] 4.3 验证角色/成员/租户/全局账号撤权立即影响后续请求，普通成员不能身份管理；验证两管理员并发停用、重复创建、旧版本覆盖及审计写入失败的事务行为。
- [x] 4.4 验证部门环路/引用并发、单部门约束、岗位名称不授权、直接绑定不接管密码、临时改密重试及单标签切换语义，并完成四页实际交互检查。
- [x] 4.5 验证数据库模式的 HTTP 直接调用、构造/重启及间接调用均不能触发被禁用的模型、通道、调度、文件、旧流和工具；确认旧模式 Web/Desktop/Channel 接口兼容。
- [x] 4.6 执行维护停机一致备份、迁移中断重试、数据库重启/审计恢复及相容版本回退演练；已有迁移标识后禁止静默启动 legacy，不根据租户数量放宽。
- [x] 4.7 编写精简版本操作/迁移说明与延期能力清单，按全部首期验收结果决定 database 启用；核对 PRD/其他 change 归档冲突并运行 OpenSpec 严格校验，未实现任务不勾选。
