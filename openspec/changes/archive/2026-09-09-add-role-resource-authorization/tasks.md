本清单以2026-09-09复核后的范围为准，全部待实施。运行入口开放另立change，保留实际分发授权验证。

## 1. 资源与授权基础

- [x] 1.1 核对五类资源原存储、稳定键、动作、作用域和实际调用点，记录开放/关闭入口，确认缺少原生ID的来源。
- [x] 1.2 复用schema_migrations新增两张grant关系表及roles.model_defaults_json，落实唯一约束、同租户引用和幂等迁移，复用角色/租户version。
- [x] 1.3 从导航、技能、工具/MCP、模型配置和Agent绑定生成目录投影，区分来源及失效引用，不建独立资源索引库。
- [x] 1.4 登记十三项新增动作，保持原九项及内置默认集合，实现共用并集/上限/空集合规则和批量过滤。
- [x] 1.5 从当前平台身份派生动态all；替普通角色授权仍检查该角色上限，保留有效租户、改密、owner及消费者检查。
- [x] 1.6 用隔离身份库验证多角色、空集合、未知ID、跨租户、全局资源上限、all新增资源与资格撤销、停用资源可维护但不能执行、来源和身份服务故障。

## 2. 角色与平台接口

- [x] 2.1 扩展现有角色列表/创建/更新，统一保存功能、grants和默认模型；新增字段省略保留，显式空值清空，创建省略为空。
- [x] 2.2 原事务内重验身份、目标、上限、引用、版本及内置/在用角色保护，写入授权和审计；复制复用创建，旧接口不能成为旁路。
- [x] 2.3 增加平台目标角色薄层适配及全局资源上限接口，共用服务/版本，不新增平台成员/组织CRUD或虚假Membership。
- [x] 2.4 实现用途明确的资源目录及平台目标投影，assign仅管理员，use按本人授权返回最小信息；过滤后搜索/分页，不泄露配置。
- [x] 2.5 扩展/auth/context的all及逐页签能力，目录/配置/执行分别报告，不增加全量本人资源接口、revision或成员预览接口。
- [x] 2.6 验证旧客户端字段保留、显式清空、并发409、提交前撤权、审计失败回滚、平台目标独立和目录用途越权。

## 3. 角色界面与菜单

- [x] 3.1 现有角色编辑增加五类资源和模型默认值，复用一个草稿及统一保存；搜索/分页保留选择，全选存具体ID。
- [x] 3.2 展示平台自身all及目标普通角色有限授权，当前角色草稿提示缺动作、失效引用和未开放原因，不将其称为成员有效权限。
- [x] 3.3 菜单/页签view接侧栏、hash、首页和页内入口共用可达性，保持原ID，管理菜单不作为业务模型使用条件。
- [x] 3.4 保留成员关联/复制、未保存保护、版本冲突、目标/账号切换及迟到响应处理，沿用三语、窄屏和键盘交互。
- [x] 3.5 浏览器验证跨页保存、全选快照、复制、平台目标切换、菜单直达拒绝、use目录独立于管理菜单和失败反馈。

## 4. 技能工具和Agent接入

- [x] 4.1 技能/工具列表及正文使用独立read和资源过滤，技能维护改为edit/enable，移除agent.read写能力，保持expected_mtime及HTTP错误。
- [x] 4.2 技能接口支持resource_id，name仅兼容唯一解析，歧义拒绝；验证内置/租户同名、显示改名、来源键变更和源文件失效。
- [x] 4.3 技能提示词/动态加载按skill.use与Agent配置相交，空集合不走旧[]转None路径，工具schema按execute过滤。
- [x] 4.4 工具真实分发前验证当前授权，覆盖override/MCP/代理路径，配置保留独立管理范围及凭据保护，不解锁关闭入口。
- [x] 4.5 Agent读/编辑/启停/启动/恢复/委派接独立动作及原绑定/owner，子任务继承可复验调用者，依赖缺失不附赠授权。
- [x] 4.6 首期不缓存跨请求授权，实际模型/工具调用重读；既有实例/schema不携带其他user/tenant授权视图，不沿用撤权结果。
- [x] 4.7 用真实身份库及受控本地工具验证实际装配/分发：空技能、借工具、动态MCP、恢复/排队后撤权、委派提权均拒绝，合法分发正常。

## 5. 模型允许集合及默认值

- [x] 5.1 实现允许模型和能力默认值校验，默认属于本角色use集合及租户上限，不建fixed/priority策略表。
- [x] 5.2 接入显式选择→唯一角色默认→已有合法默认链；默认冲突不隐式排序，无候选提示model_selection_required，显式越权不改投。
- [x] 5.3 会话设置、初始化、Bridge、摘要/子智能体及fallback统一验证允许集合，服务端解析凭据，无授权候选拒绝。
- [x] 5.4 用真实身份库及受控模型替身验证实际目标：默认冲突、单模型合并范围、越权fallback、调用间撤权、跨租户缓存及服务故障，all保留合法显式选择。

## 6. 迁移与交付

- [x] 6.1 生成只读预检及具体授权映射，保留旧读取所需兼容只读角色/关联明确列出，不自动授予新执行/维护权，不复制凭据或改owner。
- [x] 6.2 副本演练维护窗口备份、显式迁移、带守卫版本启动及重复执行，覆盖引用缺失/中断；不增加staged/enforced状态机，不兼容旧构建阻止启动。
- [x] 6.3 回归五类授权、角色事务、成员有效租户、历史owner、导航及legacy边界，验证Web/流/轮询/取消及其他未开放入口保持拒绝。
- [x] 6.4 更新操作及迁移/回退说明、脱敏证据和10份规范映射，说明组件集成与线上开放差别；归档同步主规范，不改其他change任务状态。
