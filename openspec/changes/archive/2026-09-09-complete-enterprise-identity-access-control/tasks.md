## 1. 阶段 A — 当前基线与单一验收表

- [x] 1.1 重新核对 G01～G09 当前代码及审查的 92 项精选基线结果，记录测试版本和适用边界；保留聊天 handler 已有 owner 检查及 Bridge 的 database 运行关闭事实，不将基线通过当作本次新要求完成。
- [x] 1.2 从真实路由表盘点已实现 method、别名、身份域和当前允许/关闭入口，列出实际服务工厂、后台构造/重启及间接入口；作为路由完整性检查输入，不按 GET 或前端菜单推断权限。
- [x] 1.3 建立一份验收表映射四份规范、实现切片、测试/代码版本与结果，记录 PRD 原文缺口及其他 change 的接口/归档归属；同一证据跨任务引用，变更影响结论时才重跑相关检查，延期能力不列为本期实现任务。

## 2. 阶段 A — 请求授权与认证事务

- [x] 2.1 实现小型 `auth/http_policy.py` 方法策略和完整性检查，使实际 `WebChannel.startup()` 与测试共用安装门禁的应用工厂；未知 URL 保持 404，已存在未登记方法不执行，未适配消费者按关闭契约拒绝。
- [x] 2.2 分离 database 与 legacy 认证；统一 Cookie/Bearer 选择，同值只认证一次并按 Cookie 保护、不同值返回 400，旧/URL token 不赋权；Web 登录仅签发 Cookie，JSON token 为空，清除 Web 旧 token 缓存及自动附带逻辑，不新增原生登录端点。
- [x] 2.3 按选定凭据逐请求解析权威 AuthSession、User、Tenant、Membership 和当前角色，个人/平台接口不依赖残留租户头；移除通用管理员放行短路，明确 401/403/503 和即时撤权，finally 清理请求上下文。
- [x] 2.4 统一 Cookie 管理写及 Web 登录的来源/请求格式保护、强制改密访问白名单；只有有效且不带 Cookie 的 Bearer 豁免 Cookie 检查，同值重复凭据仍须来源保护，覆盖受限平台管理员及缺来源无有效 CSRF 证明的拒绝。
- [x] 2.5 修登录、本人改密的密码版本与临时期限检查：密码计算在写锁外，登录签发在 BEGIN IMMEDIATE 同连接重核密码哈希/版本、账号状态与期限；验证登录对重置/停用、并发改密竞态，受限会话不超过临时期限；bootstrap写有效期限并使用allow_weak=False验证首次登录/改密、到期不能激活业务。
- [x] 2.6 为身份写入建立复用的同连接短事务规则：锁内重验当前操作者会话/资格、已验证密码版本、目标 expected_version、同租户引用及管理员连续性，版本条件修改与成功审计共同提交；锁超时/审计故障返回 503 并回滚，不在锁内算密码或调用外部服务。
- [x] 2.7 实现应用生命周期共享的有界账号/可信来源登录限流及 429 重试提示，避免 IdentityService 重建清零；单进程为本期部署基线，未实现共同状态的多进程部署明确拒绝。拒绝审计按有界窗口聚合脱敏计数，验证重建、容量、窗口恢复及故障不会形成无界写入或改变拒绝结果。

## 3. 阶段 A — 现有缺陷修复与关闭边界

- [x] 3.1 为租户资料和身份审计落实权限及平台/租户范围，返回字段白名单；审计先授权再筛选/分页，普通成员不能读审计、空权限角色不能读租户资料，普通响应不含 shared_root、哈希或凭据。
- [x] 3.2 成员查询返回 role_codes，更新区分省略 roles 保留、显式空数组 400 和合法替换；编辑框回显当前角色并仅提交实际角色变化，验证只改姓名/部门/岗位保持原授权。
- [x] 3.3 修正 `cow management register --admin-username` 的规范化精确账号解析、目标租户资格与归属冲突检查；验证管理员排序及重复执行不改变已确认的合法归属，不新增 CLI 账号管理命令。
- [x] 3.4 修 database 首页和 can_chat/不可用提示，按当前已验收能力选择管理或受控读取页；保持 Bridge 等实际消费者关闭，不因现有 handler owner 校验通过启动运行，历史/待办来源跳转不启动 poll/SSE。

## 4. 阶段 B — 复用服务补齐账号管理

对应接口开放前，检查验收表中它所用的 A 认证、事务和审计切片证据。服务和独立界面工作可并行实现；不要求等待无关页面或延期消费者验收。

- [x] 4.1 补平台全局账号列表 API，复用现有用户查询，只增加 q/status/page/page_size 和白名单响应；真实验证平台资格、过滤后总数/分页及普通成员拒绝。
- [x] 4.2 实现全局账号启停/平台管理员标识维护，当前请求提交操作者 recent_password 与目标版本，复用锁外计算和锁内重核，不创建近期验证票据/状态机；检查所有受影响启用租户及平台最后管理员，自我停用/降级须另有已完成改密的有效平台管理员；停用撤销全部会话，恢复不复活旧会话或单独停用成员。
- [x] 4.3 实现平台密码重置服务/API，服务端生成限期临时密码，仅成功响应展示一次，强制改密、全部会话撤销和 actor/target 审计同事务；拒绝管理员重置自身并引导本人改密；验证并发、响应丢失后旧版本409和密码不进入日志/查询结果。
- [x] 4.4 补租户名称、状态及指定有效账号管理员配置的服务闭环；普通表单不输入 shared_root，目录由受控配置生成并验证边界，管理员配置和启用独立提交版本与审计，保留停用租户先补管理员再恢复的操作顺序。
- [x] 4.5 复用成员 q/department/page 与 create-new/bind-existing，仅补 status/role 筛选、同范围去重总数和 role_codes；角色/部门关联成员均复用该列表，角色计数使用同一关系来源。验证精确已有账号绑定不改全局密码/其他租户资料，并发新建冲突不自动转绑定。

## 5. 阶段 B — 九项权限与四管理页

- [x] 5.1 保留现有九个权限 ID，补分组/标签/说明/作用域元数据，将内置角色取全目录改为显式默认集合；普通业务权限仍从有效角色并集取得，管理员资格单独检查，验证目录未来扩展不会自动增授旧角色且既有读取/待办行为不扩大。
- [x] 5.2 新增小型 `/auth/context`，复用权威身份解析，只返回当前租户本人的有效权限、管理资格和当前开放能力；零权限有效成员可读空权限摘要，不返回租户资料、不要求 tenant.info.read、不改变 `/auth/me`，摘要不作为后续请求授权凭证。
- [x] 5.3 在用户页接通平台账号页签与当前租户成员视图，复用现有表单完成账号维护/重置、成员新建/绑定、搜索和筛选分页；密码只在必要表单/一次成功结果中显示，超 100 成员通过真实分页查询可达。
- [x] 5.4 接通租户名称、状态与管理员配置表单，显示密码复核、最后管理员和版本冲突结果；普通成员只按权限进入当前租户只读视图，不请求平台租户列表。
- [x] 5.5 接通角色分组权限、内置只读、复制和关联成员；复制复用读取后 create，关联成员复用 role 筛选，不新增独立关联管理页面，保留在用删除保护且不复制管理员资格或成员绑定。
- [x] 5.6 用现有部门 parent_id/sort_order 构建可展开组织树，接通新建、父级修改/排序与部门成员筛选；复用部门服务和成员列表，回环、跨租户及在用引用拒绝时不显示成功。
- [x] 5.7 在现有管理页面接通审计筛选/分页和权限感知菜单/按钮，统一 401/403/409/503、空态、重试与未保存输入；接口失败不乐观报成功，普通业务角色不因拥有其他权限看见身份审计。
- [x] 5.8 复用账号菜单整页刷新切租户及 user/tenant 存储 helper，补管理表单离页保护和当前页面请求序号；取消不改选择，新页面先验证目标，A→B→A、标签独立及旧写仍在原租户完成符合已有契约，不重建全页 SPA 清理框架。

## 6. 本期验收、恢复与交付

- [x] 6.1 按验收表补齐本次真实后端行为证据：方法策略完整性、代表性认证/身份域分支、危险旧入口、双租户/同租户多用户、角色/账号/成员撤权、密码竞态、最后管理员并发和审计失败回滚；引用已覆盖的实现任务测试，不逐阶段重复全仓回归。
- [x] 6.2 完成四页真实浏览器操作验收：创建/绑定、资料编辑保角色、平台重置、租户管理员、角色复制、组织移动和审计；用代表性的语言/主题/窄屏组合检查交互，验证查询参数与翻页结果、只读按钮及刷新式切换，不做全组合截图矩阵。
- [x] 6.3 演练本期最小身份库迁移及维护窗口备份/中断重试/完整恢复，先验证至少一名已完成改密的有效平台管理员，旧NULL期限bootstrap先在兼容流程改密，未满足即停止升级；保持现有ID/owner/目录/权限，仅迁移必要字段/索引，不预建延期表，不得切legacy或删迁移标识绕过保护。
- [x] 6.4 复核实际冷启动、构造/重启和间接入口保持未适配消费者关闭，legacy 明确部署兼容、database 不失败回退；更新当前聊天 owner 检查与 Bridge 关闭证据，管理/目录整理不开放聊天、文件、调度、外部通道或 Desktop 企业运行。
- [x] 6.5 核对四份规范的验收表和未完成项，更新账号/角色操作、API 兼容变化、单进程限流部署、迁移恢复及明确延期清单；按变更影响运行最后必要回归和 OpenSpec 严格校验，协调原身份/账号等归档冲突，只有本期要求全部验收后才能归档。
