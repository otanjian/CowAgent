## 1. 基线、范围和迁移预演

- [x] 1.1 核对当前主规范、相关归档及未提交代码，记录本 change 替代的入口/流程决策与保留的数据隔离成果；确认迁移号与共享文件无冲突。
- [x] 1.2 盘点正式和旧个人页面/API/菜单 grant、默认指针、资源 owner/scope、来源保护、旧开关及客户端使用情况，生成只读迁移预演报告。
- [x] 1.3 建立双租户、双普通用户、本人管理员、非 owner 管理员、平台管理员及自定义角色的功能和数据范围矩阵。
- [x] 1.4 进入阶段 2 前确认凭据、审计、配额、实际路径与记忆版本等依赖切片证据，单列提供方/运行/原生 Desktop 未通过范围，不能用归档状态代替验收。

## 2. 统一对象授权与存储迁移

- [x] 2.1 在统一授权层实现智能体、记忆和连接的查询/操作范围，列表、搜索、总数、分页、详情及写入使用相同归属判定，owner 检查先于管理员例外。
- [x] 2.2 区分正式页面资格、本人对象操作与公共配置管理资格；组织与权限管理接口继续拒绝普通用户，公共编辑 grant 不单独赋予管理资格。证据：`evidence/2-2-qualification-split.md`（含 §6 收口：投影与读取接口原按功能权限放行，已改为按管理资格拒绝，并补真实服务验证）
- [x] 2.3 增加用户默认独立版本迁移；保存现有合法默认、来源和 owner，修复非法租户默认指针时保留私有归属及审计。
- [x] 2.4 实现旧个人菜单到正式菜单的事务化幂等映射、记录和权限摘要更新，保护自定义授权、主动撤权及工具/技能多对一映射。
- [x] 2.5 对新旧接口验证跨用户/跨租户拒绝、不可见统计、伪造归属、提交时撤权、版本冲突、迁移中断重试及审计失败回滚；通过后进入阶段 3。

## 3. 共用控制台与我的资源菜单删除

- [x] 3.1 移除控制台入口的管理员专属准入，改为可信正式页面资格，保留强制改密、零租户恢复、组织与权限及平台运维边界。
- [x] 3.2 将普通用户接入现有 agents/memory/channels/skills/config 页面，共用组件与正式 API，空列表有创建资格时仍可进入；不加载独立个人表单或个人/租户页签。证据：`evidence/3-2-legacy-personal-acceptance-gap.md`（四个旧个人页面验收文件已改写为正式页面契约，含 legacy 菜单 grant 映射用例）
- [x] 3.3 从聊天页、控制台及其共享桌面/移动账号菜单删除“我的资源”标题与“我的智能体、我的渠道、我的记忆、我的工具、我的技能”五项，清理专属检查/重试、占位和多余分隔线。
- [x] 3.4 保留身份、个人资料、账号安全、个人偏好、帮助与关于、退出和品牌版本；同步触发器账号设置提示、三语文案、键盘焦点、移动弹层及低高度滚动。
- [x] 3.5 改造概览数据源和卡片动作，普通用户只取得本人可见统计，成员数及公共运行详情不下发，失败不伪装零值。证据：`evidence/3-5-overview-by-scope.md`
- [x] 3.6 验证真实浏览器双角色共用页面、直达/返回/离页取消、空态/故障、刷新及账号/租户切换；旧迟到投影不能恢复“我的资源”，账号菜单不再触发五类业务预加载。证据：`evidence/3-6-browser-dual-role-acceptance.md`（真实浏览器只读验收双角色；顺带修掉 i18n 快照漂移 55 键×3 语与空态不分范围文案；直达/返回/离页取消与账号/租户切换未在现场造态）

## 4. 智能体统一生命周期和用户默认

- [x] 4.1 将管理与私有创建合入共用生命周期，统一字段、来源隔离、工作区、归属、配额预占、幂等、审计和失败补偿；普通用户创建租户首个对象仍归本人私有。证据：`evidence/4-1-unified-agent-creation.md`
- [x] 4.2 分离管理集合与聊天使用集合；管理列表包含停用对象并排除他人私有，聊天保留已获授权共享对象。
- [x] 4.3 统一编辑、配置、调试、启停、删除及来源/引用保护，管理员 owner 与普通 owner 的同类对象规则一致；旧个人入口只委托共用服务。证据：`evidence/4-3-unified-agent-lifecycle.md`
- [x] 4.4 在正式智能体接口增加 set_user_default 动作，固定当前 tenant/user、校验可管理且可使用候选、独立版本和原子审计；详情“设为默认”对所有用户使用该动作。
- [x] 4.5 将租户默认放在明确命名的管理配置，旧 set_default 仍为受管理资格保护的租户动作；新旧租户动作均拒绝私有目标，清理会隐式转共享的默认校正路径。
- [x] 4.6 实现系统供应只初始化空偏好、与用户选择竞争保护，默认解析过滤合法候选并返回生效目标和回落来源；删除清理默认引用，保留停用偏好。证据：`evidence/4-6-default-initialisation-and-source.md`
- [x] 4.7 验证创建/编辑/启停/删除双角色一致、来源保护、引用冲突、模板依赖拒绝及恢复；验证双用户双租户默认、同值重试、并发设置、供应竞态、旧客户端兼容。证据：`evidence/4-7-lifecycle-and-defaults-verification.md`（含三条显式记录的差异：租户默认无乐观锁——规范未要求、`agent_bindings.agent_id` 主键使“一 Agent 绑两租户”不可表示、仓库无“克隆来源”模板判据，模板判据仅为“变量默认 Agent 不可删”）
- [x] 4.8 验证默认修改只影响未锚定新会话，旧会话、显式目标、后台任务和渠道绑定不改变；无合法候选拒绝且绝不回落他人私有对象。通过后登记智能体/默认切片证据。证据：`evidence/4-8-default-resolution-scope.md`

## 5. 记忆、工具、技能和模型共用页面

- [x] 5.1 在现有记忆管理及正式接口统一列表、正文、编辑、删除和清空，服务端提供合法目标集合；相同类别及只读状态对两种角色一致。**前半**（服务端目标集合 + 个人域可达 + 列表/正文统一）：`evidence/5-1-memory-target-set.md`。**后半已完成**（智能体域写入）：`evidence/5-1b-write-path-and-acceptance.md`。形态为扩展 `memory_browse` 切片（`save`/`delete`/`clear` 声明为 `config`，不新增权限 id）+ 抽出共享写入核（`MemoryService` 继承 `PersonalMemoryService`，只覆盖存储根、地址文法、索引标签、检索 scope、索引库集合五个钩子），故 5.2 的「不写第二套 CRUD」同批落地。核实到的硬约束已处理：磁盘删除不会清索引行，故检索出口（`MemoryManager.search`）同时读取工作域 scope 文件的待屏蔽标签，删除后的正文不会被旧索引行检索回来。**验收更正（提权）**：原本按「目标范围」判定写入（私有智能体→归属人可写）实测为提权——数据库形态下智能体记忆根就是租户共享根，成员经私有范围写入的正文共享范围读到、成员删除会删掉共享智能体正在读的文件（`evidence/5-1b-write-path-design.md` §6 有实测输出）。写面因此改为根目录级更强规则（`_root_is_writable` 要求管理资格，读过、写不过），并把两个方向固化为回归用例。**未交付**：「本人私有智能体记忆可写」需以「每个智能体独立记忆根」为前置，见 `evidence/5-1b-write-path-and-acceptance.md` §4，spec 口径按读面按范围 + 写面限管理资格回写。
- [x] 5.2 收敛原记忆读写到同一领域服务及实际根判定，保留路径防逃逸、版本/generation、索引屏蔽和重试；旧个人接口只作薄适配。**与 5.1 后半同一次改造完成**：写入契约（`_entry_lock` 临界区、`revision_required`/`stale_revision`、`write_text_atomic`、mutation token + generation、`index_state=pending` 重试）就是 `PersonalMemoryService` 那一份，`MemoryService` 只覆盖五个钩子；新增的仅是一个 `scope` 参数（个人域 `user`、智能体域 `shared`）与一个根参数化的屏蔽入口 `pending_index_labels_for_root()`，`pending_index_labels()` 改为调用它，两域共用同一规则。
- [x] 5.3 验证本人用户记忆、本人私有智能体记忆、管理员共享记忆及普通用户共享管理拒绝；覆盖路径替换、并发清空、索引失败/重启恢复和其他目标不受影响。**已覆盖**：`tests/test_memory_console_write.py`（19 passed：管理员共享可写、他人私有与成员对自己私有/对共享的拒绝且文件字节不变、`scope=personal` 拒绝、`revision_required`/`stale_revision`、删除后列表消失、清空不动 dream/evolution、dream/evolution 双角色只读、purge 失败时 tombstone 生效且 `MemoryManager.search` 确实不返回已删正文、purge 成功后不留永久屏蔽、成员写入不可在共享范围被观测）；并发清空/重启恢复的既有覆盖在 `tests/test_personal_memory_scope_protocol.py`（同一份日志实现）。**口径更正**：「本人私有智能体记忆可写」一项验收结果为**拒绝**（提权，见 5.1 条与 `evidence/5-1b-write-path-and-acceptance.md` §3），故原句读作「本人用户记忆可写、本人私有智能体记忆**可读**、管理员共享记忆可写」；前端侧 `tests/test_memory_write_frontend.cjs`（14 passed）锁定页面不广告会被拒绝的动词。
- [x] 5.4 将成员可见的模型服务目录接入正式页面，权限按功能资源判定；把个人参数并入共用资源详情组件，退役独立个人资源面；工具与技能正式详情整合获准本人参数，模型服务共用获授权目录，公共定义/安装/全局启停/地址和凭据修改要求对应管理资格及资源授权。**已完成**：`admin.models` 作用域 `platform`→`tenant`（`auth/service.py:103`；原 `platform` 页根本不是业务入口，`_qualifyAdminConsoleEntry` 会跳过它，成员分支是死代码），新增 `model_catalog_open()`（`:3393`）使仅有 `use`/`read` 的成员也能打开页面（只按 `read` 判会在一张有行的目录上报「已关闭」），`actions.manage` 收敛为平台资格本身（`:3737-3750`，与 `/api/models` + `/config` 门一致）；可达性由默认菜单（`auth/policy.py:518,528`）与 `_migration_27`（`auth/store.py:1431`）保证。个人参数改为落在资源自身的动词上（`POST /api/tools`、`POST /api/skills` 的 `save-personal`/`clear-personal`，`route_registry.py:197-198`），读侧经 `_attach_personal_states`（`web_channel.py:8926`）下发本人状态；详情组件（`console.js:12017-12235`）**不做任何授权判断**，`configure` 受 `member_personal_console` 收窄而 `clear` 刻意不受（已存密钥必须始终可撤销），已存密钥永不回显。独立个人资源面退役：`personal-tools`/`personal-skills` 移出 `PERSONAL_VIEWS`（`personal-console.js:70-90`）、删除 `kind === 'resource'` 分支与 `/api/personal/resources` 路由及 `PersonalResourceHandler`（产品代码 0 命中，仅测试/文档/基线保留，基线记 `REMOVED`：`scripts/route-baseline.txt:349-350`），旧地址转接 `skills`；按本人作用域存储与服务保留。证据：`evidence/5-4b-personal-params-and-model-catalog.md`。**未覆盖**：浏览器级「退役地址转接不重绘」断言此前因「无 Playwright」未跑——**该前提已作废**，已用真实 Chromium 补跑并**通过**：`/admin#view-personal-tools|skills` 与 `/chat#view-personal-*` 均转接至 `#view-skills`、`#view-personal-*` 容器计数 0、对 `/api/personal/resources` 的请求计数 **0**（GET/POST 全计），且目标页消费者的启动次数恰为 1（非空转）；控制组 `/chat#view-chat` 保留自身 hash，证明清 hash 与跨区切换不是启动的普遍行为。**该断言所在的宿主文件 `tests/test_personal_console_browser.cjs` 仍由 3.2 改写中**，当前在 `:296` 更早的陈旧场景（断言 `#view-personal-agents` 会绘制）即中止，故断言本身在宿主内暂不可通过；两处待改：`@:294` 的场景须停止断言 `personal-*` 会绘制（五个 id 现已全部转接），以及从 `/chat` 起始时转接会**跨区**（`_openNavArea('admin')` 推入无 hash 的 `/admin`），故 `hash === '#view-skills'` 只在从 console 区起始时成立。另：`personal.tools`/`personal.skills` **页面 id 仍在签发**（见 8.8）。
- [x] 5.5 验证目录过滤、公共编辑 grant 不替代管理资格、敏感值不回显和实际资源调用重验，不保留独立个人资源配置页；通过后登记阶段 5 证据。**已验收**：目录过滤（`tests/test_tenant_admin_skills_menu.py`、`tests/test_identity_resource_authorization.py`）、公共编辑 grant 不替代管理资格（`tests/test_skill_public_surface_scope.py` 8 项 + `tests/test_skill_public_surface_frontend.cjs` 4 项，已做变异验证：去掉守卫后 4 条安全用例失败、控制用例仍过）、敏感值不回显（个人资源投影与平台掩码路径）、实际资源调用重验（`tests/test_personal_resource_config.py`、`tests/test_execution_authorization_fail_closed.py`）。**另交付：同名技能遮蔽内置技能时不再静默按覆盖顺序取值**——`SkillEntry.shadowed` 记录被替换的定义（`types.py:68`、`loader.py:252-267`），`SkillNameAmbiguous` + `require_unique_name`/`ambiguous_sources`/`ships_with_install`（`manager.py:17,552,563,581`）；被遮蔽定义仍可经 `resource_id` 寻址（`:519-527`）使 400 的补救真的可用；`_raise_if_skill_name_ambiguous`（`web_channel.py:492`）在 `SkillsHandler.POST`（`:9036`）与 `SkillContentHandler` GET/POST（`:9080`,`:9124`）返回 400 + `code: skill_name_ambiguous`。**关键区分**：与内置**逐字节相同的随安装副本**不算歧义（安装器每次启动都会把内置复制进工作区，若判歧义会让每个内置名都 400），只对租户自建定义判冲突。验证：45 通过、前端 9/9、i18n 5/5、定向+回归带 469 通过；六处守卫逐一变异（6/3/1+对照/2/1 条转红后复原）。**原「未验收」两条已随之作废**：「不保留独立个人资源配置页」已成立（见 5.4 条），模型目录成员侧现有载荷可验（`tests/test_member_model_catalog.py`、前端 `test_resource_detail_frontend.cjs` 9/9）。证据：`evidence/5-4-public-surface-authority.md`、`evidence/5-4b-personal-params-and-model-catalog.md`。

## 6. 渠道统一配置与扫码

- [x] 6.1 在现有消息渠道页复用完整渠道卡片、字段与手工/扫码表单，正式租户业务接口统一处理本人连接及有权公共连接，无个人/租户双工作台。证据：`evidence/6-1-shared-channel-surface.md`
- [x] 6.2 实现后端目标候选与创建归属派生，普通用户只能选择本人有效私有目标；管理员共享/公共缺省目标产生公共连接，本人私有目标产生本人连接。证据：`evidence/6-2-channel-target-derivation.md`
- [x] 6.3 将创建、修改、改绑、启用、撤销等写入合入同一实例服务，保持 owner/scope 不因普通编辑转移，统一凭据版本、审计、配额和外部应用占用校验。证据：`evidence/6-3-to-6-6-write-merge-and-scan.md`
- [x] 6.4 将扫码发起/轮询/自动保存/手工提交接入共用服务，授权绑定发起会话、租户、用户、目标及归属；目标/上下文改变拒绝复用旧扫码授权。证据：同上
- [x] 6.5 保持存量无效目标的修复提示和停用/撤销/解绑能力；拒绝空私有目标、共享误绑定和他人目标，失败不留下实例/凭据/授权消费的部分写入。证据：同上
- [x] 6.6 验证双角色同类连接的完整配置一致性、扫码重放和提供方幂等、并发修改、配额/应用竞争、治理限制和旧 API 转接；通过后登记配置证据，不能据此标记真实运行通过。证据：同上（637 passed；真实提供方运行仍属段 7）

## 7. 共用渠道运行与功能开关迁移

- [x] 7.1 开始运行改造前复核凭据、身份绑定、实际提供方适配及适用执行审批/隔离切片；列明可进行真实验收的提供方和资源条件。**已完成**：`evidence/7-1-runtime-preflight.md`。结论是**当前环境无任何提供方可做真实验收**，三个互相独立的阻塞（任一成立即足够）：① 主密钥 `COW_CREDENTIAL_MASTER_KEY` 缺失（既有密文无法解密、新凭据无法加密，`auth/crypto.py`）；② 只读复核发现**仅 feishu/dingtalk/wecom_bot 三个适配器调用 `stamp_external_identity`**，其余 8 个提供方在数据库模式下入站于身份戳处即被拒（`external_channel_unsupported`，`channel/chat_channel.py`），补绑定无济于事；③ 本人连接被总开关与空验收集双重关闭（`personal_channel_runtime=False`、`PERSONAL_RUNTIME_ACCEPTED_TYPES=frozenset()`，`channel/channel_instances.py`）。关键不对称：`scope='tenant'` 的公共连接**不受**③约束，故补齐 ① ② 后 feishu/dingtalk/wecom_bot 的公共连接可做真实入站往返。
- [x] 7.2 合并连接管理、启动合成、热更新和入站分发到共用流程，以实例实际归属、目标及发送者验证访问，不按创建者角色选择运行实现。**已完成**：`evidence/7-2-7-3-runtime-merge-audit.md`。运行面三个文件（`channel/channel_instances.py`、`agent/routing.py`、`channel/chat_channel.py`）检索**零角色标记**，分支键全部是行自身的 `scope`/`owner_user_id`；连接管理/启动合成/热更新/入站各**只有一份实现**（`_reconcile_personal_runtime` 委派给 `reconcile_instance_runtime`，不是第二个写入者）；入站按**存储的**归属 + 目标 + 观测到的发送者三元组校验（14 项），请求体字段无法影响判定，并有两个反例测试钉住；管理员本人连接与成员逐字同条件（探针给出相同 `state`/`reason`）。
- [x] 7.3 合并目录/配置/执行状态来源，迁移旧个人开关与验收记录为共用功能及真实资源条件；显式关闭不被新入口绕过，同条件对管理员本人连接一致。**部分完成**：目录/配置/执行各**单一来源**；`personal_channel_runtime` 的关闭在三个运行 seam 均成立且新入口不可绕过（探针证 `pending`）；`user_private_agent_management`、`personal_memory_write` 在共用写路径强制；管理员本人连接与成员同条件（见 7.2 证据）。**残留（已登记，非凭据阻塞）**：① `personal_channel_onboarding` 关闭时经 `/api/tenant/channels` 新建/重新启用**仍成功**，而同页扫码门读它——一面两个答案；② `member_personal_console` 关闭时私有 Agent 创建（`bind_private_agent_with_quota`）**仍成功**，且投影同时报 `create=True`。两条均为「显式关闭被新入口绕过」。**①②已收口**（`evidence/7-switch-enforcement-closure.md`）：开关读入 `_enforce_personal_instance_policy`（`auth/service.py:7911-7912`）、`bind_private_agent_with_quota`（`:1464`）、`agent/private_agent.py:209`（拒在克隆**之前**，不是克隆后补偿），投影 `create` 与写路径一致（`:3570-3580`，`scope != "self"` 短路以免收窄管理员新建）；新增 `tests/test_personal_switch_entry_point_enforcement.py`（17 项），五处守卫逐一变异（3/2/1/1/2 条转红后复原），五处全撤时 9 失败/8 通过——9 条正是缺陷断言，8 条是对照。相关套件 218 通过。另更正 `evidence/7-1-runtime-preflight.md` 中「该开关经 `personal_channel_ready` 生效」的错误判定（该函数只读声明集与配置收窄）。
- [x] 7.9 **（7.3 第三条门，同类静默失败，规范明文要求）** 共用**编辑**路径 `update_tenant_channel_instance` 在开关关闭时仍允许成员改名/换绑/换密钥，而规范 `openspec/specs/console-navigation-availability/spec.md:202` 明列「**新建/编辑个人渠道实例**」属开启类写入、须以 `capability_disabled` 拒绝；同一句前半段另保证「撤销凭据、解绑、停用、删除」在关闭后**仍须可用**。**旧包装已是正确契约**：`update_personal_channel_instance` 首行即 `require_personal_capability("personal_channel_onboarding")`（`auth/service.py:9410`），故共用路径是唯一与旧契约不一致者——与 R1/R2 同形。**不可一刀切**：该函数同时服务「成员本人自助编辑」（须拒）与「运维/租户管理员治理编辑成员行」（须继续可用，6.1 特意拆出的治理面），且须先确认「解绑」是否经此路径（若经此路径而被一并拒绝，会破坏同一句的保证）。**已收口**（`evidence/7-switch-enforcement-closure.md` §11）：守卫落在 `update_tenant_channel_instance` 的 `if allow_owner:` 分支（所有权证明之后、事务之前——所有权规则仍先作答，且拒绝先于任何字段/版本/凭据写入，无部分写入），复用 `require_personal_capability` 读同页两个开关，拒绝码 `capability_disabled`/403；只作用于成员自助面，operator 面零改动。**两个待解疑问的实测结论**：①「运维/租户管理员编辑成员行」在本函数上**不可达**——`object_scope` 对 `scope='user'` 只认 owner（实测 403 `forbidden`，既有测试已钉住），本函数上真正可达的 operator 写是公共行与管理员本人的本人行，两格都不受新开关收窄，故不存在「一刀切破坏治理面」的情形；②「解绑」**不经**此路径（`unlink_personal_channel_instance` 只被旧个人路由调用；本人行的具名空目标本就被 `personal_agent_required` 拒绝），撤销凭据与停用同样各有其路，三条均有开关关闭期仍成功的用例。新增 11 例（17→28）；变异：删守卫 4 失败/24 通过、挪到所有权证明之前 1 失败/27 通过，复原 28 通过；任务指定 8 文件 309 通过 + 5 subtests，邻接 12 文件 238 通过 + 26 subtests。**登记取舍（未收口，理由见 §11.8）**：管理员本人的本人行编辑不受开关约束（实测 200）；页面投影 `actions.update` 未随开关收窄（全量 JS 无读取点，UI 影响面为零）。
- [~] 7.10 **（7.7 未预期后果；产品决策已定：一致收口，执行已暂停）weixin 类型当前不可用，其扫码面不得再被广告**。**先更正我此前的判定（错误）**：我曾称「扫码接入整条链路无可用载体」，**不成立**——实际对外提供的扫码面是 `console.js` 的 `TENANT_CHANNEL_SCAN_TYPES = {feishu, wecom_bot}`（`:14961-14964`），二者都在 `INBOUND_IDENTITY_STAMPING_TYPES` 内且工作正常，故**扫码能力本身健在**；真正失效的只有 **weixin** 一支（它虽有独立适配器文件 `weixin_scan_adapter.py`，但并非扫码面的全部载体）。**7.7 的创建兜底按 `inbound_identity_admissible`（仅 feishu/dingtalk/wecom_bot）拒绝 weixin（正确）⇒ 该扫码面必然失败 ⇒ 按本 change 自身原则「页面不得广告会被拒绝的动作」必须停止提供**。weixin 扫码面真实可达：路由 `route_registry.py:195` `/api/weixin/qrlogin`（能力 `weixin_scan`/`qr`/`poll`），`weixin_scan_adapter.py:317` 经 `service.create_tenant_channel_instance` 落库（栈见下），`web_channel.py:8001,8050,8088,8109,8125,8231` 六处引用，前端 `weixin_scan_*` 与 QR 面板 `console.js:15767-15801,16157,16273`。**落地范围：仅收口 weixin 一支，不动 feishu/wecom_bot 扫码**；并须保留扫描机制的有价值覆盖（幂等、并发只提交一次、回执重放拒绝、授权保留重试、跨租户隔离）——能用可入册类型承载的改指 feishu/wecom_bot，「weixin 可建」这一错误前提才改为断言拒绝，**不得**把 22 项一律退化成拒绝断言。**待裁决**：weixin 补身份戳（数据齐备：`weixin_message.py:41` 的 `from_user_id`；但属 7.1 审计中延后的逐提供方真实验收项，不宜静默实施）还是整体退役。证据：`evidence/7-10-weixin-scan-consistent-close.md`。**执行状态：随「渠道暂不做验收」一并暂停**——收口本身是行为修复（停止广告必然失败的面），与测试无关，但编辑区与其他 worker 重叠（`route_registry.py`／`console.js`／i18n 快照），故未在暂停期单方面动手；`/api/weixin/qrlogin` 目前仍在册、前端 weixin QR 面板仍在渲染，即**控制台仍会提供一个必然以 400 失败的入口**，待恢复渠道工作时收口或随人工测试结论一并裁决。
- [~] 7.4 **移交人工验证（用户决定：渠道暂不做自动验收，后续人工测试）**。原受阻原因（凭据与前置，非代码）：需与既有密文同一把 `COW_CREDENTIAL_MASTER_KEY`、一个真正装配 `_channel_mgr` 的运行进程（否则 `_runtime_manager()` 返回 None，apply 永远 `pending`，「已连接」不可观测）、以及 feishu/dingtalk/wecom_bot 中任一提供方的真实 `*_app_id`/`*_secret` 等字段与≥2 个真实账号；本人连接另需先把类型加入 `PERSONAL_RUNTIME_ACCEPTED_TYPES` 并在 7.6 与真实证据同批提交。最小清单见 `evidence/7-1-runtime-preflight.md`。逐提供方验证真实公共与本人连接往返、普通 owner 与管理员 owner 功能一致、他人/群聊拒绝、解绑/停用/撤权即时生效和无错误默认回落。
- [~] 7.5 **移交人工验证（用户决定：渠道暂不做自动验收）**。原受阻原因（依赖 7.4 的凭据与运行进程）：凭据轮换失败/连接失败/重启恢复/治理停用需连接先能真正建立；`evidence/7-1-runtime-preflight.md` 另记有两项须先钉住才能区分「已保存」与「已连接」：dingtalk/weixin 的最小凭据集本身未验证（`channel_instances.py` 自认），以及本人连接的类型白名单为空。验证凭据轮换失败、连接失败、服务重启、旧实例恢复和治理停用，准确区分保存、连接与可对话状态；未通过范围保持未验收并记录原因。
- [~] 7.6 **移交人工验证（用户决定：渠道暂不做自动验收）**。原受阻原因（须与真实证据同批）：显式启用要求同一提交内既有真实证据、又把类型加入 `PERSONAL_RUNTIME_ACCEPTED_TYPES`（及共享实例路由的 `PUBLIC_PERSONAL_INGRESS_TYPES`）；未有真实证据前不得启用。取得真实证据后按共用配置显式启用；确认不保留角色专属运行代码，且不把 Web 通过等同原生 Desktop 或其他提供方通过。

- [x] 7.7 **（7.x 复核新发现）渠道类型目录准入性**：目录给出的 8 项里有 5 项**从不** `stamp_external_identity`，其入站永远无法按实例证明发送者，却能通过创建校验、被真正拉起、并被控制台报为 `connected`（可达形态是**公共归属** `scope='tenant'`；本人侧因运行开关关闭暂不可达），第一条消息在 `channel/chat_channel.py` 的身份戳前置检查处被拒，早于绑定查询且不落待绑定列表——操作者只从一行服务端 warning 得知。**根因**：`personal_channel_ready` 的 docstring 自称按「入站可证明发送者」判定，实现只查 `MULTI_INSTANCE_READY`（该集合含义是「可跑多实例」），**过滤存在但键错了事实**。**形态**：新增共用声明 `INBOUND_IDENTITY_STAMPING_TYPES = {feishu, dingtalk, wecom_bot}` + mode-aware 谓词，并入 `personal_channel_ready`（成员面前端无需改动，已按 `spec.ready` 收窄）；**不可直接删条目**——`tenant_channel_types()` 同时承担「新建候选」与「已存在行的字段契约」，删条目会让既有行编辑时渲染空字段，需另加与个人 `ready` 分离的判决字段；写入侧兜底的位置有权衡（放进 `_channel_credential_keys` 会一并挡住既有非打戳行的凭据轮换，是单向门）。裁定与推荐见 `evidence/7-channel-type-admissibility.md`；同时更正 `evidence/3-6-browser-dual-role-acceptance.md` 中把该现象判为「非缺陷」的循环论证（只核对「列表==声明」，未核对声明是否为真）。规范未直接裁定：`tenant-channel-configuration` 只管「提交必被拒」，而该类提交并不被拒；`console-navigation-availability`「运行未开放不得关闭目录」讲的是执行状态而非目录准入性，故收窄目录不违规。**已修复**（`evidence/7-7-channel-type-admissibility-fix.md`）：新增 `INBOUND_IDENTITY_STAMPING_TYPES`（`channel/channel_instances.py:122`）与 mode-aware 谓词 `inbound_identity_admissible`（`:129`），并入 `personal_channel_ready`（新原因 `no_inbound_identity`，`:1298-1304`，置于声明收窄检查**之前**以免报错原因失真），`tenant_channel_types()` 保留全部条目与字段契约、仅新增分离判决 `inbound_admissible`（`:1192-1197`），写入兜底落在**创建分支**（`auth/service.py:8208-8213`），前端共用面收窄一行（`console.js:14908-14911`）。四处守卫均做变异验证（分别 4/1/3/2 条转红后复原）；必改用例两处（任务只点名 499-514，另一处 `:1286-1291` 同因，`set(ready)` 会转空）；`test_tenant_channel_http.py` 增加「跳过不适格类型 + 断言 feishu 确被尝试」的控制，避免空循环蒙过。
- [x] 7.8 **（7.7 残留，同类静默失败）** 已存在的**不适格公共行**仍可被重新启用：写入兜底按权衡选择放在创建分支（放进 `_channel_credential_keys` 会使既有行的凭据轮换成为单向门、并让已含该类型的租户策略在无关编辑时无法保存），因此 `set_tenant_channel_instance_active`（`auth/service.py:8698`）在 `scope=="user"` 段之后、共享 `if active:` 之前仍放行。本人侧重启用已被 `_enforce_personal_instance_policy` 关闭。**已修复**：守卫落在 `auth/service.py:8785-8795`（`if active and row["scope"] != "user":`，复用同一 `inbound_identity_admissible`，不引入第二事实源；拒绝码沿用成员侧同因所用的 `channel_type_not_ready`/403）。**只拒「转 active」**，停用/删除/修复/轮换均不进入该分支——以控制用例钉住既有不适格行仍可停用（五类型各一条，`active→0` 且落一条 `channel.instance.disable` 审计），并以 feishu 行仍可重新启用的反向控制确保不是「一律拒绝」。可达性已核：`rg "UPDATE tenant_channel_instances SET active"` 生产命中仅 `:8821`（=1，即本函数）与 `:9526`（=0，撤销凭据），故无其他重新启用入口需要补。变异：置空守卫 → 1 失败/15 通过（控制用例仍绿），复原 → 16 通过；新增 3 项（13→16）。相关套件 258 通过 + 5 subtests；`test_capability_matrix.py` + `test_object_scope.py` 40 通过。证据：`evidence/7-7-channel-type-admissibility-fix.md` §12。**未覆盖**：无活进程「connected → 停 → 重启用被拒」实测（仍受 `COW_CREDENTIAL_MASTER_KEY` 与真实凭据阻塞，现有证据均为在进程内驱动真实服务/加密/数据库）；直写 SQL 置 `active=1` 可绕过（服务层已审计为无此路径）；平台面 `/api/channels` 仍为 §9 未决项。

## 8. 兼容收口、回归与交付文档

- [x] 8.1 实现旧 personal-* 地址到正式页面的受权转接，保留合法对象和上下文、离页保护及迟到响应隔离；旧个人 API 只转换参数/响应并委托统一业务服务。证据：`evidence/8-1-legacy-personal-address-forward.md`
- [x] 8.2 记录一个兼容发布周期及调用观测，确认正式页面不再使用旧入口；按迁移报告收口旧菜单、开关与个人专属组件，退役不删除业务数据或凭据。**记录半已完**：`evidence/8-2-compat-cycle.md`（可重跑调用观测，11 断言 + 4 变异钉住；旧入口实时清单含 `console.js:1462-1466/1802-1816`、`personal-console.js:70-82/799-805`（由 `chat.html:2821` 装载）、`route_registry.py:200-203`、五开关 `auth/policy.py:564-583` 及其读取点账）。**未覆盖**：无生产调用计数（不存在计数器，只能静态观测 + 断言）。数据面已关闭：升级只删 `role_resource_grants` 菜单行（`_migration_26`），不删业务行与凭据，有断言钉住。**退役半未做 → 见 8.8**。
- [x] 8.8 **（8.2 拆分；用户决定强制收口，残留见 `evidence/8-8-component-retirement.md` §7）组件面退役**：8.2 要求「收口旧菜单、开关与个人专属组件」，但本轮只做了调用观测与清单，**旧组件仍在生产路径上**：`personal-console.js`（3 视图）及其 i18n 由 `chat.html:2821/2803` 装载、薄适配旧端点（`route_registry.py:200-203`）仍注册、后端仍签发五个 `personal.*` 页面（`auth/service.py:117-121`、`173-187`、循环 `:3451`）。退役这些是生产代码改动（含三语 i18n 与路由基线同步），**不属于 8.6 的校验范围**，故单列。注意 `route-baseline.txt` 已因 5.5 变更（`/api/personal/resources` → `REMOVED`），退役前需先重算冻结基线。**已收口（强制）**：`personal-console.js` 已删除、`chat.html` 模块装载行已移除、五个 `personal.*` 页面**签发**已移除（`PERSONAL_CONSOLE_PAGES` 全无）、`scripts/route-baseline.txt` 已重算；`canonical_menu_id`（`auth/service.py:124`）与 `:3324` 的 **grant 规范化未随之退役**（停止签发 ≠ 停止规范化，这是最易误伤既有授权的点）；i18n parity 实测 5/5，cjs 实测 45 失败（基线 44，差 1 即下述残留①）。测试是**改写为新契约而非删除**：`test_channel_workbench_frontend.cjs:405` 断言模块未被加载，`test_personal_address_forward_frontend.cjs:135,139` 断言 `invalidatePersonalViews` 不存在（其隔离已无主体：全仓库 JS 已无 `api/personal` 取数点，而转接先于授权门/离页检查仍由 `:110-125` 覆盖）。**并更正本条原判定**：四个**薄适配旧端点**（`route_registry.py:200-203`）**予以保留**，原「待退役」的框架是错的——`unified-console-access:53` 要求不保留的是「独立业务实现」，而 `:59-61` 场景明文要求兼容期内旧接口提交仍由统一服务处理并保持响应兼容，故退役它们会破坏本 change 自身的 delta 场景。**残留（后续修复）**：① `tests/test_personal_console_frontend.cjs` 仍失败（退役模块的测试文件）；② `tests/test_personal_console_browser.cjs` 有 1 场景未完成（`#account-menu-retry` 未变可见），亦使 5.4 的浏览器断言仍不能在宿主内通过；③ 死 i18n 文件 `i18n/personal-console.js` 仍被 `chat.html:2803` 加载（消费者已删，功能无害，移除须键级更新 i18n 快照）；④ 8.8 自身的完整验收（变异验证、必需套件全跑、基线一致性复核）未做。
- [x] 8.3 完成菜单及偏好迁移幂等、中断补偿、显式关闭保持、只读/运行分片启用与恢复演练；回退不恢复私有读取或默认转共享旁路。**已完成**：新增 `tests/test_console_migration_drill.py`（12 项，驱动真实 `IdentityStore` 执行器跑合成 v24 库：幂等、中断补偿（25 已提交/26 回滚且 25 自带 `ALTER TABLE` 亦回滚）、重试等价全新升级指纹、显式关闭保持、回退不重开私有读取或默认转共享；2 项变异在 `/tmp` 副本验证非空洞）。**未覆盖**：运行分片（需真实凭据与装配 `_channel_mgr` 的进程）。
- [x] 8.4 对授权、HTTP、浏览器、i18n、路由基线和真实渠道执行完成有针对性的回归，比较双角色字段/动作/接口/状态，不以复制实现的测试替代可观察行为验证。**强制收口，真实渠道部分外置**：授权/HTTP/i18n/路由基线/前端基线均已执行（i18n 快照键级重整 74 处、前端基线在 Playwright 条件重算并归类、真实浏览器双角色见 3.6 证据）；**真实渠道运行回归按用户决定改为人工测试**（7.4–7.6），本 change 内不再自动验收。**进行中（原记录）**：前端基线 `evidence/8-4-frontend-baseline.md`（43 条失败已定位并更正归类；其中 i18n 快照漂移为真实缺口，已修，见 `evidence/3-6-browser-dual-role-acceptance.md` §4）；真实浏览器双角色见 3.6 证据。
- [x] 8.7 **（8.4 复核新发现）浏览器验收环境已具备，先前「无 Playwright」的归类作废**：**已完成**。本机 `ms-playwright` 缓存本就有 `chromium_headless_shell-1243`，恰是 Playwright 1.63 所需构建（`playwright install --dry-run chromium` 确认），仅缺驱动包。已装 Python `playwright==1.63.0`（`.venv`）与 Node `playwright@1.63.0`（全局，`NODE_PATH="$(npm root -g)"`），实测两者均可启动 Chromium 153.0.8010.12 并渲染。因此 `evidence/8-4-frontend-baseline.md` 中把 `tests/test_appearance_browser.cjs` 的 1 条失败归因为「本机无 Playwright」**属于环境借口、未经检验**——该解释现已可证伪。重定性与运行方式见 `evidence/8-4b-browser-harness-and-reclassification.md`；另：3.6 的「真实浏览器双角色验收」此前只做到**只读观察**，现在具备可执行条件，其未验收的一半可按需补做。

  **已落实的精确运行条件（三条都有坑，必须按其一来跑）**：
  1. **`NODE_PATH` 必须显式设置**，否则**静默跳过**而非报错：`node -e "require.resolve('playwright')"` 在未设时抛 `MODULE_NOT_FOUND`，而 `tests/test_personal_console_browser.py::_playwright_available` 正是用 `require.resolve` 探测、失败即 `skipTest` → 报告成 `ok (skipped=1)`，**看起来是绿的**。本仓库无 `package.json`/`node_modules`；本会话的 shell 恰好已导出 `NODE_PATH=<nvm 全局 node_modules>`（`~/.zshrc`/`.zprofile`/`.zshenv` 均未设置，故**不可依赖**）。已由本人复核：`env -u NODE_PATH node -e "require.resolve('playwright')"` → 抛错；加回 → 解析成功。**规范命令**：`NODE_PATH="$(npm root -g)" node --test tests/*.cjs`。
  2. **`PLAYWRIGHT_BROWSERS_PATH` 不要设**：默认缓存已含 1.63 所需构建，`test_appearance_browser.cjs` 走系统 Chrome（`channel:'chrome'`）不碰下载；而 `tests/test_personal_console_browser.py` 的 docstring 却让人「设 `PLAYWRIGHT_BROWSERS_PATH` 指向其浏览器缓存」——按它做**会破坏运行**（`conf().get('browsers_download_dir')` 实测返回 `None`，仓库内亦无 `ms-playwright` 目录）。**该 docstring 是误导性指引，属小缺陷，收口时改**。
  3. `test_appearance_browser.cjs` 的失败**不是环境、也不是产品缺陷**，而是**整文件陈旧于产品现状且多点失效**：该测试仅在 `#login-tenant-group` 可见时才登录，而产品改为**凭据登录成功后**才显示该组（`console.js:18584/18484`，且 `:18541` 的 `if (!pwdInput?.value) return false;` 使无密码提交根本不发请求），故**测试从未提交凭据** → 产品不解除 `#app` 的 `hidden`。在 `/tmp` 副本中修好登录取向仍会在 `#account-menu-prefs` 死（夹具只为 `identity==='database'` 设 `login.user`，默认身份是 `public`），再修后场景 1–3 通过、场景 4 又死在 `#account-menu-tenant`（该 id 在 `chat.html` 已不存在，产品改用 `#tenant-menu` + `switch_tenant`）。反证：带凭据登录的探针显示 `#app` 正常解除且**零 pageerror**，故**非产品缺陷**。该文件对干净 `git archive HEAD` 逐字节同样失败，故确为既有，但「既有」≠「因缺 Playwright」。
- [x] 8.9 **（8.7 连带）前端基线「43」不再是有效基线，已在 Playwright 可用条件下重算**：**已完成**。条件齐备实测 `NODE_PATH="$(npm root -g)" node --test tests/*.cjs` → **708 / 664 / 44**（不带 `NODE_PATH` 则 708 / 665 / **43**，差的那条正是浏览器用例被跳过）。失败仍是五组、合计 44：36 `test_session_history_frontend`（沙箱缺 `queueMicrotask`，实测产品调用 1 次、测试白名单 0 次）＋5 `test_sidebar_account_frontend`（**陈旧契约**：断言已退役的 `mode:'legacy'/'unknown'` 与无 `identity_mode` 的 legacy 响应体，而产品 `_normalizeAccountCheck` 只认 `identity_mode==='database'`，后端只发该值且 `identity_mode=legacy` 启动即拒——旧表述「因产品逻辑而失败」属误导，已更正）＋1 `_tmp_repro_modeldefaults`（沙箱缺 `document.querySelector`）＋2 浏览器文件（原先被跳过掩盖者）。**42 条非浏览器失败已对干净 HEAD 复核为既有**（`git archive HEAD` → 101/59/42），即「既有」这次是**实测**而非沿用。**未发现产品缺陷**：专门探了最有安全味的一条（HTTP 503 响应体被当作活会话），产品是 fail-closed（`console.js:669` 在 `!response.ok` 时先抛错再归一化）。旧表及其周边失真表述（计数、"43 条全是环境/沙箱缺口"、过时的 699）已定向替换，新增「更正三」记录条件变化、更正后计数、实测 vs 仅假设对照表与未覆盖项。**注意计数随并发工作移动**：取样时另一 worker 新增的 `tests/test_resource_detail_frontend.cjs`（9 项）使总数 699→708，其首次取样（03:01）因该文件正被写入而多出 3 条失败（47），稳定取样为 44——**数字须与取得条件一起读**。
- [x] 8.10 **（8.7/8.9 连带，真实缺口）浏览器与 cjs 用例不在任何 CI 中运行**——**强制收口：事实已登记，决策外置**。原记录：无 CI workflow 安装 Playwright、也不跑 `node --test`，因此上述陈旧用例（`test_appearance_browser.cjs` 多点失效、`test_personal_console_browser.cjs` 驱动已退役 DOM）**可以无限期不被发现**——不是「跑起来是绿的」，而是「根本没人跑，且缺 `NODE_PATH` 时会伪装成绿」。三条应对：(1) 明确规范命令与 `NODE_PATH`(见 8.7)；(2) 修掉 `tests/test_personal_console_browser.py` 里误导性的 `PLAYWRIGHT_BROWSERS_PATH` 指引；(3) 决定这些用例是修成可跑（`test_appearance_browser.cjs` 三点陈旧、`test_personal_console_browser.cjs` 待 3.2 改写完）还是显式标记为未纳入 CI 的已知失败。**可否纳入 CI 属本 change 外的决策**，此处只登记事实与选项，不擅自扩范围。
- [x] 8.5 按实际结果更新统一控制台方案、菜单/权限/记忆设计及交付文档，区分规划、已实现、已验收与未覆盖；同步本 change 对旧要求的取代矩阵和必要的 capability Purpose。**已完成**：9 份文档加状态块（规划/已实现/已验收/未覆盖），取代矩阵见 `evidence/8-5-doc-closure.md` §3，capability Purpose 同步清单 §4（6 项，执行留给归档）。**顺带更正**：先前把「折叠分组移出焦点顺序」记为已验收，但它有实现（`console.js:2050-2073`）而无测试、亦无 3.4 证据文件，**降级为已实现（未验收）**；另记两处并发导致的他人证据陈旧（`evidence/5-4-public-surface-authority.md` §3 称成员模型目录为死代码，但工作树已实现（`auth/service.py:103` scope `tenant`、`:3393 model_catalog_open()`，29+7 测试通过）→ 记为已实现（未验收）；`evidence/8-1-*` 仍称 `/api/personal/resources` 为活的薄端点，实际已移除）——不改他人证据文件，收口时统一重算。
- [x] 8.6 运行 change 和主规范严格校验，审查合并后的要求无旧个人入口、默认转共享或管理员专属业务流程冲突。**已执行**：`openspec validate --all --strict` → 81 passed / 0 failed；冲突审查四轴结论——① 旧个人入口：六个被 REMOVED 的 requirement 名只在各自主规范的**删除目标文件**内出现，无悬空引用（`console-information-architecture/spec.md`、`member-personal-console/spec.md`）；② 默认转共享：`tenant-default-agent-administration` 要求租户默认只接受同租户已共享可用目标、旧租户 API「不静默转换为用户偏好」、成员调用 403，`user-default-agent-selection` 要求「不得更新租户默认、全局默认或智能体私有归属」，两语义已分离；③ 管理员专属流程：`rbac-authorization` 要求所有权「MUST NOT…授予公共资源维护资格」、管理员身份「不授予私有内容」、放宽「MUST NOT 扩大到共享 Agent 的同类维护动作」，无冲突；④ 判定：**8.8 曾构成归档阻塞**（其 ADDED 要求声称旧独立实现已退役而实际仍在生产路径），已按其强制收口处理。**「整体完成」的声明口径**：本 change 由用户决定强制收口归档，以下项**明确外置、非遗漏**——7.4/7.5/7.6/7.10 移交人工验证（用户决定渠道暂不做自动验收）；8.8 残留 4 项与 8.4 的真实渠道验收、8.10 的 CI 决策均记于各自条目与 `evidence/8-8-component-retirement.md` §7。故本条声明的是「**已收口并按用户决定外置**」，而非「全部切片均通过」。
