## 1. 阶段 A：导航增量与依赖基线

- [x] 1.1 核对当前 Web 菜单、智能体配置页和聊天入口，记录本 change 与 `prd-02-platform-navigation`、`add-workbench-todos` 的菜单差异；按本提案保留相邻菜单变更。
- [x] 1.2 登记 PRD-02 / v1.2 原文缺失及当前用户范围映射，记录 Agent ID、配置版本、会话和工作空间引用基线，确认无需业务数据迁移。
- [x] 1.3 检查实际交付身份模式与读取/运行切片证据；进入阶段 B 前确认 legacy 的既有能力可用，或 database 的身份读取/隔离已真实验收且运行关闭策略可复用。未具备 database 依赖时不得声明该模式可交付，也不扩大到实现身份体系。

## 2. 阶段 B：菜单拆分与使用页

- [x] 2.1 将 `agents` 配置入口移至「管理 → 配置」之后并更名「智能体配置」，更新页面标题与 `VIEW_META`，保持现有配置表单、内部创建跳转和版本冲突规则。
- [x] 2.2 在工作台原智能体位置新增 `agent-workbench` 视图；实现独立激活、面包屑、配置抽屉关闭与历史面板偏好恢复，覆盖两个目录之间来回切换。
- [x] 2.3 实现 `/api/agents?view=workbench` 白名单投影，返回可见启用项的 ID、名称、头像、简介、默认及运行状态；无参数读取与管理写入保持兼容，不新增配置存储。
- [x] 2.4 按阶段 A 确认的模式接入读取和头像访问约束；database 仅复用已验收身份域，运行未开放时返回不可启动原因，不通过客户端参数或默认管理响应绕开边界。
- [x] 2.5 实现默认项优先且其余顺序稳定的卡片列表、单智能体入口、头像/简介回退和「开始对话」动作；不显示配置编辑控件。
- [x] 2.6 实现独立列表状态、进入刷新和手动刷新，区分加载/空态/失败并支持重试；加入请求乱序、导航离开及身份上下文变化时的结果丢弃，不在读取时修改活动会话。
- [x] 2.7 补齐简体中文、繁体中文、英文文案及桌面宽屏/窄屏样式，保证键盘焦点、卡片动作语义和长文本安全展示。

## 3. 阶段 B：指定智能体聊天闭环

- [x] 3.1 调整指定智能体启动入口，在全局状态修改前执行未保存内容保护与最新可用性校验，给出可区分的取消、不可用和加载失败结果。
- [x] 3.2 接入一次新会话提交，显式绑定目标 `agent_id`、使用目标默认工作空间，清除不应继承的项目/会话成员/覆盖设置；维持其他会话历史和后台运行。
- [x] 3.3 从卡片及配置页共用启动流程，进入现有聊天后展示所选智能体名称和头像、聚焦输入框；单智能体模式同样清楚，不自动发消息。
- [x] 3.4 增加启动期间防重复和上下文检查，处理双击、校验中离开、取消后状态不变，以及目标在展示后或首条消息前失效；禁止静默切换到默认智能体。
- [x] 3.5 核对首条及后续消息继续执行显式目标与现有模式校验，工作台目录刷新不能改写已绑定会话的目标；不解除 database 运行关闭策略。

## 4. 阶段 C：验收与兼容检查

- [x] 4.1 进入验收阶段前复核所选部署模式的真实依赖证据；以可运行本地服务验证配置保存、列表更新、点击卡片、首条消息与会话归属闭环，不以 DOM 占位或 mock 运行成功代替功能验收。
- [x] 4.2 为新增读取投影补充行为测试，覆盖默认/单智能体、启用过滤、字段白名单、失败及旧 API 契约；若启用 database，再覆盖跨租户、无读取资格、失效会话及头像访问。
- [x] 4.3 为新启动流程补充关键回归，覆盖 A 项目到 B 根空间、新会话归属、重复点击、取消未保存确认、迟到响应、删除/停用后不回退默认，验证原会话和后台回复不受影响。
- [x] 4.4 浏览器验收菜单顺序、标题/面包屑、配置编辑器和内部创建跳转，覆盖三语、窄屏、键盘操作、长文本、空态/错误重试以及单智能体聊天身份展示。
- [x] 4.5 运行受影响的既有 Agent Web 管理、Agent 管理/Registry/路由及多智能体状态隔离测试；验证 Desktop 默认目录接口兼容，修复由本次修改引入的回归。
- [x] 4.6 完成 feature flag / 模式验收记录：本期不新增功能开关；legacy 保持现有聊天能力，database 若启用则只在真实读取依赖通过后开放列表，并验证不可运行提示与服务端拒绝一致。

## 5. 阶段 C：交付、恢复与文档

- [x] 5.1 核对实施前后的 Agent ID、配置及会话数据归属一致，验证新旧前后端接口兼容边界与仅回退本 change 代码的恢复方式，不删除已生成的用户会话。
- [x] 5.2 更新 Web 使用说明，说明「管理 → 智能体配置」与「工作台 → 智能体」、已保存且启用的展示条件，以及进入后为所选智能体开启新聊天。
- [x] 5.3 汇总实际验收证据和未覆盖模式；实现完成后再归档，合并导航主规范中的旧管理菜单限制，避免与本次双入口要求矛盾，保持其他 change 的独立任务状态。

## 6. 独立验收发现与修复

- [x] 6.1 修复工作台头像尺寸、默认徽标定位、卡片间距与窄屏布局，统一配置标题并改善不可用状态的语义和对比。
- [x] 6.2 启动前读取最新工作台投影，校验目标及运行状态，保护取消、并发点击和导航离开；读取不得改变已绑定会话。
- [x] 6.3 统一聊天身份展示随会话/智能体切换更新，丢弃旧会话的模型与工作空间响应。
- [x] 6.4 修复透明状态提示、配置排序、语言切换和头像刷新；补充实际行为回归测试。
- [x] 6.5 复验宽屏、375px 窄屏、深浅主题及真实页面启动，记录测试结果与未覆盖的运行模式，不以旧勾选状态替代验收。
