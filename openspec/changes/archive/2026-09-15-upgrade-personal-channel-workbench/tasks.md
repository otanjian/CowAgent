## 1. 依赖切片与实施基线

- [x] 1.1 核对 `enable-member-personal-console` 的个人实例、私有目标所有权、身份挑战、治理、凭据、审计和配额切片，以及 `move-personal-menu-to-account` 的菜单实现；记录实际代码与可复跑证据，不以 change 完成状态代替检查。
- [x] 1.2 核对飞书注册和一次性授权的当前作用域、AuthSession、消费并发及失败原子性，核对部署支持的扫码状态存储形态；列出本次个人扫码开放前必须补齐的具体缺口及验证入口。
- [x] 1.3 建立两个租户、两个普通成员及管理员的隔离夹具，包含系统专属助理、自建私有、公共、他人、停用及不存在目标；记录初始运行开关与接受集合，阶段 2 开始前确认基础身份/存储切片可用。

## 2. 个人服务契约与目标边界

- [x] 2.1 实现可共用的个人目标判定，验证非空、同租户、本人私有、实际存在、启用及当前使用资格；由个人创建、目标更新、凭据轮换、启用和底层 `scope='user'` 创建调用。
- [x] 2.2 在写事务内复检成员和所有权，结合现有实例版本、配额、应用占用与审计提交；补充直接 API 越权、空/公共/他人/跨租户/失效目标和并发修改测试，证明失败无部分写入。
- [x] 2.3 扩展个人列表响应，提供最小 `agent_options`、创建动作及原因、按租户策略筛选的渠道类型和对象状态；验证普通成员无需公共或智能体管理页权限，管理员也只能取得本人候选。
- [x] 2.4 为个人 `start_binding` / `unlink` 补齐版本条件和当前 owner 校验；明确更换目标后的关联一致性，保证解除身份关联保留实例目标，覆盖关联与配置并发。
- [x] 2.5 接通启动与入站的目标可用性复检及脱敏状态投影，分开保存、连接、身份关联和可对话；覆盖目标停用/转公共/移交、凭据撤销、治理停用与运行开关收回。
- [x] 2.6 运行个人接口、实例服务、身份关联、凭据、配额和运行拒绝相关测试；修正原测试中默认使用公共目标创建个人实例的夹具，保留公共路径正向对照。此组验证通过后进入阶段 3。

## 3. 个人渠道工作台与共用展示

- [x] 3.1 抽取渠道卡片、图标、凭据字段及扫码/手工模式展示模块，使用显式容器与回调；隔离公共/个人 DOM ID、草稿及请求适配器，并接入脚本装载顺序。
- [x] 3.2 将 `personal-channels` 从通用列表接入专用控制器，保留账号菜单和授权标识，提供标题、当前租户范围、搜索、卡片、接入入口及可新增的空状态。
- [x] 3.3 完成个人新增/编辑表单：必选本人私有目标、无可用目标引导、按后端定义渲染凭据、空密钥保留、错误保留当前有效草稿；保存及维护全部调用个人接口。
- [x] 3.4 根据服务端动作实现启停、凭据更新与撤销，展示目标失效、配额、治理和连接状态；实现本人账号关联挑战、有效期、状态刷新和解除关联交互，不将其显示为清空智能体。
- [x] 3.5 对页面读取、表单、扫码和关联轮询统一接入当前租户/登录会话及代次控制；切换租户、退出、关闭表单、权限失效时清理秘密与消费者，覆盖迟到响应和旧回调。
- [x] 3.6 补齐简体、繁体、英文文案及深浅色布局，运行个人工作台和既有公共渠道前端测试，验证其他个人页面和账号菜单回归；通过后进入扫码集成阶段。

## 4. 飞书与企微个人扫码

- [ ] 4.1 按 1.2 记录的缺口补齐个人飞书注册/授权绑定，覆盖 user、tenant、AuthSession、personal scope、type、create 用途和目标；隔离同一用户的公共/个人会话，旧公共授权不得用于个人创建。
- [ ] 4.2 为个人创建接口与服务包装接通 `scan_ticket`，创建前重验当前私有目标及策略，成功才消费授权；验证并发最多一次创建、失败不消费、跨绑定拒绝及创建授权不能用于编辑。
- [ ] 4.3 在个人表单实现先选目标再发起飞书扫码、自动派生名称和自动保存；更换目标作废原扫码，响应丢失刷新本人列表核实，不盲目重建。
- [ ] 4.4 将企微 SDK 结果填入个人短时草稿，保留显式提交及近期密码校验；覆盖 SDK 失败后的手工入口，禁止把浏览器回调作为免密授权。
- [ ] 4.5 验证扫码取消、过期、会话撤销、换目标、切换租户、重复领取、保存失败与秘密清理；同时回归公共飞书/企微和微信原有协议。阶段 1 的个人扫码前置缺口未补齐时不得把本阶段标为完成。

## 5. 存量兼容与配置闭环验收

- [ ] 5.1 增加存量无效目标的待修复投影和合法目标修复操作；覆盖空目标、公共目标、他人/跨租户、已删除及停用目标，证明不自动改归属、不换默认、不删除凭据历史。
- [ ] 5.2 验证无效目标实例仍可改名、停用、撤销和解除身份关联，合法实例不重建；回归公共实例空目标的租户公共默认路由及公共管理资格。
- [ ] 5.3 使用真实 HTTP 应用和浏览器完成成员及管理员个人工作台验收：空列表创建、两类本人私有目标、配置编辑、拒绝目标、无目标引导、身份挑战和状态展示；断言不读取或写入公共渠道接口。
- [ ] 5.4 覆盖两个租户切换、保存冲突、候选/状态服务失败及扫码迟到回调，记录浏览器和接口证据；模拟提供方结果明确标为配置契约验证。
- [ ] 5.5 验证 `member_personal_console`、`personal_channel_onboarding`、`personal_channel_runtime`、租户策略及逐类型接受范围的独立效果，演练页面回退仍保留服务端校验和关闭操作。此阶段通过后才能进入真实运行验收。

## 6. 真实个人运行验收与按类型开放

- [ ] 6.1 复核真实运行所依赖的身份、私有资源、凭据、配额、执行及适用审批消费方证据；准备飞书与企微真实账号/手机/提供方版本和受控后端。缺少外部条件时本阶段保持未完成，不以 mock 代替。
- [ ] 6.2 完成飞书个人实例真实扫码、落库、连接、本人身份关联及双向消息验收，覆盖他人/陌生人和群聊拒绝、目标失效、解绑、撤销、治理停用及适用审批；保存脱敏的类型、版本、scope 和环境证据。
- [ ] 6.3 完成企微智能机器人个人实例真实 SDK 接入、密码提交、落库、连接、本人关联及双向消息验收，覆盖与 6.2 相同的拒绝和撤权矩阵；单独记录证据，公共实例成功不能代替。
- [ ] 6.4 仅对已通过 6.2 或 6.3 且满足 6.1 的对应类型登记 `PERSONAL_RUNTIME_ACCEPTED_TYPES`，验证登记不会自动打开部署总开关，未验收类型、公共个人入口和微信保持原范围；演练按类型关闭及恢复后重新授权。

## 7. 文档、交付与归档

- [ ] 7.1 更新个人渠道使用说明、已有非法目标的修复方式、扫码提供方差异和运行开关说明，记录无需业务表迁移的核对结果及异常恢复步骤。
- [ ] 7.2 汇总本 change 的实现、配置闭环及飞书/企微真实运行证据，对照两个 capability 的所有场景；未完成的真实验收保留未勾选，不将文档完成、模拟通过或运行关闭视为整体交付完成。
- [ ] 7.3 全部承诺任务完成后，协调 `enable-member-personal-console` 的主规范同步次序；执行 OpenSpec 严格校验和 `scripts/check_change_deltas.py` 的 `check_deltas`，归档后调用 `check_applied` 检查已应用增量。该脚本 CLI 默认还检查历史上游同步冲突清单，本 change 不承接其无关文件覆盖；保留微信/Desktop 兄弟 change 的独立任务状态。
