# Route authorization baseline
#
# Frozen at: origin/rdai@dc760766
# Totals:    _WEB_URLS 115 patterns  vs  ROUTE_POLICY 110 patterns / 140 method entries
# Gap:       5 patterns registered in _WEB_URLS but ABSENT from ROUTE_POLICY
#            -> MUST be filled with a verified policy each (task 2.9), not defaulted
#
# Sources:
#   _WEB_URLS     channel/web/web_channel.py
#   ROUTE_POLICY  auth/http_policy.py
#
# Columns: pattern <TAB> METHOD <TAB> policy <TAB> permission
/	GET	public	
/api/admin/overview	GET	tenant	
/api/agents	GET	tenant	
/api/agents	POST	tenant	
/api/agents/([^/]+)/avatar	GET	tenant	
/api/agents/([^/]+)/avatar	POST	tenant	
/api/agents/([^/]+)/files/([^/]+)	GET	tenant	
/api/agents/([^/]+)/files/([^/]+)	PUT	tenant	
/api/branding	GET	tenant	
/api/branding	POST	tenant	
/api/branding/assets/(.*)	GET	public	
/api/branding/public	GET	public	
/api/branding/reset	POST	tenant	
/api/channels	GET	platform	
/api/channels	POST	platform	
/api/feishu/register	GET	personal	
/api/feishu/register	POST	personal	
/api/file	GET	tenant	
/api/health	GET	public	
/api/history	GET	tenant	
/api/identity/audit	GET	tenant	
/api/knowledge/action	POST	closed	
/api/knowledge/graph	GET	closed	
/api/knowledge/import	POST	closed	
/api/knowledge/list	GET	closed	
/api/knowledge/read	GET	closed	
/api/logs	GET	tenant	
/api/logs/download	GET	tenant	
/api/memory	GET	closed	
/api/memory/content	GET	closed	
/api/messages/delete	POST	tenant	
/api/models	GET	platform	
/api/models	POST	platform	
/api/platform/external-identity-attempts	GET	platform	
/api/platform/tenants	GET	platform	
/api/platform/tenants	POST	platform	
/api/platform/tenants/([^/]+)	GET	platform	
/api/platform/tenants/([^/]+)	POST	platform	
/api/platform/tenants/([^/]+)/admins	GET	platform	
/api/platform/tenants/([^/]+)/admins	POST	platform	
/api/platform/tenants/([^/]+)/agents	GET	platform	
/api/platform/tenants/([^/]+)/agents	POST	platform	
/api/platform/tenants/([^/]+)/authorization/catalog	GET	platform	
/api/platform/tenants/([^/]+)/resources	GET	platform	
/api/platform/tenants/([^/]+)/resources	PUT	platform	
/api/platform/tenants/([^/]+)/roles	GET	platform	
/api/platform/tenants/([^/]+)/roles	POST	platform	
/api/platform/tenants/([^/]+)/roles/([^/]+)	DELETE	platform	
/api/platform/tenants/([^/]+)/roles/([^/]+)	POST	platform	
/api/platform/users	GET	platform	
/api/platform/users/([^/]+)	PATCH	platform	
/api/platform/users/([^/]+)/external-identities	GET	platform	
/api/platform/users/([^/]+)/external-identities	POST	platform	
/api/platform/users/([^/]+)/external-identities/([^/]+)	DELETE	platform	
/api/platform/users/([^/]+)/password	POST	platform	
/api/projects	GET	tenant	
/api/projects/browse	GET	closed	
/api/projects/create	POST	tenant	
/api/projects/manage	DELETE	tenant	
/api/projects/manage	PUT	tenant	
/api/projects/order	POST	tenant	
/api/projects/select	POST	tenant	
/api/prompt/optimize	POST	tenant	
/api/scheduler	GET	closed	
/api/scheduler/delete	POST	closed	
/api/scheduler/run	POST	closed	
/api/scheduler/toggle	POST	closed	
/api/scheduler/update	POST	closed	
/api/sessions	GET	tenant	
/api/sessions/(.*)	DELETE	tenant	
/api/sessions/(.*)	GET	tenant	
/api/sessions/(.*)	PUT	tenant	
/api/sessions/(.*)/clear_context	POST	tenant	
/api/sessions/(.*)/generate_title	POST	tenant	
/api/sessions/(.*)/settings	GET	tenant	
/api/sessions/(.*)/settings	POST	tenant	
/api/skills	GET	tenant	
/api/skills	POST	tenant	
/api/skills/content	GET	tenant	
/api/skills/content	POST	tenant	
/api/tenant	GET	tenant	tenant.info.read
/api/tenant/authorization/catalog	GET	tenant	
/api/tenant/channels	GET	tenant	
/api/tenant/channels	POST	tenant	
/api/tenant/channels/([^/]+)	POST	tenant	
/api/tenant/channels/([^/]+)/active	POST	tenant	
/api/tenant/departments	GET	tenant	tenant.org.read
/api/tenant/departments	POST	tenant	
/api/tenant/departments/([^/]+)	DELETE	tenant	
/api/tenant/departments/([^/]+)	PUT	tenant	
/api/tenant/external-identity-attempts	GET	tenant	
/api/tenant/members	GET	tenant	tenant.members.read
/api/tenant/members	POST	tenant	
/api/tenant/members/([^/]+)	POST	tenant	
/api/tenant/members/([^/]+)/external-identities	GET	tenant	tenant.members.read
/api/tenant/members/([^/]+)/external-identities	POST	tenant	
/api/tenant/members/([^/]+)/external-identities/([^/]+)	DELETE	tenant	
/api/tenant/permissions	GET	tenant	
/api/tenant/roles	GET	tenant	tenant.members.read
/api/tenant/roles	POST	tenant	
/api/tenant/roles/([^/]+)	DELETE	tenant	
/api/tenant/roles/([^/]+)	POST	tenant	
/api/todos	GET	tenant	
/api/todos/(.*)	GET	tenant	
/api/todos/(.*)/events	GET	tenant	
/api/todos/(.*)/source	GET	tenant	
/api/todos/summary	GET	tenant	
/api/tools	GET	tenant	
/api/version	GET	public	
/api/voice/asr	POST	tenant	
/api/voice/tts	POST	tenant	
/api/weixin/qrlogin	GET	closed	
/api/workspace/meta	GET	tenant	
/api/workspace/read	GET	tenant	
/api/workspace/resolve	GET	tenant	
/api/workspace/search	GET	tenant	
/api/workspace/tree	GET	tenant	
/api/workspace/write	POST	tenant	
/assets/(.*)	GET	public	
/auth/check	GET	public	
/auth/context	GET	tenant	
/auth/login	POST	public	
/auth/logout	POST	public	
/auth/me	GET	personal	
/auth/password	POST	personal	
/auth/profile	PATCH	personal	
/auth/profile/avatar	GET	personal	
/auth/profile/avatar	POST	personal	
/cancel	POST	tenant	
/chat	GET	tenant	
/config	GET	platform	
/config	POST	platform	
/mcp/oauth/callback	GET	public	
/message	POST	tenant	
/poll	POST	tenant	
/preview/(.+)	GET	public	
/stream	GET	tenant	
/upload	POST	tenant	
/uploads/(.*)	GET	tenant	
/v1/chat/completions	POST	tenant	

# --- GAPS: in _WEB_URLS but NOT in ROUTE_POLICY (policy undecided) ---
/admin	?	UNREGISTERED	-
/api/identity/administered-tenants	?	UNREGISTERED	-
/api/scenes	?	UNREGISTERED	-
/api/scenes/activate	?	UNREGISTERED	-
/api/scenes/workbench/import	?	UNREGISTERED	-

# --- RESOLVED in change fork-decoupling-and-tenant-hardening (group 2/4) ---
# The five gaps above are now registered in channel/web/route_registry.py with
# these verified policies (not defaults). See evidence.md for rationale.
# The three scenes routes gained the functional permission they execute under
# (task 4.9: the scene console is the chat consumer surface, so chat.use).
/admin	GET	tenant	-
/api/identity/administered-tenants	GET	personal	-
/api/scenes	GET	tenant	chat.use
/api/scenes/activate	POST	tenant	chat.use
/api/scenes/workbench/import	POST	tenant	chat.use
# The log surfaces read the *process-global* run.log (every tenant's activity),
# so they are platform control-plane data, not tenant data (task 4.11). They are
# re-policy'd from tenant to platform rather than left readable by any member.
/api/logs	GET	platform	-
/api/logs/download	GET	platform	-

# --- Also discovered by the three-leg coverage invariant (leg 3) ---
# Implemented by the handler and documented, but previously unregistered
# (the completeness gate answered 405):
/api/weixin/qrlogin	POST	closed	-
/api/todos	POST	tenant	-
/api/todos/(.*)	PATCH	tenant	-
# Declared in ROUTE_POLICY but NOT implemented by any handler (dead entry; the
# gate let the request through and web.py answered 405):
/api/sessions/(.*)	GET	REMOVED	-

# --- Revised by browser verification (task 10.1) ---
# A top-level document navigation cannot send X-Tenant-ID. With the gate now
# enforcing the table, `/` -> `/chat` answered 400 missing_tenant before the
# handler on every browser visit, so the login UI never rendered. The shell is
# not tenant data: it is a document, and every data API it loads keeps its own
# tenant/platform policy (the gate's purpose). Both are therefore public, which
# is also the pre-change effective behavior (the gate was a no-op for them).
/chat	GET	public	-
/admin	GET	public	-

# --- Added by add-default-user-avatars ---
# Per-account avatar read for the sidebar/menu/hero and the admin member /
# platform-account lists. `personal` (any authenticated session); the handler
# narrows to self OR platform admin OR a caller sharing an active tenant with
# the target, and returns 404 (never the bundled default bytes) when the account
# has no upload. The default avatars are static assets under /assets/avatars/.
/api/users/([^/]+)/avatar	GET	personal	-

# --- Added by add-tenant-archive ---
# `DELETE /api/platform/tenants/{id}` archives (soft-deletes) a tenant: the
# handler only sets `archived_at`/`active=0` and retains all data, so the route
# is `platform` (it mutates platform-level data, like the other tenant writes on
# this pattern). Restore reuses the existing `POST` on the same pattern via
# `operation=restore`, so no new method entry is needed for it.
/api/platform/tenants/([^/]+)	DELETE	platform	-

# --- Changed by open-tenant-knowledge-console ---
# The five knowledge consumers were `closed` (deferred), so database mode
# short-circuited them to 503 `database_unavailable` and the console page hung
# on its loading placeholder. They are now `tenant` consumers: the gate only
# authenticates and requires a valid tenant selection, the handler enforces
# `knowledge.read` for reads and a data-root + Agent-ownership decision for
# writes, plus the tenant-Agent binding and private-owner
# scoping. The policy entry carries no `permission` because one route serves
# both a read and a write (different permissions).
/api/knowledge/action	POST	tenant	-
/api/knowledge/graph	GET	tenant	-
/api/knowledge/import	POST	tenant	-
/api/knowledge/list	GET	tenant	-
/api/knowledge/read	GET	tenant	-

# --- Added by enable-member-personal-console ---
# The member personal console surface (task 9.3). `personal` (any authenticated
# session) is the *whole* route-level claim: the gate proves a session, and the
# handler then takes the tenant from the verified request context and the owner
# from that session, so neither a body field nor an `X-Tenant-ID` can name
# another member or another tenant. No permission is attached because the same
# path serves several operations with different permissions (list vs edit vs
# clear); the functional/resource checks run inside the handler. The public
# management surfaces stay where they were: `/api/channels` is `platform`,
# `/api/tenant/channels` is `tenant` (tenant_admin), `/api/agents` is `tenant`.
/api/memory/personal	GET	personal	-
/api/memory/personal	POST	personal	-
/api/memory/personal/content	GET	personal	-
/api/personal/channels	GET	personal	-
/api/personal/channels	POST	personal	-
/api/personal/channels/([^/]+)	GET	personal	-
/api/personal/channels/([^/]+)	POST	personal	-
/api/personal/resources	GET	personal	-
/api/personal/resources	POST	personal	-

# --- evolution ------------------------------------------------------------
#
# Append-only, same two rules as below the header: a later row for the same
# pattern/method overrides the frozen row above it, so the historical value
# stays readable and the change stays traceable. A section names the change,
# the source of the new policy, the authorization behind it and the handler.
#
# 2026-09-15  change: complete-database-capability-parity (tasks 3.7 / 4.3 / 9.2)
#   The five scheduler management methods leave `closed` and carry the slice's
#   own policy. Source: auth/capability_matrix.py (the single capability
#   registry) consumed by channel/web/route_registry.py through S(). Handler:
#   the existing Scheduler*Handler classes in channel/web/web_channel.py, which
#   now authorize through the one service shared with the Agent tool and the
#   background loop (agent/tools/scheduler/authorization.py) instead of opening
#   the task store themselves.
#   Policy `tenant`: a task belongs to one tenant, and the slice declares no
#   route-level permission, so the gate resolves the session *and* the tenant
#   selection and hands that same context to the handler -- which is what the
#   handlers ask for themselves. Owner/agent/resource checks stay per object in
#   the handler, and `run` is additionally gated by the slice's acceptance
#   evidence (R1).
/api/scheduler	GET	tenant	
/api/scheduler/delete	POST	tenant	
/api/scheduler/run	POST	tenant	
/api/scheduler/toggle	POST	tenant	
/api/scheduler/update	POST	tenant	

# 2026-09-15  change: complete-database-capability-parity (tasks 5.1-5.3 / 9.2)
#   The two legacy memory reads leave `closed` and carry the slice's own policy
#   and permission. Source: auth/capability_matrix.py (`memory_browse`) consumed
#   by channel/web/route_registry.py through S(). Handler:
#   channel/web/memory_console.py, which adapts the legacy surface onto the
#   delivered personal memory service -- an explicit personal | private_agent |
#   shared target, the owner check *before* the read, and a stable machine code
#   for every refusal (never a 200 carrying an error message).
#   Policy `tenant` + permission `memory.read`: the content belongs to one
#   tenant, so the gate resolves the session and the tenant selection; which
#   object the caller may read (their own memory, or a private Agent they own)
#   stays a per-object decision inside the handler.
#
# 2026-09-15  change: unify-console-by-data-scope (task 5.1, second half)
#   The same page's edit/delete/clear for the *Agent* domain leave `closed`.
#   One route per verb so the gate can close one verb without closing the
#   others. Access class is `config` (they write stored data) rather than
#   `execute`, which this registry reserves for actions that run something.
#   Authorization is not a permission id: the handler resolves the target and
#   applies the caller's range (owner for a private Agent, tenant-administration
#   qualification for a shared one) before any mutation. The member's own memory
#   keeps its separate `/api/memory/personal*` write rows below/above — these
#   verbs refuse `scope=personal` so there is exactly one write path per domain.
/api/memory	GET	tenant	memory.read
/api/memory/content	GET	tenant	memory.read
/api/memory/save	POST	tenant	memory.read
/api/memory/delete	POST	tenant	memory.read
/api/memory/clear	POST	tenant	memory.read

# 2026-09-15  change: complete-database-capability-parity (tasks 6.5-6.6 / 9.2)
#   Project browsing leaves `closed` and the controlled local import becomes a
#   first-class surface. Source: auth/capability_matrix.py (`project_browse`)
#   consumed by channel/web/route_registry.py through S(). Handlers:
#   channel/web/project_import.py (the import preview/publish/cancel seam) and
#   the existing project handlers in channel/web/web_channel.py, whose upstream
#   `_import_local_file` loopback + per-start token + owned-session contract is
#   preserved and *added* to (one-time handle, staging, atomic publish, quota
#   reservation) rather than replaced.
#   Policy `tenant`: the projects belong to one tenant and one member, so the
#   gate resolves the session and the tenant selection; the per-object owner,
#   path and session checks stay in the handler, which re-resolves every path
#   through common/safe_fs against the verified identity.
/api/projects/browse	GET	tenant	
/api/projects/import/preview	POST	tenant	
/api/projects/import	POST	tenant	
/api/projects/import/cancel	POST	tenant	

# 2026-09-15  change: complete-database-capability-parity (tasks 7.6-7.7 / 9.2)
#   The two WeChat scan methods leave `closed`. Source:
#   auth/capability_matrix.py (`weixin_scan`) consumed by
#   channel/web/route_registry.py through S(). Handler:
#   channel/web/scan_onboarding.py, which replaces the process-global
#   `_qr_state` with a session-bound state machine (actor/AuthSession/tenant/
#   scope/owner/provider/target) and commits the authorization consumption, the
#   instance/credential write, the quota reservation and the audit entry in one
#   transaction.
#   Policy `tenant` (config class): the caller must belong to a tenant, while
#   whether *this* caller may bind *this* instance stays a per-object decision
#   inside the handler; the QR result itself is a short-lived secret and is
#   never part of a route-level grant.
/api/weixin/qrlogin	GET	tenant	
/api/weixin/qrlogin	POST	tenant	
# 2026-09-16  change: unify-console-by-data-scope (task 5.5, first half)
#   The standalone personal resource surface is retired. A member's own
#   parameters for a granted tool or skill are edited in that resource's detail
#   component on the formal 工具与技能 page, so the write arrives on the page's
#   own endpoint -- one surface, one authority, and no second path that could
#   drift from the first. The owner-scoped storage/service
#   (`save_personal_resource_config` / `clear_personal_resource_config`) is
#   unchanged: it is what the shared endpoint now writes through.
#   Both rows are REMOVED (the pattern is no longer registered, so the gate
#   answers 404 like any other unknown URL) rather than moved, because the
#   *authority* is what the two rows below pin: the personal verbs keep their
#   place on the endpoints that already carry the read.
/api/personal/resources	GET	REMOVED	-
/api/personal/resources	POST	REMOVED	-

# 2026-09-16  change: unify-console-by-data-scope (task 5.5, second half)
#   `/api/tools` and `/api/skills` gain the personal-parameter verbs on POST.
#   Policy `tenant`: the gate resolves the session *and* the tenant selection,
#   while `actor_user_id` is the session's and the resource's grant stays a
#   per-object decision in the handler -- there is no `user_id` parameter to
#   widen, so a route-level permission would be a second, weaker authority.
#   The public definition/enable verbs on the same methods keep their own
#   management qualification inside the handler (task 5.4), so a member holding
#   only `tool.execute` / `skill.use` reaches their own parameters and nothing
#   else.
/api/tools	POST	tenant	-
/api/skills	POST	tenant	-

# 2026-09-16  change: unify-console-by-data-scope (task 8.8, recompute)
#   Recomputed from the registry rather than hand-patched, because the file had
#   drifted from `route_registry.ROUTE_POLICY` since task 5.5. Current state:
#   128 patterns / 166 method entries; every non-REMOVED baseline row matches the
#   derived policy and permission (0 mismatches), and the three REMOVED rows are
#   the two from task 5.5 above plus the historical `GET /api/sessions/(.*)`.
#
#   Task 8.8 retired the *component* axis -- the `personal-console.js` module,
#   its i18n namespace and the issuance of the five `personal.*` page ids. That
#   has no route footprint: the four thin personal endpoints below stay
#   registered, unchanged, because they are the spec-sanctioned forwarding to
#   the unified services (design.md D-memory: "`/api/memory/personal*` 只作参数
#   转换和正式服务委托"; the memory/channel rows keep the services' own
#   owner/version/generation rules). Their only in-repo caller was the retired
#   module, so the rows are now pinned by `tests/test_route_registry.py`
#   (`PersonalConsoleRouteTests`) rather than by a live frontend call.
#
#   The recompute also found three registry entries with no baseline row, from
#   the archived change `complete-desktop-and-scan-real-acceptance`
#   (`fork:desktop-auth`, `channel/web/auth_handlers.py`). They are recorded
#   here so the frozen file covers the registry; `public` is the registry's own
#   policy: the consent page and the code exchange must work before a tenant
#   selection exists, and the authorization itself is the one-time code bound to
#   the initiating Web AuthSession, PKCE S256 verifier and exact origin.
/auth/desktop/authorize	GET	public	-
/auth/desktop/authorize	POST	public	-
/auth/desktop/token	POST	public	-

